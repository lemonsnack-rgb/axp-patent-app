# CK.Patent 순회 체크리스트 (태스크 유실 방지용 정본)

규칙: 요소 1개당 최소 1케이스. `[x]` = 실측 완료(실측로그.md에 결과 기재), `[ ]` = 미실측.
이 파일이 남은 작업의 정본이다. 컨텍스트가 끊겨도 이 파일만 보면 이어서 진행할 수 있다.

---

## 1단계. 공통 셸

- [x] `특허 검색` link → `/patent-front/patents`
- [x] `논문 검색` link → `/patent-front/papers`
- [x] `한국어` (aria-label `언어 설정`) 개방 → 옵션 3개 채집
- [x] 언어 `English` 전환
- [x] 언어 `한국어` 복귀
- [x] 언어 `简体中文` 전환 → `?lang=zh-cn`, 전면 중문화, `<html lang>`=ko 유지
- [x] `사이드바 접기/펴기` 접기 → 240px→64px, 라벨 `사이드바 펼치기` 로 변경
- [x] `사이드바 펼치기` 펴기 → 복원
- [x] `CK.Patent` 로고 → `a`·`button` 없음, 클릭 대상 아님
- [x] breadcrumb `논문 검색` 표시 전환 (특허 측 `특허 검색` 표시만 확인)
- [~] `문의하기` — 지시에 따라 `추후 추가` 로 기재, 실행 안 함
- [~] 푸터 외부링크 12개 — 지시에 따라 `추후 추가` 로 기재, href 만 실측

## 2단계. 검색식 작성·유효성 검증 (특허)

- [x] F1 빈 값 → `검색` disabled
- [x] F2 `and` → 활성, 실행됨, `0건`
- [x] F3 `(하이브리드 and 자동차` → disabled + V1
- [x] F4 `하이브리드 and` → 활성
- [x] F5 `"하이브리드 자동차` → disabled + V2
- [x] F6 `하이브리드 and 자동차` → `1,684건`
- [x] F7 `@#$%` → 활성, `0건`
- [x] F8 `하이브리드 AND 자동차` → `1,684건` (대소문자 무관)
- [x] F9 `하이브리드 or 자동차` → `173,413건`
- [x] F10 `하이브리드` → `16,607건`
- [x] F11 `하이브리드 not 자동차` → `14,923건` (산술 정합)
- [x] F12 `*수소 자동차*` → `155,748건`
- [x] F13 `자동차` → `158,490건`
- [x] F14 `KEY:(자동차)` → `158,490건`
- [x] F15 `TAC:(자동차)` → `168,578건`
- [x] F16 `DSC:(자동차)` → `383,150건`
- [x] URL 인코딩·새로고침 복원 확인 (결함 아님 확정)
- [x] F17 공백만 입력
- [x] F18 `or` 단독 / `not` 단독
- [x] F19 연산자 연속 `하이브리드 and and 자동차`
- [x] F20 괄호 정상 조합 `(하이브리드 or 수소) and 자동차`
- [x] F21 닫는 괄호만 `하이브리드 and 자동차)`
- [x] F22 필드절 + 연산자 조합 `KEY:(자동차) and TAC:(수소)`
- [x] F23 일자 범위 표기 `AD:([2020-01-01 ~ 2020-12-31])`
- [x] F24 개별 필드코드 `TI:(자동차)` `AB:` `CL:` `CLI:` `CLA:` `AN:` `PN:` `RN:` `AP:` `APD:` `INV:` `AG:` `IPCM:` `CPCM:`
- [x] F25 매우 긴 검색식 (길이 상한 존재 여부)
- [x] F26 존재하지 않는 필드코드 `XXX:(자동차)`
- [x] F27 한글 연산자 `그리고` / `또는` 인식 여부

## 3단계. 특허 검색 입력 필터

- [x] 초기 상태·기본값 채집 (`기간`=`최근 5년`, `검색어 범위`=`명칭+요약+독립항`, `국가`=`KR 한국`·`US 미국`, `문헌종류`=`전체`, `상태`=`전체`)
- [x] `검색어 범위` `명칭+요약+전체청구항` → `168,578건`, scope=`TITLE_ABSTRACT_ALL_CLAIMS`
- [x] `검색어 범위` `상세설명` → `401,480건`, scope=`DESCRIPTION`
- [x] `국가설정 (12)` 대화상자 개방 → 추가 국가 12개 채집
- [x] `국가설정` 대화상자 Escape 닫기
- [x] `항목별 상세검색` 펼침 → 필드코드 카탈로그 채집
- [x] 필터 적용 시점 확인 → 토글만으로는 재검색 안 됨. `검색` 클릭 시 적용 (전 케이스 절차에 반영 필요)
- [x] `초기화` → URL `?lang=ko`, 검색식·필터·결과 전부 초기 복귀, 언어는 유지
- [x] 기준값 재확인 `자동차` = `158,490건`
- [x] `국가` `KR 한국` 토글 해제 → `countries=US`, `0건`
- [x] `국가` `US 미국` 토글 해제 → `countries=KR`, `158,490건`
- [x] `국가` `전체` 클릭 → `countries=`(빈 값), `158,490건`
- [!] `검색어 범위` `명칭+요약+독립항` scope 파라미터명 → **확인 불가 확정**(기본값은 URL 미노출). `확인 필요` 로 기재
- [x] `국가` `JP 일본` `CN 중국` `EP 유럽` `PCT` `DE 독일` `GB 영국` 각 토글
- [x] `국가설정` 대화상자에서 `선택 추가` 실행 → 국가 버튼 목록 변화
- [x] `국가설정` 대화상자 `취소` 버튼
- [x] `문헌종류` `전체` `공개` `등록` `실용공개` `실용등록` 각 토글 → 건수 변화
- [x] `기간` `전체` `최근 10년` `최근 20년` 각 선택 → 건수 변화
- [x] `시작일`/`종료일` 직접 입력 (spinbutton `연도`/`월`/`일`)
- [x] `날짜 선택도구 표시` 버튼 (시작일·종료일 각 1개)
- [x] 시작일 > 종료일 역전 입력 시 검증
- [x] `상태` `전체` 클릭
- [x] `상태 A` `출원` `심사중` `등록` 각 토글
- [x] `상태 I` `거절` `소멸` `취하` `포기` `무효` 각 토글
- [x] `초기화` 클릭 → 모든 필터·검색식 초기 상태 복귀 확인
- [x] `항목별 상세검색` 필드 입력 → 검색식 자동 생성 여부
- [x] `항목별 상세검색` `IPCM`/`CPCM` 옵션 `Original or Current` / `Current Only` 선택
- [x] `+ 검색 필드 추가·확장` 클릭
- [x] `항목별 상세검색` 접기(토글 재클릭)
- [x] `검색 히스토리 (N)` 펼침 → 항목 구조·재실행 동작
- [x] `검색 히스토리` 항목 클릭 → 검색식 복원 여부
- [x] `검색 히스토리` 삭제 기능 존재 여부 → **실제 삭제까지 실행 가능**(사용자 승인). 실행하고 결과 기록
- [x] 검색 버튼 Enter 키 제출 여부

## 4단계. 특허 검색결과 목록

- [x] 표 컬럼 채집 (`상태` `문헌번호` `원문` `발명의 명칭` `출원인` `출원일` `IPC`)
- [x] 결과 없음 상태 문구 (`검색 결과가 없습니다.\n검색어나 필터 조건을 바꿔 다시 시도해 주세요.`)
- [x] `정렬` combobox (렌더 `매칭순`) 개방 → 옵션 채집 → 각 옵션 적용
- [x] `페이지당 개수` combobox (렌더 `20개`) 개방 → 옵션 채집 → 각 옵션 적용
- [x] 좌측 필터 `국가` 펼침·적용
- [x] 좌측 필터 `권리상태` 펼침·적용
- [x] 좌측 필터 `출원연도` 펼침·적용
- [x] 좌측 필터 `IPC` 펼침·적용
- [x] `모든 필터` 대화상자 개방 → 구성 채집 → 적용·취소
- [x] `전체 선택` 체크박스
- [x] 개별 행 선택 체크박스
- [x] 선택 후 노출되는 액션 존재 여부
- [x] 행 클릭 → 상세 이동 동작 (기지 결함 대상)
- [x] `새 탭에서 열기` → 새 탭 상세 진입
- [x] `원문` 컬럼 요소 클릭 (PDF 여부)
- [x] 페이지네이션 `1`~`10` 숫자 클릭
- [x] 페이지네이션 `다음 페이지 그룹` / `이전 페이지 그룹`
- [x] `논문으로` 버튼 → 논문 검색으로 검색식 유지 이동 여부
- [x] `IPC` 컬럼 값이 `-` 인 원인 확인 가능 여부 (관측만)
- [x] 문헌번호 표기 형식 차이 (`1020020054831 A` vs 상세 URL `KR 10-2620258 B1`)
- [x] 검색어 하이라이팅 여부

## 5단계. 특허 상세

- [x] 섹션 목록 채집 (`서지사항` `인명정보` `요약` `상세설명` `청구범위` `패밀리정보` `인용·피인용` `분류코드` `기타정보`)
- [x] 서지사항 필드 전체 채집 (문헌번호~서열목록)
- [x] 섹션 탭 9개 각 클릭 → 해당 섹션 스크롤·표시
- [~] `원문 PDF 다운로드` — 파일 다운로드 기능은 **구현 예정**(사용자 확인). 다운로드 실행하지 않고 버튼 존재·활성 상태만 기록
- [x] `독립항` / `전체청구항` 토글
- [x] `확대` 버튼 (도면)
- [x] `도면 이미지 없음` 상태 표시
- [x] `타임라인` 표시 값
- [x] `패밀리 문헌 (0건)` 빈 상태
- [x] `인용 (0건)` 빈 상태
- [x] `피인용 (2건)` 목록
- [x] `선행기술문헌 (5건)` 목록
- [x] `IPC` / `CPC` 분류코드 표시
- [x] `권리변동 이력` 표시
- [x] `행정처리(수발신) 이력` (`접수·발송일` `서류명` `처리상태`)
- [x] `탭 닫기` 클릭
- [x] 존재하지 않는 문헌번호 URL 직접 진입 시 오류 화면

## 6단계. 논문 검색 입력

- [x] 필터 구성 채집 (`발행연도` `검색어 범위` `항목별 상세검색` `검색 히스토리 (4)`)
- [x] 특허와의 차이 확인 (`input[type=number]` YYYY vs 특허 `input[type=date]`)
- [x] `발행연도` `전체` `최근 1년` `최근 5년` `최근 10년` 각 선택 → 건수 변화
- [x] `발행연도` 직접 입력 (YYYY 2칸), 역전 입력 검증
- [x] `검색어 범위` `제목+초록` / `제목+초록+전문` → 건수 변화 + scope 파라미터
- [x] 논문 검색식 유효성 검증 (특허와 동일 문구인지 확인)
- [x] 논문 필드코드 카탈로그 채집 (`항목별 상세검색` 펼침)
- [x] 논문 `초기화`
- [x] 논문 `검색 히스토리 (4)` 펼침
- [x] 논문 기본값 채집 (a11y 트리)

## 7단계. 논문 검색결과 목록

- [x] `296건` (`도서`) 결과 목록 구조 채집
- [x] 초록 미리보기 잘림 표시 확인
- [x] `정렬` combobox 옵션 채집·적용
- [x] `페이지당 개수` combobox 옵션 채집·적용
- [x] 좌측 필터 `발행연도` `저널명` `모든 필터`
- [ ] `전체 선택` / 개별 선택
- [x] 행 클릭 → 상세 이동 (**기지 결함**: 동작하지 않음 — 재현 확인)
- [x] `새 탭에서 열기` → 동작 여부 (기지 결함 범위 판정)
- [x] 페이지네이션 `1`~`10`, `이전/다음 페이지 그룹`
- [x] 미리보기 패널 `◀` `▶` `닫기`
- [x] 미리보기 패널 `본문 보기\n내부 전용`
- [x] `특허로` 버튼 → 특허 검색 이동
- [x] 검색어 하이라이팅 여부

## 8단계. 논문 상세

- [x] 필드 구성 채집 (`저자명` `발행일` `저널명` `저널명(영문)` `분야` `키워드` `초록` `영문초록`)
- [x] `초록은 미리보기만 가능합니다.` 표시 확인
- [x] 초록·영문초록 잘림 + `…` 확인
- [x] `관련 논문` `검색결과 상위 10건` 목록·링크 확인
- [x] `출처 이동` 클릭 → 외부 이동 (scienceon.kisti.re.kr)
- [ ] `본문 보기\n내부 전용` 클릭
- [x] `관련 논문` 항목 클릭 → 해당 논문 상세 이동
- [x] `탭 닫기` 클릭
- [x] `저널명`/`저널명(영문)`/`분야` 가 `—` 인 케이스 표시 확인
- [x] 초록 실제 표시 문자수가 규격(구간표)과 일치하는지 계수 검증
- [x] 존재하지 않는 uuid 직접 진입 시 오류 화면

## 9단계. 마감 작업

- [ ] 요소 인벤토리 ↔ 케이스 1:1 대조, 미대응 0건 확인
- [ ] 결함 후보 D1~D6 + 추가분을 설계 원본과 대조해 판정 확정
- [ ] `docs/테스트시나리오.md` 작성
- [ ] `docs/테스트-결함목록.md` 작성
