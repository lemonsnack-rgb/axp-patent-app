# CK.Patent QC 테스트 케이스 목록 (121건)

- **대상**: https://www.copykiller.com/patent-front/
- **구성**: 공통 셸 12 · 특허 검색식 29 · 특허 입력 필터 32 · 특허 결과 목록 11 · 특허 상세 12 · 논문 입력 8 · 논문 결과 목록 9 · 논문 상세 8
- **기준 실측**: 2026-08-10 (app-version 2.16.1) — `참고 관측값`은 당시 스냅샷이며, 건수 판정은 불변식(합계 정합·단조성 등) 기준
- **판정 값**: Pass / Fail / 확인 필요 / 근거 없음 / 미실측 / 추후 추가 / 구현 예정
- **원본 문서**: `docs/테스트 시나리오/QC-테스트시나리오.md` (표기 규칙 · 판정 정의 · 주의사항 T1~T10 · 불변식 · 실행 방법) / `QC-테스트시나리오.xlsx` (기록 워크북)

**수행 방법**: 각 케이스의 사전조건 → 수행 절차(①②③ 순) 순으로 조작하고 기대 결과와 대조한다. 결과 기록은 `QC-테스트시나리오.xlsx`의 [케이스] 시트(동일 ID 행)에 한다. 근거 없는 항목은 `근거 없음`으로 기록만 남기고, Fail 등재 전 원본 문서의 기지 결함(§15)·결함 아님 확정(§16) 목록과 대조한다.

## 공통 셸

| 케이스 ID | 케이스명 | 요소 | 사전조건 | 수행 절차 | 기대 결과 · 판정 기준 | 참고 관측값(2026-08-10) · 회귀 연계 |
|---|---|---|---|---|---|---|
| TC-CMN-001 | 사이드바 메뉴 이동 — 특허 검색 | `특허 검색` (link) | 로그인 상태 (화면 무관) / (공통: 로그인 상태, 언어 ko (§3 P1~P4)) | ① 사이드바 `특허 검색` 클릭 ② URL·화면 확인 | `/patent-front/patents` 로 이동, 특허 검색 화면 표시 — 근거: 실측 href 확인(2026-08-10) |  |
| TC-CMN-002 | 사이드바 메뉴 이동 — 논문 검색 | `논문 검색` (link) | 로그인 상태, 언어 ko (§3 P1~P4) | ① 사이드바 `논문 검색` 클릭 ② URL·화면·breadcrumb 확인 | `/patent-front/papers` 로 이동, breadcrumb `논문 검색` 표시 — 근거: 실측(2026-08-10) |  |
| TC-CMN-003 | 언어 메뉴 개방 | `언어 설정` (button, 렌더 텍스트 `한국어`, `haspopup="menu"`) | 로그인 상태, 언어 ko (§3 P1~P4) | ① 헤더 `한국어` **실제 클릭** (T1: 스크립트 클릭은 열리지 않음) ② 메뉴 항목 채집 | `근거 없음` (설계에 다국어 명세 없음 — 동작 기록만). 참고: `role="menu"` 개방, 항목 3개 `한국어`(checked)/`English`/`简体中文` |  |
| TC-CMN-004 | 언어 전환 — English |  | 로그인 상태, 언어 ko (§3 P1~P4) | ① `언어 설정` → `English` 클릭 ② URL·사이드바·필터 라벨·`<html lang>` 확인 | `근거 없음` (라벨 전환은 기록). 확인 지점: URL `?lang=en`, 사이드바 `Patent search`/`Paper search`, `aria-label` `Language` | 회귀 연계: `<html lang>`이 `ko`로 남으면 BUG-013 재현 (§15) |
| TC-CMN-005 | 언어 전환 — 简体中文 |  | 로그인 상태, 언어 ko (§3 P1~P4) | ① `언어 설정` → `简体中文` 클릭 ② URL·라벨·`<html lang>`·기존 검색식/`scope` 파라미터 유지 확인 | `근거 없음` (기록). 확인 지점: URL `?lang=zh-cn`, 사이드바 `专利检索`/`论文检索`, 중문판 전각 괄호 `国家设置（12）`, 검색식·`scope` 유지 | 회귀 연계: `<html lang>` `ko` 유지 시 BUG-013 |
| TC-CMN-006 | 언어 전환 — 한국어 복귀 |  | 로그인 상태, 언어 ko (§3 P1~P4) | ① `Language`(또는 `语言设置`) → `한국어` 클릭 | URL `?lang=ko`, 모든 라벨 한국어 복귀 — 근거: 실측(2026-08-10) Pass |  |
| TC-CMN-007 | 사이드바 접기 | `사이드바 접기/펴기` (button, aria-label만 존재) | 로그인 상태, 언어 ko (§3 P1~P4) | ① 사이드바 상단 아이콘 버튼 클릭 ② 폭·메뉴 텍스트·로고·버튼 라벨 확인 | `근거 없음` (기록). 참고 관측: 폭 240px→64px, 메뉴 텍스트·로고 사라짐, 라벨이 `사이드바 펼치기`로 변경(T3) | 회귀 연계: `aria-expanded` 부재 → BUG-019 (개선) |
| TC-CMN-008 | 사이드바 펴기 | `사이드바 펼치기` (button — 접힘 상태에서의 라벨) | 로그인 상태, 언어 ko (§3 P1~P4) | ① 접힌 상태에서 아이콘 버튼 클릭 | 라벨 `사이드바 접기/펴기` 복귀, 메뉴 텍스트·로고 복원 — 근거: 실측(2026-08-10) Pass |  |
| TC-CMN-009 | 로고 클릭 가능 여부 | `CK.Patent` (image, `/patent-front/icons/ck-patent-logo.png`) | 로그인 상태, 언어 ko (§3 P1~P4) | ① 로고 상위 요소에 `a`·`button` 존재 여부 확인 | `근거 없음` (기록). 참고 관측: 클릭 대상 아님(홈 이동 링크 없음) |  |
| TC-CMN-010 | breadcrumb 표시 전환 | `breadcrumb` (navigation) | 로그인 상태, 언어 ko (§3 P1~P4) | ① `/patents`에서 표기 확인 ② `논문 검색`(또는 `논문으로`)으로 전환 후 표기 확인 | 특허 화면 `특허 검색` / 논문 화면 `논문 검색` — 근거: 실측(2026-08-10, `논문으로` 전환 시 breadcrumb 전환 확인) |  |
| TC-CMN-011 | 문의하기 | `문의하기` (button) | 로그인 상태, 언어 ko (§3 P1~P4) |  | 판정 고정: **추후 추가** — 외부 상담 채널. 실행하지 않는다 |  |
| TC-CMN-012 | 푸터 외부링크 (11종 + 메일 2종) | `help@copykiller.com` `원격지원` `marketing@muhayu.com` `Blog` `Youtube` `LinkedIn` `Instagram` `개인정보 처리방침` `이용약관` `회사소개` `채용안내` | 로그인 상태, 언어 ko (§3 P1~P4) | ① href 값만 대조 (실제 이동은 실행하지 않음) | (href 기준값, 실측 2026-08-10): `mailto:help@copykiller.com` / `http://939.co.kr/copykiller/` / `mailto:marketing@muhayu.com` / `http://blog.naver.com/muhayuin` / `https://www.youtube.com/channel/UC1BsUvbI3poyxKV8F0GYCpA` / `https://kr.linkedin.com/company/muhayu` / `https://www.instagram.com/copykiller_official/` / `https://www.muhayu.com/privacy_policy` / `https://www.muhayu.com/policy` / `https://www.muhayu.com/muhayu` / `https://muhayu.ninehire.site/` / 판정 고정: **추후 추가** (href 대조만 수행) |  |

## 특허 검색식

| 케이스 ID | 케이스명 | 요소 | 사전조건 | 수행 절차 | 기대 결과 · 판정 기준 | 참고 관측값(2026-08-10) · 회귀 연계 |
|---|---|---|---|---|---|---|
| TC-PAT-FRM-001 | 빈 검색식 |  | 특허 검색 화면, 초기화 직후. ※ 건수 케이스(008~018)는 `기간`=`전체` 명시 설정 (§9 공통 사전조건) | ① 검색식을 비운 상태 유지 ② `검색` 버튼 상태 확인 | `검색` 버튼 `disabled`, 빈 상태 화면 유지 — 근거: 설계 `PAT-INP-041` precondition(`값 1개 이상`) |  |
| TC-PAT-FRM-002 | 연산자 단독 `and` |  | 특허 검색 화면, 초기화 직후. ※ 건수 케이스(008~018)는 `기간`=`전체` 명시 설정 (§9 공통 사전조건) | ① `and` 입력 ② 버튼 상태·안내 문구 확인 ③ 활성이면 `검색` 클릭, 결과·히스토리 카운트 확인 | `근거 없음` — 설계는 유효성 검증을 실행 차단 조건으로 규정하지 않음 (`PAT-INP-030` postcondition `진단 표시(실행 차단 안 함)`) | 참고 관측: 버튼 활성·안내 없음·실행되어 `0건`(E0), 히스토리 +1 → BUG-011 (검증 일관성) |
| TC-PAT-FRM-003 | 괄호 불일치 |  | 특허 검색 화면, 초기화 직후. ※ 건수 케이스(008~018)는 `기간`=`전체` 명시 설정 (§9 공통 사전조건) | ① `(하이브리드 and 자동차` 입력 ② 버튼 상태·안내 문구 확인 | `근거 없음` (설계는 진단 표시만 규정, 차단 없음) | 참고 관측: `검색` `disabled` + **V1** 표시 → 설계와 차단 정책 상충: BUG-021 (설계 확정 필요) |
| TC-PAT-FRM-004 | 연산자로 끝나는 불완전 식 |  | 특허 검색 화면, 초기화 직후. ※ 건수 케이스(008~018)는 `기간`=`전체` 명시 설정 (§9 공통 사전조건) | ① `하이브리드 and` 입력 ② 버튼 상태·안내 문구 확인 | V1이 `연산자` 짝을 안내하므로 차단(또는 진단 표시) 예상 | 참고 관측: 버튼 **활성**·안내 없음 → BUG-011 |
| TC-PAT-FRM-005 | 따옴표 불일치 |  | 특허 검색 화면, 초기화 직후. ※ 건수 케이스(008~018)는 `기간`=`전체` 명시 설정 (§9 공통 사전조건) | ① `"하이브리드 자동차` 입력 ② 버튼 상태·표시되는 문구가 V1/V2 중 무엇인지 확인 | V1(짝 오류) 안내 예상 (V1 문구가 `따옴표 짝` 담당) | 참고 관측: `disabled` + **V2**(특수문자) 표시 → 문구-원인 불일치: BUG-010 |
| TC-PAT-FRM-006 | 특수문자만 입력 |  | 특허 검색 화면, 초기화 직후. ※ 건수 케이스(008~018)는 `기간`=`전체` 명시 설정 (§9 공통 사전조건) | ① `@#$%` 입력 ② 버튼 상태 확인 ③ 활성이면 `검색` 클릭 | V2 문구가 `특수문자`를 안내하므로 차단 예상 | 참고 관측: 버튼 **활성**·안내 없음·실행 `0건` → BUG-011 |
| TC-PAT-FRM-007 | URL 인코딩·새로고침 복원 |  | 특허 검색 화면, 초기화 직후. ※ 건수 케이스(008~018)는 `기간`=`전체` 명시 설정 (§9 공통 사전조건) | ① `@#$%` 검색 ② `location.href` 확인 ③ 새로고침 ④ URL·검색식 입력창·결과 확인 | 검색식·결과 유지 — 근거: 실측 Pass. 참고: 직후 `?query=@#$%` → 새로고침 후 `?query=@%23$%25` 정규화 (기능 손실 없음 — 결함 아님 확정 §16) |  |
| TC-PAT-FRM-008 | AND 연산 |  | 특허 검색 화면, 초기화 직후. ※ 건수 케이스(008~018)는 `기간`=`전체` 명시 설정 (§9 공통 사전조건) | ① `하이브리드 and 자동차` 입력 → `검색` ② 건수 기록 | 불리언 AND 적용 (E1 안내 `불리언 연산자(AND/OR/NOT) 지원`). 검산은 TC-PAT-FRM-012의 I2로 수행 | 참고 관측값: `1,684건` |
| TC-PAT-FRM-009 | 연산자 대소문자 무관 |  | 특허 검색 화면, 초기화 직후. ※ 건수 케이스(008~018)는 `기간`=`전체` 명시 설정 (§9 공통 사전조건) | ① `하이브리드 AND 자동차` 입력 → `검색` | 소문자(TC-PAT-FRM-008)와 동일 건수 — 근거: 실측 Pass |  |
| TC-PAT-FRM-010 | OR 연산 |  | 특허 검색 화면, 초기화 직후. ※ 건수 케이스(008~018)는 `기간`=`전체` 명시 설정 (§9 공통 사전조건) | ① `하이브리드 or 자동차` 입력 → `검색` | 불변식 I3 — 각 단독어 건수보다 크거나 같음 | 참고 관측값: `173,413건` (`하이브리드` 16,607 · `자동차` 158,490) |
| TC-PAT-FRM-011 | 단독어 기준값 |  | 특허 검색 화면, 초기화 직후. ※ 건수 케이스(008~018)는 `기간`=`전체` 명시 설정 (§9 공통 사전조건) | ① `하이브리드` 입력 → `검색` ② 건수 기록 (I2 검산용 기준값) |  | 참고 관측값: `16,607건` |
| TC-PAT-FRM-012 | NOT 연산 — 산술 정합 검증 |  | 특허 검색 화면, 초기화 직후. ※ 건수 케이스(008~018)는 `기간`=`전체` 명시 설정 (§9 공통 사전조건) | ① `하이브리드 not 자동차` 입력 → `검색` ② 불변식 I2 검산: `하이브리드` − `하이브리드 and 자동차` = 본 건수 | I2 정확히 일치 — 근거: 실측 검산 (16,607−1,684=14,923) |  |
| TC-PAT-FRM-013 | 와일드카드 |  | 특허 검색 화면, 초기화 직후. ※ 건수 케이스(008~018)는 `기간`=`전체` 명시 설정 (§9 공통 사전조건) | ① `*수소 자동차*` (플레이스홀더 예시 문자열) 입력 → `검색` | `근거 없음` (설계에 와일드카드 명세 없음 — 오류 없이 실행되는지 기록) | 참고 관측값: `155,748건` |
| TC-PAT-FRM-014 | 자유어 기준값 |  | 특허 검색 화면, 초기화 직후. ※ 건수 케이스(008~018)는 `기간`=`전체` 명시 설정 (§9 공통 사전조건) | ① `자동차` 입력 → `검색` ② 건수·URL 기록 | URL `?query=자동차` (기본 필터는 파라미터 미노출 — T4) | 참고 관측값: `158,490건` |
| TC-PAT-FRM-015 | 범위코드 필드절 `KEY` |  | 특허 검색 화면, 초기화 직후. ※ 건수 케이스(008~018)는 `기간`=`전체` 명시 설정 (§9 공통 사전조건) | ① `KEY:(자동차)` 입력 → `검색` ② TC-PAT-FRM-014와 건수 대조 | 기본 범위(`명칭+요약+독립항`) 자유어 검색과 동일 건수 — 근거: 설계 `PAT-INP-020`·불변식 I4 | 참고 관측값: `158,490건` |
| TC-PAT-FRM-016 | 범위코드 필드절 `TAC` |  | 특허 검색 화면, 초기화 직후. ※ 건수 케이스(008~018)는 `기간`=`전체` 명시 설정 (§9 공통 사전조건) | ① `TAC:(자동차)` 입력 → `검색` | `KEY`보다 넓은 범위 (I4: KEY ≤ TAC) — 근거: 설계 `PAT-INP-020` | 참고 관측값: `168,578건` |
| TC-PAT-FRM-017 | 필드절 `DSC` — 명명 충돌 확인 |  | 특허 검색 화면, 초기화 직후. ※ 건수 케이스(008~018)는 `기간`=`전체` 명시 설정 (§9 공통 사전조건) | ① `DSC:(자동차)` 입력 → `검색` ② 라디오 `상세설명` 검색(TC-PAT-INP-008)과 건수 대조 | 설계 `PAT-INP-020`은 `DSC`=명칭+요약+전체청구항+상세설명(누적)으로 표기. 실서비스 필드 카탈로그는 `DSC`=`상세설명` 단독 | 참고 관측값: 필드절 `383,150건` ≠ 라디오 `401,480건` → 명명 충돌: BUG-017/BUG-022 (문서 정정 대상) |
| TC-PAT-FRM-018 | 필드절이 범위 라디오를 덮어쓰는지 |  | 특허 검색 화면, 초기화 직후. ※ 건수 케이스(008~018)는 `기간`=`전체` 명시 설정 (§9 공통 사전조건) | ① `검색어 범위`를 `명칭+요약+독립항`으로 두고 `DSC:(자동차)` 검색 ② `상세설명`으로 바꿔 동일 검색 ③ 두 건수 비교 | 두 경우 동일 건수 — 근거: `PAT-INP-020` postcondition `사용자 직접 필드식은 불변` | 참고 관측값: 양쪽 `383,150건` |
| TC-PAT-FRM-019 | 공백만 입력 |  | 특허 검색 화면, 초기화 직후. ※ 건수 케이스(008~018)는 `기간`=`전체` 명시 설정 (§9 공통 사전조건) | ① 공백(스페이스) 3회만 입력 ② `검색` 버튼 상태 확인 | 공백은 값으로 취급되지 않아 `검색` `disabled` — 근거: `PAT-INP-041` precondition (`값 1개 이상`) | 판정 규칙: `disabled` → Pass / 활성 → Fail (§15 BUG-011 계열로 기록) |
| TC-PAT-FRM-020 | 연산자 단독 `or` / `not` |  | 특허 검색 화면, 초기화 직후. ※ 건수 케이스(008~018)는 `기간`=`전체` 명시 설정 (§9 공통 사전조건) | ① `or` 입력 → 관측 3요소 기록 (❶`검색` 버튼 상태 ❷표시 문구 — §7 V1/V2/없음 대조 ❸활성이면 실행해 결과) ② `not`으로 반복 ③ TC-PAT-FRM-002(`and`)의 기록과 대조 | 근거 없음 — 설계에 해당 입력의 명세 없음 (관측 기록) | 판정 규칙: `and`와 동일 거동 → `근거 없음(기록)` / 다른 거동 → `확인 필요` (검증 비일관 — §15 BUG-011에 연계 기록) |
| TC-PAT-FRM-021 | 연산자 연속 |  | 특허 검색 화면, 초기화 직후. ※ 건수 케이스(008~018)는 `기간`=`전체` 명시 설정 (§9 공통 사전조건) | ① `하이브리드 and and 자동차` 입력 ② 관측 3요소 기록 (버튼 상태 / 문구 V1·V2·없음 / 활성이면 실행 결과) | 근거 없음 — 설계에 해당 입력의 명세 없음 (관측 기록) | 판정 규칙: `disabled`+V1 → `근거 없음(기록)` / 활성·실행됨 → `확인 필요` (§15 BUG-011에 연계 기록) / 그 외 조합 → `확인 필요` |
| TC-PAT-FRM-022 | 괄호 정상 조합 |  | 특허 검색 화면, 초기화 직후. ※ 건수 케이스(008~018)는 `기간`=`전체` 명시 설정 (§9 공통 사전조건) | ① `(하이브리드 or 수소) and 자동차` 입력 → `검색` | 검증 통과·정상 실행. I3/I4 준용 — `하이브리드 and 자동차` 건수 이상 (or 확장) |  |
| TC-PAT-FRM-023 | 닫는 괄호만 |  | 특허 검색 화면, 초기화 직후. ※ 건수 케이스(008~018)는 `기간`=`전체` 명시 설정 (§9 공통 사전조건) | ① `하이브리드 and 자동차)` 입력 ② 관측 3요소 기록 (버튼 상태 / 문구 / 활성이면 실행 결과) ③ 여는 괄호(TC-PAT-FRM-003) 기록과 대칭 여부 대조 | 근거 없음 — 설계에 해당 입력의 명세 없음 (관측 기록) | 판정 규칙: FRM-003과 동일 거동(`disabled`+V1) → `근거 없음(기록)` / 다른 거동 → `확인 필요` (괄호 검사 비대칭 — §15 BUG-011에 연계 기록) |
| TC-PAT-FRM-024 | 필드절 + 연산자 조합 |  | 특허 검색 화면, 초기화 직후. ※ 건수 케이스(008~018)는 `기간`=`전체` 명시 설정 (§9 공통 사전조건) | ① `KEY:(자동차) and TAC:(수소)` 입력 → `검색` | 필드절 조합이 오류 없이 실행 — 근거: 기능정의서 검색식 표기(`CODE:(값)` AND 결합) |  |
| TC-PAT-FRM-025 | 일자 범위 표기 |  | 특허 검색 화면, 초기화 직후. ※ 건수 케이스(008~018)는 `기간`=`전체` 명시 설정 (§9 공통 사전조건) | ① `AD:([2020-01-01 ~ 2020-12-31])` 입력 → `검색` | 일자 필드절 실행 — 근거: 기능정의서 `일자 CODE:([from ~ to])` |  |
| TC-PAT-FRM-026 | 개별 필드코드 인식 |  | 특허 검색 화면, 초기화 직후. ※ 건수 케이스(008~018)는 `기간`=`전체` 명시 설정 (§9 공통 사전조건) | ① 아래 14종을 순서대로 입력 → `검색` → 건수 기록 (값은 전부 실측된 필드 플레이스홀더 예시값): `TI:(도서관)` ② `AB:("전기 자동차")` ③ `CL:(하이브리드 and 자동차)` ④ `CLI:(하이브리드 and 자동차)` ⑤ `CLA:(하이브리드 and 자동차)` ⑥ `AN:(1020080012345)` ⑦ `PN:(1020100012345)` ⑧ `RN:(1012345670000)` ⑨ `AP:(엘지*)` ⑩ `APD:(서울)` ⑪ `INV:(김한국)` ⑫ `AG:(특허법인*)` ⑬ `IPCM:(G06F*)` ⑭ `CPCM:(C01C*)` | 각 코드가 필드로 해석되어 실행 (자유어와 다른 건수 또는 형식 값 매칭) — 근거: 부록 필드코드 카탈로그(기능정의서) | 참고 관측값: `TI:(도서관)` 93건 / `AB:(전기 자동차)` 374,896건 |
| TC-PAT-FRM-027 | 매우 긴 검색식 |  | 특허 검색 화면, 초기화 직후. ※ 건수 케이스(008~018)는 `기간`=`전체` 명시 설정 (§9 공통 사전조건) | ① `자동차 or 수소` 뒤에 ` or 자동차 or 수소`를 49회 이어붙여 총 100항(약 650자) 검색식을 작성해 입력 ② 버튼 상태·실행·오류(V3 여부) 기록 | `근거 없음` (길이 상한 명세 없음 — 동작 기록만. 실행 실패 시 V3 문구 여부 기록) |  |
| TC-PAT-FRM-028 | 존재하지 않는 필드코드 |  | 특허 검색 화면, 초기화 직후. ※ 건수 케이스(008~018)는 `기간`=`전체` 명시 설정 (§9 공통 사전조건) | ① `XXX:(자동차)` 입력 ② 관측 3요소 기록 (버튼 상태 / 문구 / 활성이면 실행 결과) | 근거 없음 — 설계에 미존재 코드의 처리 명세 없음 (관측 기록) | 판정 규칙: 검증 통과·실행 0건 → `확인 필요` (§15 BUG-006과 동일 계열로 연계 기록 — 미존재 코드 `TAC_DSC:()` 실측과 동일 양상) / 차단·안내 표시 → `근거 없음(기록)` |
| TC-PAT-FRM-029 | 한글 연산자 인식 여부 |  | 특허 검색 화면, 초기화 직후. ※ 건수 케이스(008~018)는 `기간`=`전체` 명시 설정 (§9 공통 사전조건) | ① `하이브리드 그리고 자동차` / `하이브리드 또는 자동차` 각각 실행 ② 영문 연산자 결과와 대조 | `근거 없음` (한글 연산자 명세 없음 — 일반 검색어로 취급되는지 기록) |  |

## 특허 입력 필터

| 케이스 ID | 케이스명 | 요소 | 사전조건 | 수행 절차 | 기대 결과 · 판정 기준 | 참고 관측값(2026-08-10) · 회귀 연계 |
|---|---|---|---|---|---|---|
| TC-PAT-INP-001 | 초기 상태·기본값 |  | `초기화` 직후 / (공통: 특허 검색 화면 (§3 P4 초기화 상태)) | ① 각 필터 그룹의 위젯 종류·옵션·기본 선택 확인 (§3.2의 a11y 트리 채집 방법으로 확인) | 근거: 설계 `PAT-INP-011~014`, 실측(2026-08-10): / - `국가` — group, button 9개(`전체` `KR 한국` `US 미국` `JP 일본` `CN 중국` `EP 유럽` `PCT` `DE 독일` `GB 영국`) + `+\n국가설정 (12)`. 기본 **`KR 한국`·`US 미국`**(`pressed`) / - `문헌종류` — group, button 5개(`전체` `공개` `등록` `실용공개` `실용등록`). 기본 `전체` / - `기간` — group, **radio 4개**(`전체` `최근 5년` `최근 10년` `최근 20년`). 기본 **`최근 5년`**(표시 기준) / - `시작일`/`종료일` — `Date` 위젯(spinbutton `연도`/`월`/`일` + `날짜 선택도구 표시`) / - `상태` — group 3분할: `상태 전체`(`전체`) / `상태 A`(`출원` `심사중` `등록`) / `상태 I`(`거절` `소멸` `취하` `포기` `무효`). `A`·`I`는 버튼이 아니라 그룹 구분 StaticText / - `검색어 범위` — group, radio 3개. 기본 `명칭+요약+독립항` | 주의: 기간 기본값 `최근 5년`은 **표시일 뿐 쿼리에 미적용** (BUG-003) — 건수 케이스는 `기간`을 명시 설정할 것 |
| TC-PAT-INP-002 | 검색식 입력창 구문 강조 |  | 특허 검색 화면 (§3 P4 초기화 상태) | ① 검색식에 `DSC:(자동차)` 입력 ② 입력창 내부 렌더 구조 확인 | 구문 강조(편집기) — 근거: `PAT-INP-030` `편집기(구문강조+실시간 진단)`. 참고 관측: `DSC`와 `:(자동차)`가 별도 StaticText로 분리 렌더 | 회귀 연계: 설계의 `입력방식 토글`(일반/편집기)은 실서비스에 없음 → BUG-020 |
| TC-PAT-INP-003 | 필터 적용 시점 |  | 특허 검색 화면 (§3 P4 초기화 상태) | ① `자동차` 검색 실행 상태에서 `국가` `KR 한국` 토글 해제 ② `검색`을 누르지 않고 건수·URL 확인 ③ `검색` 클릭 후 재확인 | 토글 시 `aria-pressed`만 즉시 갱신, 건수·URL 불변. `검색` 클릭 후에만 적용 — 근거: `PAT-INP-011` postcondition `다음 검색 payload에 countries[] 포함` |  |
| TC-PAT-INP-004 | 국가 필터 — `US 미국` 단독 |  | 검색식 `자동차`, `기간`=`전체` / (공통: 특허 검색 화면 (§3 P4 초기화 상태)) | ① `KR 한국` 토글 해제(US만 남김) → `검색` ② URL·건수 확인 | URL `countries=US` — 근거: `PAT-INP-011`. 참고 관측값: `0건` (한글 검색어라 US 문헌 무매칭) |  |
| TC-PAT-INP-005 | 국가 필터 — `KR 한국` 단독 |  | 특허 검색 화면 (§3 P4 초기화 상태) | ① `KR 한국` 선택·`US 미국` 해제 → `검색` ② I9 검산: KR + US = 기본(KR·US) | URL `countries=KR`, I9 정합 — 근거: 실측 검산 | 참고 관측값: `158,490건` |
| TC-PAT-INP-006 | 국가 필터 — `전체` |  | 특허 검색 화면 (§3 P4 초기화 상태) | ① `전체` 클릭 → `검색` ② `전체`만 `pressed`·개별 국가 해제 확인 ③ URL 파라미터 표기 확인 | `"전체"=라디오, 해제 불가` — 근거: `PAT-INP-011` | 회귀 연계: URL이 `countries=`(빈 값)로 표기되면 BUG-015. 관측 한계: 한글 검색어로는 `전체`와 `KR` 단독을 건수로 구별 불가 (영문 검색어 케이스 별도 필요) |
| TC-PAT-INP-007 | 검색어 범위 — `명칭+요약+전체청구항` |  | 검색식 `자동차`, `기간`=`전체` / (공통: 특허 검색 화면 (§3 P4 초기화 상태)) | ① `검색어 범위`에서 `명칭+요약+전체청구항` 선택 → `검색` ② URL·건수 확인 ③ I4 검산: `TAC:(자동차)` 건수와 일치 여부 | URL `scope=TITLE_ABSTRACT_ALL_CLAIMS`, `TAC:()` 필드절과 건수 일치 — 근거: `PAT-INP-020`, 불변식 I4 | 참고 관측값: `168,578건` |
| TC-PAT-INP-008 | 검색어 범위 — `상세설명` |  | 특허 검색 화면 (§3 P4 초기화 상태) | ① `상세설명` 선택 → `검색` ② URL·건수 확인 ③ 필드절 `DSC:(자동차)` 건수와 대조 | 설계 표기는 `DSC(명칭+요약+전체청구항+상세설명)`(`PAT-INP-020`, 단 `라벨 정합은 명세 비교 확인 대상`으로 명시). 실서비스 내부 코드는 `TAC_DSC` | 참고 관측값: `scope=DESCRIPTION`, `401,480건` ≠ 필드절 `383,150건` → BUG-005 계열(BUG-017/022 명명 충돌) |
| TC-PAT-INP-009 | 검색어 범위 — 기본값 파라미터 |  | 특허 검색 화면 (§3 P4 초기화 상태) | ① `명칭+요약+독립항`(기본) 상태로 검색 ② URL에 `scope` 파라미터 존재 여부 확인 | **확인 불가 확정** — 기본값은 URL에 직렬화되지 않아 파라미터명을 확인할 방법이 없음 (T4) |  |
| TC-PAT-INP-010 | `국가설정` 대화상자 개방 | `국가설정 (12)` (button) — 표기형 `국가설정 (N)`. `(12)`는 선택 가능한 추가 국가 목록 수(고정) | 특허 검색 화면 (§3 P4 초기화 상태) | ① `+\n국가설정 (12)` **실제 클릭** ② 대화상자 제목·항목·버튼 채집 | `국가설정 팝업에서 추가` — 근거: `PAT-INP-011`. 참고 관측: `role="dialog"` 제목 `추가 국가 선택`, checkbox 12개(`FR 프랑스` `RU 러시아` `IN 인도` `TW 대만` `CA 캐나다` `AU 호주` `IT 이탈리아` `NL 네덜란드` `AT 오스트리아` `CH 스위스` `DK 덴마크` `ES 스페인`), 버튼 `취소` `선택 추가` |  |
| TC-PAT-INP-011 | `국가설정` 대화상자 Escape 닫기 |  | 특허 검색 화면 (§3 P4 초기화 상태) | ① 대화상자 개방 상태에서 Escape 키 | `근거 없음` (기록). 참고 관측: 닫힘 |  |
| TC-PAT-INP-012 | `항목별 상세검색` 펼침 — 필드코드 카탈로그 | `항목별 상세검색\n— 명칭·청구항·분류코드 등 필드별 조합` (button, expandable — 펼침 시 라벨 `항목별 상세검색`으로 축약, T3) | 특허 검색 화면 (§3 P4 초기화 상태) | ① 클릭해 펼침 ② 그룹·필드코드 채집 (대화상자가 아니라 **인라인 확장**) | `필드별 조합(펼침/접힘 · 필드 추가/확장 모달)` — 근거: `PAT-INP-050`. 기준 카탈로그(실측 2026-08-10): / - `명칭·청구항·설명` — `TI` `AB` `CL` `CLI` `CLA` `DSC` / - `번호·일자` — `AN` `PN` `RN` `AD`(범위 `~`) `PD`(범위 `~`) `RD`(범위 `~`) / - `인명` — `AP` `APD` `INV` `AG` / - `분류코드` — `IPCM`·`CPCM` + 옵션 `Original or Current`/`Current Only` / - 확장 — `+\n검색 필드 추가·확장` |  |
| TC-PAT-INP-013 | `초기화` |  | 검색식 `DSC:(자동차)`, `검색어 범위`=`상세설명`, 검색 실행 상태 / (공통: 특허 검색 화면 (§3 P4 초기화 상태)) | ① `초기화` 클릭 ② URL·검색식·필터·결과 영역·언어 파라미터 확인 | `입력값·활성필드를 디폴트로 복원` — 근거: `PAT-INP-042`. 확인 지점: URL `?lang=ko`만 잔존(query·scope 제거), 검색식 비움·`검색` `disabled`, 필터 전부 기본값, 빈 상태 E1, 언어는 유지 |  |
| TC-PAT-INP-014 | 국가 필터 개별 6개 |  | 검색식 `자동차`, `문헌종류`=`전체`, `기간`=`전체`, `상태`=`전체` / (공통: 특허 검색 화면 (§3 P4 초기화 상태)) | ① `국가` `전체` → 대상 국가만 선택 → `검색` (JP/CN/EP/PCT/DE/GB 6회 반복) ② 각 URL 값·건수 기록 ③ DE·GB는 상위 행을 KR 단독 결과와 대조 | 선택 국가의 문헌만 반환, `PCT`→URL 값 `WO` 매핑 — 근거: `PAT-INP-011` | 참고 관측값: JP/CN/EP/PCT `0건` 정상. **DE·GB는 `158,490건`으로 KR 결과가 그대로 반환** → BUG-001 (치명) |
| TC-PAT-INP-015 | `문헌종류` 5종 |  | 검색식 `자동차`, `국가`=`전체`, `기간`=`전체` / (공통: 특허 검색 화면 (§3 P4 초기화 상태)) | ① 각 항목 단독 선택 → `검색` (5회) ② I1 검산: 4종 합 = `전체` | `docTypes[] 포함`(URL은 한글 값 그대로, 예 `docTypes=공개`), I1 정확히 일치 — 근거: `PAT-INP-012`, 불변식 I1 | 참고 관측값: 전체 158,490 / 공개 73,464 / 등록 17,481 / 실용공개 67,184 / 실용등록 361 |
| TC-PAT-INP-016 | `기간` 프리셋 4종 |  | 검색식 `자동차`, `국가`=`전체`, `문헌종류`=`전체` / (공통: 특허 검색 화면 (§3 P4 초기화 상태)) | ① 각 라디오 **명시 클릭** → `검색` (4회) ② URL 파라미터·건수 기록 ③ I7 검산(단조성) ④ 초기화 직후 기본 `최근 5년` 상태로도 검색해 명시 클릭과 결과 비교 | 선택값이 `dateFrom`/`dateTo`로 반영 — 근거: `PAT-INP-013` | 참고 관측값: `전체`=`datePreset=ALL` 158,490 / `최근 5년`=`dateFrom=2021-08-10&dateTo=2026-08-10`(datePreset 없음) **0건** / `최근 10년`=`…&datePreset=Y10` 9,230 / `최근 20년`=`…&datePreset=Y20` 40,087 / 회귀 연계: 기본값 미적용=BUG-003 · `datePreset` 누락=BUG-014 · `최근 5년` 0건=BUG-025(데이터 적재 범위 확인) |
| TC-PAT-INP-017 | `시작일`/`종료일` 직접 입력 |  | 특허 검색 화면 (§3 P4 초기화 상태) | ① `2010-01-01` ~ `2010-12-31` 입력 → `검색` ② URL·건수·프리셋 라디오 상태 확인 | `dateFrom/dateTo 반영` — 근거: `PAT-INP-013`. 참고 관측값: `2,628건`, `dateFrom=2010-01-01&dateTo=2010-12-31`. 관측 기록: 프리셋 라디오가 해제되지 않고 `datePreset=ALL` 동시 직렬화 |  |
| TC-PAT-INP-018 | 일자 역전 입력 |  | TC-PAT-INP-017 직후 (`2,628건` 상태) / (공통: 특허 검색 화면 (§3 P4 초기화 상태)) | ① `시작일` `2020-12-31`, `종료일` `2020-01-01` 입력 ② 안내 문구·버튼 상태 확인 ③ `검색` 클릭 후 URL·건수·입력창 값 확인 | 역전 범위에 대한 안내 또는 차단 | 참고 관측: 안내 없음·버튼 활성·제출해도 URL/건수는 이전 조건 유지·입력창에는 역전 값 잔존 → BUG-005 |
| TC-PAT-INP-019 | `상태` 9종 |  | 검색식 `자동차`, `국가`=`전체`, `기간`=`전체` / (공통: 특허 검색 화면 (§3 P4 초기화 상태)) | ① 각 항목 단독 선택 → `검색` (9회) ② `출원`과 `심사중`의 건수·상위 5행 대조 | `statuses[] 포함`, 서로 다른 상태는 다른 결과 집합 — 근거: `PAT-INP-014` | 참고 관측값: 전체 158,490 / 출원 4,205 / 심사중 **4,205(출원과 완전 동일)** / 등록 17,843 / 거절 43,465 / 소멸 48,548 / 취하 37,437 / 포기 6,856 / 무효 24 → `출원`≡`심사중`이면 BUG-004 |
| TC-PAT-INP-020 | `항목별 상세검색` 필드 조립 |  | 특허 검색 화면 (§3 P4 초기화 상태) | ① `TI`에 `도서관`, `AB`에 `"전기 자동차"` 입력 ② 입력 즉시 검색식 반영 여부 확인 ③ `검색` 실행 후 조립식·입력값·필드 구성 확인 | `검색식에 CODE:(값) AND 누적` · `실행 후 입력값만 비우고 구성 유지` — 근거: `PAT-INP-050`. 확인 지점: 실행 시 `TI:(도서관) AND AB:("전기 자동차")` 조립·입력창 반영·필드값 비움·구성 유지 | 참고: 필드마다 `<코드> <라벨> 검색식에 추가`(값 없으면 disabled) / `<코드> <라벨> 제거` 버튼 개별 존재 |
| TC-PAT-INP-021 | 따옴표(정확구) 구문 |  | 특허 검색 화면 (§3 P4 초기화 상태) | ① `AB:(전기 자동차)` 검색 ② `AB:("전기 자동차")` 검색 ③ 건수 비교 | 정확구가 크게 좁혀짐(따옴표 짝이 맞으면 차단되지 않음) — 근거: 실측 | 참고 관측값: `374,896건` / `733건` |
| TC-PAT-INP-022 | `분류코드 검색 대상` 옵션 | `분류코드 검색 대상` (combobox, 옵션 `Original or Current`=`ORIGINAL_OR_CURRENT` / `Current Only`=`CURRENT`) | 특허 검색 화면 (§3 P4 초기화 상태) | ① `IPCM`에 `G06F*` 입력, `Original or Current`로 `검색` ② `Current Only`로 변경 후 `검색` ③ 조립 검색식·결과 확인 | 두 옵션 모두 분류코드 검색 실행 | 참고 관측: 기본 옵션 `IPCM:(G06F*)` 1,253,662건 정상 / `Current Only`는 `IPCM:(G06F*) AND IPCCM:(G06F*)` 조립(카탈로그에 없는 `IPCCM`) → **V3 검색 실패** → BUG-002 (치명) |
| TC-PAT-INP-023 | `+ 검색 필드 추가·확장` 대화상자 |  | 특허 검색 화면 (§3 P4 초기화 상태) | ① `항목별 상세검색` 펼침 → `+\n검색 필드 추가·확장` 클릭 ② 그룹 8개·필드 82개·하단 선택 수·버튼 채집 ③ 설계 제외 필드(`BT` `SM` `CC`)·비특허 인용 필드(`NPCT`) 존재 여부 확인 | 근거: 기능정의서 「정합 주의 — 검색필드에서 제외 확정(2026-07-08)」 및 부록 카탈로그: / - `BT` 배경기술·`SM` 해결수단 **없어야 함** / `CC` 국가코드 **없어야 함** / 비특허 인용은 `NPCT` | 참고 관측: 8그룹 82필드(`명칭·청구항·설명` 15 / `번호·일자` 16 / `인명` 11 / `분류코드` 8 / `권리·실시권` 11 / `인용` 6 / `국가R&D` 5 / `표준·서열` 10), 하단 `18개 선택`, 버튼 `닫기` `적용하기`. **`BT`·`SM` 존재** → BUG-012 / `CC` 없음(정상) / `NPCT` 없음, `NPCY`·`NPRD` 존재 → BUG-023(문서) |
| TC-PAT-INP-024 | `국가설정` `선택 추가` / `취소` |  | 특허 검색 화면 (§3 P4 초기화 상태) | ① `국가설정 (12)` → `FR 프랑스` 체크 → `선택 추가` ② 국가 그룹에 추가·선택 상태 확인 ③ 재개방 → `RU 러시아` 체크 → `취소` ④ 미반영 확인 ⑤ 재개방 시 목록 수·체크 상태 확인 | `선택 추가` 시 국가 그룹에 `pressed` 상태로 추가, `취소` 시 미반영, 목록은 12개 유지·추가 항목 `checked` — 근거: `PAT-INP-011`·실측 Pass |  |
| TC-PAT-INP-025 | `검색 히스토리` 구성·정규화 저장 | `검색 히스토리\n(N)` (button, expandable) | 특허 검색 화면 (§3 P4 초기화 상태) | ① 펼침 ② 표 헤더·하단 안내·항목별 버튼 수 채집 ③ 저장 형태 확인: `자동차`(기본 범위)→`KEY:(자동차)`, 범위 `명칭+요약+전체청구항`→`TAC:(자동차)`, 범위 `상세설명`→`TAC_DSC:(자동차)`, 직접 필드절은 그대로 | 검색 실행마다 1건 누적(`PAT-INP-041`), ★즐겨찾기(`PAT-INP-062`)·삭제(`PAT-INP-063`) 제공 — 근거: 설계 | 참고 관측: 헤더 `검색식`/`재검색`, 안내 E4, 항목당 버튼 2개뿐(검색식+`재검색`) → 즐겨찾기 미구현 BUG-008 · 삭제 미구현 BUG-009 · 중복 누적 BUG-016. 히스토리는 계정 단위 누적 |
| TC-PAT-INP-026 | `재검색` |  | 특허 검색 화면 (§3 P4 초기화 상태) | ① `KEY:(…)` 항목의 `재검색` 클릭 → 저장 당시 조건(필터 포함) 복원·건수 재현 확인 ② `TAC_DSC:(…)` 항목의 `재검색` 클릭 → 건수 확인 | `선택 검색식으로 재실행` — 근거: `PAT-INP-061` | 참고 관측: `KEY` 항목은 저장 당시 조건(`dateFrom=2010-01-01` 등)까지 복원해 `2,628건` 정확 재현 / `TAC_DSC` 항목은 **0건** (파서가 `TAC_DSC` 미인식) → BUG-006 |
| TC-PAT-INP-027 | Enter 키 제출 |  | 특허 검색 화면 (§3 P4 초기화 상태) | ① 검색식 `도서관` 입력 ② 입력창 포커스 상태에서 **실제 Enter 키** 입력 ③ URL·검색 실행·입력창 내용 확인 | 검색 실행 — 근거: `PAT-INP-041` interaction `클릭 / ⏎` | 참고 관측: 미실행, 개행만 추가 (스크립트·실제 키 양쪽 동일) → BUG-007 |
| TC-PAT-INP-028 | `날짜 선택도구 표시` (기간 2개 + 항목별 상세검색 내 6개) | `날짜 선택도구 표시` (button, `haspopup="menu"`) | 특허 검색 화면 (§3 P4 초기화 상태) | ① `시작일`의 버튼 **실제 클릭** → 열리는 팝업의 형태·항목을 기록 ② 팝업에서 `2010-01-01` 선택 → 입력창 반영 여부 기록 ③ `종료일` 및 `항목별 상세검색`의 `AD`/`PD`/`RD` 범위 위젯에서 반복 | `근거 없음` (위젯 상세 명세 없음 — 개방·선택·반영 여부 기록). 2026-08-10 순회에서는 위젯 자체만 확인 |  |
| TC-PAT-INP-029 | 필드별 `검색식에 추가` / `제거` 버튼 | `<코드> <라벨> 검색식에 추가` / `<코드> <라벨> 제거` (필드마다 개별 존재) | 특허 검색 화면 (§3 P4 초기화 상태) | ① `TI`에 `도서관` 입력 → `TI 발명의 명칭 검색식에 추가` 클릭 → 검색식 입력창·`검색` 버튼의 변화를 기록 ② `TI 발명의 명칭 제거` 클릭 → 변화를 기록 ③ 값 없는 필드의 `검색식에 추가` disabled 확인 | `검색식에 CODE:(값) AND 누적` — 근거: `PAT-INP-050` (버튼 단위 동작은 2026-08-10 미실측 → 이번 순회 대상) | 참고: 실측(TC-PAT-INP-020) 기준 필드 입력값은 즉시 반영되지 않고 `검색` 실행 시 조립됨 — 버튼 단독 동작은 2026-08-10 미실측이므로 관측을 기록한다 |
| TC-PAT-INP-030 | `검색 필드 추가` 대화상자 `적용하기` |  | 특허 검색 화면 (§3 P4 초기화 상태) | ① `+ 검색 필드 추가·확장` → `FN` 공고번호 체크 → `적용하기` ② `항목별 상세검색`에 필드 추가 확인 ③ 하단 `N개 선택` 카운트 변화 확인 ④ `닫기`(미적용) 동작 대조 | `필드 추가/확장 모달` — 근거: `PAT-INP-050` (적용 실행은 2026-08-10 미실측 → 이번 순회 대상) |  |
| TC-PAT-INP-031 | `항목별 상세검색` 접기 |  | 특허 검색 화면 (§3 P4 초기화 상태) | ① 펼침 상태에서 토글 버튼 재클릭 ② 접힘·라벨 복원(T3) 확인 | `펼침/접힘` — 근거: `PAT-INP-050` |  |
| TC-PAT-INP-032 | `검색 히스토리` 100건 상한 |  | `검색 히스토리\n(N)`의 현재 카운트 N 확인 (안내 E4 `검색식은 최대 100개까지 누적됩니다`) / (공통: 특허 검색 화면 (§3 P4 초기화 상태)) | ① 검색식 `자동차`로 `검색`을 (100−N)회 반복 실행해 카운트 100 도달 (동일 검색식도 중복 누적됨 — BUG-016 실측) ② 도달 후 1회 더 검색 ③ 카운트 표시·최고(最古) 항목 축출/신규 추가 거부 등 동작 기록 | `최대 100건` — 근거: `COM-010`. 상한 도달 시 동작은 명세 없음(`근거 없음`으로 기록). 삭제 수단 부재(BUG-009)로 정리 불가 상태 여부 함께 기록 |  |

## 특허 결과 목록

| 케이스 ID | 케이스명 | 요소 | 사전조건 | 수행 절차 | 기대 결과 · 판정 기준 | 참고 관측값(2026-08-10) · 회귀 연계 |
|---|---|---|---|---|---|---|
| TC-PAT-LST-001 | 결과 표 구성 |  | `하이브리드 and 자동차` 검색 실행 상태(참고 1,684건). 010 패싯 케이스만 `도서관`(584건) 기준 (§11 공통 사전조건) | ① 표 컬럼·상단 요약바·좌측 필터·정렬/개수 컨트롤 채집 | 근거: `PAT-LST-010/020`, 실측(2026-08-10): / - 컬럼: `(체크박스)` `상태` `문헌번호` `원문` `발명의 명칭` `출원인` `출원일` `IPC` / - 상단: `검색식` + 입력값 + 건수 + `논문으로` / - 좌측 필터: `국가` `권리상태` `출원연도` `IPC` `모든 필터` (모두 `haspopup="dialog"`) / - 정렬/개수: `정렬`(렌더 `매칭순`) · `페이지당 개수`(렌더 `20개`) — 접근성 이름과 렌더 텍스트가 다름(N7) / - 문헌번호가 `a` 링크(행 클릭이 아니라 문헌번호·명칭이 상세 링크) | 회귀 연계: `IPC` 컬럼 전 행 `-` → BUG-024 / 문헌번호 표기 형식 차이 → BUG-018 |
| TC-PAT-LST-002 | 결과 없음 상태 |  | `하이브리드 and 자동차` 검색 실행 상태(참고 1,684건). 010 패싯 케이스만 `도서관`(584건) 기준 (§11 공통 사전조건) | ① 0건이 되는 검색 실행 (예: `and`) ② 안내 문구 대조 | `0건` + E0 문구 — 근거: `PAT-LST-070`(0건=안내) |  |
| TC-PAT-LST-003 | `정렬` | `정렬` (combobox, 렌더 텍스트가 현재값) | `하이브리드 and 자동차` 검색 실행 상태(참고 1,684건). 010 패싯 케이스만 `도서관`(584건) 기준 (§11 공통 사전조건) | ① **실제 클릭**으로 개방 → 옵션 채집 ② 각 옵션 적용 → URL·상위 행 출원일·건수 확인 | 정렬 변경 시 재요청, 건수 유지 — 근거: `PAT-LST-040`. 참고 관측: 옵션 3개 `매칭순`(기본)/`출원일(최신)`=`sort=APPLICATION_DATE_DESC`/`출원일(오래된)`=`…_ASC`, 방향 정상 | 참고: 최신순 1위가 `2019-04-08` → 적재 데이터 출원일 상한 관측 (BUG-025와 정합) |
| TC-PAT-LST-004 | `페이지당 개수` |  | `하이브리드 and 자동차` 검색 실행 상태(참고 1,684건). 010 패싯 케이스만 `도서관`(584건) 기준 (§11 공통 사전조건) | ① 개방 → 옵션 채집 ② `50개`·`200개` 적용 → URL·실제 행수·페이지 버튼 수 확인 (I8 검산) | 옵션 4개(`20개` `50개` `100개` `200개`), 선택값=실제 행수, I8 정합 — 근거: `PAT-LST-040`·실측 (`200개` 적용 시 584건→3페이지) |  |
| TC-PAT-LST-005 | 좌측 필터 `권리상태` | `권리상태` (button, `haspopup="dialog"`) | `하이브리드 and 자동차` 검색 실행 상태(참고 1,684건). 010 패싯 케이스만 `도서관`(584건) 기준 (§11 공통 사전조건) | ① 클릭 개방 (첫 클릭에 안 열리면 재클릭 — 관측된 토글 타이밍 특성) ② 항목·건수 채집 ③ I5 검산: 항목 합 = 총건수 ④ `등록` 체크 → `적용` → URL·건수·상태 컬럼·버튼 선택 수 확인 | 체크 후 적용 시 재검색 — 근거: `PAT-LST-030`. I5 정확히 일치 | 참고 관측값(1,684건 기준): `등록 (725건)` `소멸 (344건)` `거절 (308건)` `공개 (180건)` `취하 (113건)` `포기 (14건)`. 적용 시 `fk_statuses=등록`·`725건`·전 행 `등록`·버튼에 `1` |
| TC-PAT-LST-006 | 선택(체크박스) |  | `하이브리드 and 자동차` 검색 실행 상태(참고 1,684건). 010 패싯 케이스만 `도서관`(584건) 기준 (§11 공통 사전조건) | ① `전체 선택` 클릭 → 전 행 체크 ② 재클릭 → 전 해제 ③ 행 1건만 선택 → `전체 선택` `mixed`(삼상) 확인 ④ 선택 후 나타나는 액션 버튼 존재 여부 확인 | 선택 상태 반영 + 선택 대상 액션(라이브러리 저장 등) — 근거: `PAT-LST-022/060` | 참고 관측: 선택 동작 정상·`mixed` 지원. **선택 후 액션 버튼 없음** → BUG-027 |
| TC-PAT-LST-007 | 페이지네이션 | `navigation "pagination"` — 숫자 + `이전 페이지 그룹`/`다음 페이지 그룹` | `하이브리드 and 자동차` 검색 실행 상태(참고 1,684건). 010 패싯 케이스만 `도서관`(584건) 기준 (§11 공통 사전조건) | ① `2` 클릭 → `pageNo=2`·행 교체 확인 ② `다음 페이지 그룹` → 버튼 11~20·`pageNo=11` ③ `이전 페이지 그룹` → 버튼 1~10·`pageNo=10` ④ 1페이지에서 `이전 페이지 그룹` `disabled` 확인 | 페이지 이동 시 재요청 — 근거: `PAT-LST-040`·실측 Pass |  |
| TC-PAT-LST-008 | 행 요소 (링크·원문·하이라이팅) |  | `하이브리드 and 자동차` 검색 실행 상태(참고 1,684건). 010 패싯 케이스만 `도서관`(584건) 기준 (§11 공통 사전조건) | ① 문헌번호·발명의 명칭의 링크 여부·href 확인 ② `원문` 컬럼 요소 상태 확인 ③ 명칭 내 검색어 분리 렌더(하이라이팅) 확인 | 근거: `PAT-LST-020`·실측: 문헌번호=`link`(`/patent-front/patents/<번호>?query=…&pageNo=…`), 명칭=동일 href `link` + `새 탭에서 열기` 별도, `원문`=`button "원문 (준비 중)"` `disabled`(구현 예정 — 결함 아님), 하이라이팅 구현 | 회귀 연계: 목록 `1010395920000` vs 상세 URL `KR%2010-2620258%20B1` 형식 차이 → BUG-018 |
| TC-PAT-LST-009 | `논문으로` (교차검색) |  | `하이브리드 and 자동차` 검색 실행 상태(참고 1,684건). 010 패싯 케이스만 `도서관`(584건) 기준 (§11 공통 사전조건) | ① 특허 결과(패싯 적용 상태 포함)에서 `논문으로` 클릭 ② URL·breadcrumb·검색식·특허 전용 파라미터 제거 확인 | 검색어 이관·화면 전환 — 근거: `PAT-LST-010`·`COM-050`. 확인 지점: `/patent-front/papers?query=…`, breadcrumb `논문 검색`, `fk_statuses` 등 특허 전용 패싯 제거 | 참고 관측값: `하이브리드 and 자동차` → 논문 `39건` |
| TC-PAT-LST-010 | 좌측 패싯 `국가`·`출원연도`·`IPC`·`모든 필터` |  | 검색식 `도서관` (`584건` 기준) / (공통: `하이브리드 and 자동차` 검색 실행 상태(참고 1,684건). 010 패싯 케이스만 `도서관`(584건) 기준 (§11 공통 사전조건)) | ① 각 패싯 개방 → 항목수·상위 항목 채집 ② I6 검산: 개별 패싯 항목수 합 = `모든 필터` 체크박스 수 ③ I5 검산(권리상태 합=총건수) ④ `모든 필터`에서 `권리상태`의 `등록` 체크 → `적용` → URL·건수·버튼 선택 수 확인 ⑤ `취소` 동작 대조 | 체크 후 적용 시 재검색 → 1페이지로 — 근거: `PAT-LST-030`. I5·I6 정합 | 참고 관측값(도서관 584건): `국가` 1항목(`한국 (584건)`) / `권리상태` 6항목(합 584) / `출원연도` 31항목 / `IPC` 20항목(`G06Q-050/26 (82건)` 등) / `모든 필터` 58 체크박스 / 회귀 연계: 패싯에 IPC 값이 있는데 표 `IPC` 컬럼이 `-`면 BUG-024 재현 |
| TC-PAT-LST-011 | 문헌번호 링크 이동·`새 탭에서 열기` |  | `하이브리드 and 자동차` 검색 실행 상태(참고 1,684건). 010 패싯 케이스만 `도서관`(584건) 기준 (§11 공통 사전조건) | ① 문헌번호 링크 클릭 → 같은 탭 상세 이동 확인 ② 목록 복귀 후 `새 탭에서 열기` 클릭 → 새 탭 개방·상세 렌더·`rel="opener"` 확인 | 상세 진입 — 근거: `PAT-LST-021`(행 선택→상세)·실측(`rel="opener"`는 `탭 닫기` 동작 조건, T10) |  |

## 특허 상세

| 케이스 ID | 케이스명 | 요소 | 사전조건 | 수행 절차 | 기대 결과 · 판정 기준 | 참고 관측값(2026-08-10) · 회귀 연계 |
|---|---|---|---|---|---|---|
| TC-PAT-DET-001 | 섹션 구성 |  | 기준 문헌 /patents/KR%2010-2620258%20B1. 로드 완료 대기(T6)·스크롤은 내부 컨테이너(T5) (§12 공통 사전조건) | ① 상단 요소·섹션 탭·하위 구성·버튼 채집 | 근거: `PAT-DET-010~140`, 실측(2026-08-10): / - 상단: `탭 닫기` · 상태(`등록공고`) · `KR` · `10-2620258 B1` · 발명의 명칭 · `원문 PDF 다운로드` / - 섹션 탭 9개: `서지사항` `인명정보` `요약` `상세설명` `청구범위` `패밀리정보` `인용·피인용` `분류코드` `기타정보` / - 하위: `타임라인` / `패밀리 문헌 (N건)` / `인용 (N건)` / `피인용 (N건)` / `선행기술문헌 (N건)` / `IPC`·`CPC` / `권리변동 이력` / `행정처리(수발신) 이력`(`접수·발송일` `서류명` `처리상태`) / `도면 (N)` / - 버튼: `독립항` `전체청구항` `확대` |  |
| TC-PAT-DET-002 | 서지사항 필드 |  | 기준 문헌 /patents/KR%2010-2620258%20B1. 로드 완료 대기(T6)·스크롤은 내부 컨테이너(T5) (§12 공통 사전조건) | ① 서지사항 필드 목록 채집·기준 목록과 대조 | (기준 목록, 실측 2026-08-10): `문헌번호` `문헌종류` `출원번호` `출원일` `공개번호` `공개일` `국제공개번호` `국제공개일` `공고번호` `공고일` `등록번호` `등록일` `권리상태` `원출원번호` `국제출원번호` `우선권주장일` `심사청구일` `존속기간(예상)만료일` `권리변동` `최종처분상태` `청구항 수` `출원구분` `번역문 제출일` `도면 수` `실시권 등록일` `지정국` `서열목록` | 참고: 값 정합성(수집 DB 대조)은 별도 QC 문서(`QC-상세페이지-적재데이터-대조.md`) 범위 |
| TC-PAT-DET-003 | 원문 PDF 다운로드 | `원문 PDF 다운로드` (button) | 기준 문헌 /patents/KR%2010-2620258%20B1. 로드 완료 대기(T6)·스크롤은 내부 컨테이너(T5) (§12 공통 사전조건) |  | 판정 고정: **구현 예정** — `aria-label="원문 준비 중"`·`disabled` 확인만 수행, 실행하지 않음 (목록 `원문 (준비 중)`과 일치 — 결함 아님) |  |
| TC-PAT-DET-004 | 섹션 탭 이동 |  | 로드 완료 대기 (T6 — 직후 클릭은 무시됨: BUG-032) / (공통: 기준 문헌 /patents/KR%2010-2620258%20B1. 로드 완료 대기(T6)·스크롤은 내부 컨테이너(T5) (§12 공통 사전조건)) | ① `기타정보` 클릭 → 내부 컨테이너 `scrollTop`·섹션 위치·활성 탭 확인 (T5) ② 이어서 `서지사항` 클릭 → 위로 이동 여부 확인 | 클릭한 섹션이 화면에 표시 — 근거: `PAT-DET-040` `해당 섹션으로 스크롤 · 활성 탭 표시` | 참고 관측: `기타정보` 클릭 시 9,921에서 멈춤(섹션은 3,785px 아래) → BUG-029 / 위로 이동 전혀 안 됨 → BUG-030 / 활성 탭 어긋남(`상세설명` 표시) → BUG-031 |
| TC-PAT-DET-005 | `독립항` / `전체청구항` 토글 |  | 기준 문헌 /patents/KR%2010-2620258%20B1. 로드 완료 대기(T6)·스크롤은 내부 컨테이너(T5) (§12 공통 사전조건) | ① `전체청구항` 클릭 ② `독립항` 재클릭 ③ `청구범위` 텍스트 분량 변화·원복 확인 | 독립항만 ↔ 전체 청구항 전환 — 근거: `PAT-DET-091`. 참고 관측: 4,366자(독립항, 기본 `aria-pressed=true`) ↔ 7,786자(전체) |  |
| TC-PAT-DET-006 | `확대` (도면) |  | 기준 문헌 /patents/KR%2010-2620258%20B1. 로드 완료 대기(T6)·스크롤은 내부 컨테이너(T5) (§12 공통 사전조건) | ① `확대`(aria-label `확대`) 클릭 ② 대화상자 내용 확인 | 선택 도면 확대 표시 — 근거: `PAT-DET-140`. 참고 관측: `role="dialog"` 개방, `도 1`+도면 설명. `도면 이미지 없음`(aria-label `도 1 확대`) 버튼이 이미지 미적재 상태를 명시 |  |
| TC-PAT-DET-007 | `타임라인` |  | 기준 문헌 /patents/KR%2010-2620258%20B1. 로드 완료 대기(T6)·스크롤은 내부 컨테이너(T5) (§12 공통 사전조건) | ① 타임라인 표시 값과 서지사항 동일 항목 값 대조 | 서지 일자 컬럼 조합과 일치 — 근거: `PAT-DET-050`(타임라인=일자 조합). 참고 관측(기준 문헌): 출원일 `2021-11-02` / 공개일 `2023-05-09` / 등록일 `2023-12-27` / 존속만료 `2043-12-27`, 서지사항과 일치 |  |
| TC-PAT-DET-008 | 패밀리·인용·피인용·선행기술문헌 |  | 기준 문헌 /patents/KR%2010-2620258%20B1. 로드 완료 대기(T6)·스크롤은 내부 컨테이너(T5) (§12 공통 사전조건) | ① `패밀리 문헌 (N건)` 표시·목록 확인 ② `인용 (N건)`·`피인용 (N건)` 목록 채집 후 **두 집합 대조** ③ `선행기술문헌 (N건)` 표기 형식 확인 | 인용(backward)과 피인용(forward)은 서로 다른 집합 — 근거: `PAT-DET-110`·수집 명세(인용 `citation` vs 피인용 `prior_technology_document` 분리) | 참고 관측(기준 문헌): 인용 2건과 피인용 2건의 문헌번호가 **완전 동일**(`KR 10-2025-0129739` `KR 10-2012-0135415`) → D40 / 선행기술문헌 표기 혼재(`KR KR 10-2002-0072536` 국가코드 중복, `1014896661` 등 국가코드 없음) → D41 |
| TC-PAT-DET-009 | 분류코드·기타정보 |  | 기준 문헌 /patents/KR%2010-2620258%20B1. 로드 완료 대기(T6)·스크롤은 내부 컨테이너(T5) (§12 공통 사전조건) | ① `IPC`/`CPC` 값·구분자 확인 ② `권리변동 이력`·`행정처리(수발신) 이력` 표시 확인 ③ 서지사항 `권리변동` 값과 정합 확인 | 분류·이력 표시 — 근거: `PAT-DET-120/130`. 참고 관측(기준 문헌): IPC `G06F-003/0483 · G06F-016/957 · G06F-040/216`(구분자 `·`), 권리변동 `2023-12-29`/`주식회사 플렉슬`, 행정처리 1행, 서지 `권리변동`=`있음`과 정합 | 참고: 상세에서 IPC가 정상 표시되면 목록 `IPC` 컬럼 `-`는 표 표시 문제로 확정 (BUG-024 보강) |
| TC-PAT-DET-010 | 도면 수 표기 정합 |  | 기준 문헌 /patents/KR%2010-2620258%20B1. 로드 완료 대기(T6)·스크롤은 내부 컨테이너(T5) (§12 공통 사전조건) | ① 서지사항 `도면 수` 값과 `도면 (N)` 섹션 표기·`도면 이미지 없음` 상태 대조 | 서지 `도면 수`와 도면 섹션 건수 정합 — 근거: `PAT-DET-140`·수집 명세(`drawing_count`) | 참고 관측(기준 문헌): `도면 수` `0건`인데 `도면 (1)` 섹션 존재 → D42 |
| TC-PAT-DET-011 | `탭 닫기` (경로 2종 — T10) |  | 기준 문헌 /patents/KR%2010-2620258%20B1. 로드 완료 대기(T6)·스크롤은 내부 컨테이너(T5) (§12 공통 사전조건) | ① 목록에서 `새 탭에서 열기`로 상세 개방 → `탭 닫기` 클릭 → 탭 종료 확인 ② 주소창 직접 진입 → `탭 닫기` 클릭 → 목록 이동(fallback) 확인 | opener 있는 탭=닫힘 / 직접 진입=목록 복귀 — 근거: 실측(결함 아님 확정 §16)·`PAT-DET-013` |  |
| TC-PAT-DET-012 | 존재하지 않는 문헌번호 진입 |  | 기준 문헌 /patents/KR%2010-2620258%20B1. 로드 완료 대기(T6)·스크롤은 내부 컨테이너(T5) (§12 공통 사전조건) | ① `/patent-front/patents/9999999999999` 직접 진입 ② 오류 표시·잔여 UI 확인 | 오류 안내 — 근거: `PAT-LST-070`(실패=오류 메시지)·실측. 확인 지점: `role="alert"` + V4 문구, 상단 `탭 닫기`+푸터만 렌더 |  |

## 논문 입력

| 케이스 ID | 케이스명 | 요소 | 사전조건 | 수행 절차 | 기대 결과 · 판정 기준 | 참고 관측값(2026-08-10) · 회귀 연계 |
|---|---|---|---|---|---|---|
| TC-PAP-INP-001 | 초기 구성 · 특허와의 차이 |  | 논문 검색 화면 (/patent-front/papers) | ① `/patent-front/papers` 진입 ② 필터 구성·위젯 종류 채집, 특허와 대조 | 근거: `PAP-INP-010/020/040/050`, 실측: / - `발행연도` — `전체` `최근 1년` `최근 5년` `최근 10년` + `input[type=number]` placeholder `YYYY` 2개 (특허는 `기간`+`input[type=date]`) / - `검색어 범위` — `제목+초록`(기본) / `제목+초록+전문` / - `항목별 상세검색\n— 제목·초록·키워드·저자 등 필드별 조합` / - `검색 히스토리\n(N)` — 특허와 별도 카운트(도메인별 관리) |  |
| TC-PAP-INP-002 | `발행연도 프리셋` |  | 검색식 `하이브리드 and 자동차`, `검색어 범위`=`제목+초록+전문` / (공통: 논문 검색 화면 (/patent-front/papers)) | ① 각 라디오 선택 → `검색` (4회) ② URL·건수·연도 입력창 자동 반영 확인 ③ I7 검산(단조성) | `yearFrom/yearTo 반영`, 단조성 정합 — 근거: `PAP-INP-010`, 불변식 I7 | 참고 관측값: `전체` 438(파라미터 없음) / `최근 1년` `yearFrom=2025&yearTo=2026` 0 / `최근 5년` 20 / `최근 10년` 126. 기본값이 `전체`라 특허 BUG-003 같은 미적용 문제 없음 |
| TC-PAP-INP-003 | 논문 `검색어 범위` |  | 논문 검색 화면 (/patent-front/papers) | ① `제목+초록` / `제목+초록+전문` 각각 선택 → `검색` ② URL·건수 확인 | 범위 확장 시 건수 증가(포함 관계) — 근거: `PAP-INP-020` | 참고 관측값: `제목+초록`(기본) 39·파라미터 없음 / `제목+초록+전문` `scope=TITLE_ABSTRACT_FULL` 438 |
| TC-PAP-INP-004 | 논문 검색식 유효성 검증 (특허와 동일 여부) |  | 논문 검색 화면 (/patent-front/papers) | ① `and` / `(도서관 and 시스템` / `"도서관 시스템` / `@#$%` / `도서관 and 시스템` 순차 입력 ② 버튼 상태·문구를 특허(TC-PAT-FRM-002~006)와 대조 | 특허와 동일한 V1·V2 문구·동일 판정 패턴 — 근거: 실측(2026-08-10 동일 확인) | 회귀 연계: BUG-010·BUG-011이 논문에도 동일 적용됨 (별도 결함 아님) |
| TC-PAP-INP-005 | 논문 `항목별 상세검색` 필드 카탈로그 |  | 논문 검색 화면 (/patent-front/papers) | ① 펼침 ② 필드·플레이스홀더 채집 ③ `검색 필드 추가·확장` 버튼 존재 여부 확인 | (기준 카탈로그, 실측): `TI` 제목 / `AB` 초록 / `KWD` 키워드 / `DSC` 전문 / `AP` 저자 — 5개. **특허와 달리 필드 확장 버튼 없음.** 입력 id 접두사 `field-search-` | 참고: `DSC`가 특허=`상세설명`, 논문=`전문`으로 도메인별 의미가 다름 (D38 계열 — BUG-022와 같은 명명 문제로 기록) |
| TC-PAP-INP-006 | 논문 `초기화` |  | 검색 실행 + `pageSize`·`pageNo` 파라미터 존재 상태 (예: `?query=도서관&pageSize=100&pageNo=3`) / (공통: 논문 검색 화면 (/patent-front/papers)) | ① `초기화` 클릭 ② URL·검색식·라디오·빈 상태 확인 | 입력값·활성필드 디폴트 복원 — 근거: `PAP-INP-032`. 확인 지점: 검색식 비움·`검색` `disabled`·`전체`/`제목+초록` 복귀·빈 상태 E2 | 참고 관측: `query`·`pageNo`는 제거되나 **`pageSize=100` 잔존** → D43 (판정 보류 — `pageSize`는 입력값이 아닌 표시 옵션이므로 의도일 수 있음. 설계 확정 필요) |
| TC-PAP-INP-007 | 논문 `검색 히스토리` |  | 논문 검색 화면 (/patent-front/papers) | ① `검색 히스토리\n(N)` 펼침 ② 구성(헤더·버튼·안내)을 특허(TC-PAT-INP-025)와 대조 ③ 특허 히스토리와 카운트 분리 확인 | 특허와 동일 구조(`PAP-INP-050` — `특허와 동일 · 공통·횡단`), 도메인별 별도 카운트 | 회귀 연계: ★즐겨찾기·삭제 부재 시 BUG-008/009 (논문 동일 적용) |
| TC-PAP-INP-008 | 발행연도 직접 입력·역전 |  | 논문 검색 화면 (/patent-front/papers) | ① 연도 입력창(YYYY 2칸)에 `2016`~`2026` 입력 → `검색` → URL·건수 확인 (프리셋 `최근 10년`의 자동 반영 값과 동일 — 실측 126건과 대조 가능) ② 역전 값 `2026`~`2016` 입력 → 안내·차단·적용 여부 기록 ③ 특허 일자 역전(TC-PAT-INP-018)과 동작 대조 | `yearFrom/yearTo 반영` — 근거: `PAP-INP-010`. 역전 처리 명세는 없음 — 특허 BUG-005와 동일 양상이면 동일 계열로 기록 |  |

## 논문 결과 목록

| 케이스 ID | 케이스명 | 요소 | 사전조건 | 수행 절차 | 기대 결과 · 판정 기준 | 참고 관측값(2026-08-10) · 회귀 연계 |
|---|---|---|---|---|---|---|
| TC-PAP-LST-001 | 결과 목록 구성 |  | 논문 검색 실행 상태(참고: `도서` 296 / `도서관` 543) (§14 공통 사전조건) | ① 상단 요약바·좌측 필터·정렬/개수·카드 구성·페이지네이션 채집 | 근거: `PAP-LST-010/020/030/040`, 실측: / - 상단: `검색식` + 입력값 + 건수 + `특허로`(렌더 `→\n특허로`) / - 좌측 필터: `발행연도` `저널명` `모든 필터` (모두 `haspopup="dialog"`) / - 정렬/개수: `매칭순` `20개` / - 카드: 체크박스(`<제목> 선택`) · 제목 · `새 탭에서 열기` · 저자·저널·연도 메타라인 · 초록 미리보기(말줄임) / - 페이지네이션 `1`~`10` + `이전/다음 페이지 그룹` |  |
| TC-PAP-LST-002 | 초록 미리보기 잘림 |  | 논문 검색 실행 상태(참고: `도서` 296 / `도서관` 543) (§14 공통 사전조건) | ① 목록 카드의 초록이 말줄임으로 잘려 표시되는지 확인 | 초록 전문 미노출(미리보기만) — 근거: 설계 `abstractPreview.ts`(미리보기 규격)·실측 Pass |  |
| TC-PAP-LST-003 | 카드 클릭 — 상세 이동 여부 |  | 논문 검색 실행 상태(참고: `도서` 296 / `도서관` 543) (§14 공통 사전조건) | ① 카드 제목 클릭 ② URL 변화·오버레이 개방 여부 확인 ③ 카드 내 링크 요소 구조 확인 | 특허 목록과 동일하게 상세 진입 — 근거: `PAP-LST-021`(선택→패널 오픈 — 단 특허와 상호작용 일관성 필요) | 참고 관측: URL 불변, `role="dialog"` 미리보기 오버레이 개방. 카드 내 링크는 `새 탭에서 열기`뿐, 제목은 `span` → BUG-026 (사용자 제보 재현) |
| TC-PAP-LST-004 | 미리보기 오버레이 구성·이동 |  | 논문 검색 실행 상태(참고: `도서` 296 / `도서관` 543) (§14 공통 사전조건) | ① 카드 클릭으로 오버레이 개방 → 구성 채집 ② `다음`(`▶`) 2회 → 위치 표시 `2/20`→`3/20`·본문 교체 확인 ③ `이전`(`◀`) → `2/20` ④ `닫기` → 오버레이 닫힘·URL 유지 확인 | 근거: 실측 Pass: 제목 `논문 상세`, 부제 `선택한 논문의 상세 정보`, 위치 `n / 페이지 내 건수`, 버튼 `이전`/`다음`/`닫기`/`본문 보기\n내부 전용`, 링크 `새 탭`·`출처 이동`, 필드 `저자명` `발행일` `저널명` `저널명(영문)` `분야` `키워드`(값 없으면 `—`) |  |
| TC-PAP-LST-005 | `정렬`·`페이지당 개수` |  | 논문 검색 실행 상태(참고: `도서` 296 / `도서관` 543) (§14 공통 사전조건) | ① `정렬` 개방 → 옵션 채집 → `발행연도(최신)` 적용 → URL·상위 연도 내림차순 확인 ② `페이지당 개수` `100개` 적용 + `pageNo=3` 이동 → 행수·페이지 버튼 수 I8 검산 | 옵션 `매칭순`/`발행연도(최신)`/`발행연도(오래된)` (특허와 축 다름 — 근거: `PAP-LST-040` `매칭순/발행연도`), `sort=PUBLICATION_DATE_DESC`, I8 정합 | 참고 관측값: `도서관` 543건·`pageSize=100` → 카드 100·페이지 6 |
| TC-PAP-LST-006 | 좌측 패싯 `발행연도`·`저널명`·`모든 필터` |  | 논문 검색 실행 상태(참고: `도서` 296 / `도서관` 543) (§14 공통 사전조건) | ① 각 패싯 개방 → 항목수 채집 ② I6 검산: 개별 합 = `모든 필터` 체크박스 수 ③ `모든 필터`에서 연도 1개(예: `2024 (3건)`) 체크 → `적용` → URL·건수·버튼 선택 수 확인 | 저널/연도 필터 추가해 재검색 — 근거: `PAP-LST-030`. I6 정합, 적용 건수=패싯 표기 건수 | 참고 관측값(도서관 543건): `발행연도` 42 / `저널명` 19 / `모든 필터` 61. `2024 (3건)` 적용 → `fk_years=2024`·`3건` |
| TC-PAP-LST-007 | 선택(체크박스) — 특허와 대조 |  | 논문 검색 실행 상태(참고: `도서` 296 / `도서관` 543) (§14 공통 사전조건) | ① `전체 선택` 클릭/재클릭 ② 행 1건만 선택 → `전체 선택` 상태 확인 (특허=`mixed`와 대조) ③ 선택 후 액션 버튼 존재 여부 확인 | 선택 상태 반영 + 선택 대상 액션 — 근거: `PAP-LST-022/060` | 참고 관측: 1건 선택 시 `전체 선택`이 `false`로 남음(특허는 `mixed`) → D44 / 선택 후 액션 없음 → BUG-027 (특허·논문 공통) |
| TC-PAP-LST-008 | `특허로` (교차검색)·하이라이팅 |  | 논문 검색 실행 상태(참고: `도서` 296 / `도서관` 543) (§14 공통 사전조건) | ① 논문 결과(정렬·패싯 적용 상태)에서 `특허로` 클릭 → URL·건수·breadcrumb·논문 전용 파라미터 제거 확인 ② 결과 내 검색어 하이라이팅(`mark` 요소) 확인 | 검색어 이관·전환, 논문 전용 파라미터(`sort=PUBLICATION_DATE_DESC`·`fk_years`) 제거 — 근거: `PAP-LST-010`·`COM-050`·실측 Pass. 하이라이팅 구현(client) |  |
| TC-PAP-LST-009 | `새 탭에서 열기`·페이지네이션 |  | 논문 검색 실행 상태(참고: `도서` 296 / `도서관` 543) (§14 공통 사전조건) | ① `새 탭에서 열기` 클릭 → 새 탭 상세 렌더·href(`/patent-front/papers/<uuid>?query=…`)·`rel="opener"` 확인 ② 페이지네이션 숫자·`이전/다음 페이지 그룹` 이동 확인 | 새 탭 상세 진입(검색어 파라미터 유지)·페이지 이동 재요청 — 근거: `PAP-LST-020/040`·실측 Pass | 기준 문헌: `/patent-front/papers/019fb750-6b52-7891-a1e5-ea7c180c96ad?query=도서` (`웹기반 도서 위치 시각화 시스템`) |

## 논문 상세

| 케이스 ID | 케이스명 | 요소 | 사전조건 | 수행 절차 | 기대 결과 · 판정 기준 | 참고 관측값(2026-08-10) · 회귀 연계 |
|---|---|---|---|---|---|---|
| TC-PAP-DET-001 | 논문 상세 구성 |  | 기준 문헌 /papers/019fb750-6b52-7891-a1e5-ea7c180c96ad?query=도서 (§14 공통 사전조건) | ① 상단·필드·관련 논문·푸터 채집 | 근거: 실측(논문 상세는 설계 보류 영역 — 구성 확인 위주): / - 상단: `탭 닫기` · 제목 · 대등제목 · `출처 이동` · `본문 보기\n내부 전용` / - 필드: `저자명` `발행일` `저널명` `저널명(영문)` `분야` `키워드` `초록` `영문초록` — 값 없는 필드는 `—` / - `관련 논문` `검색결과 상위 10건` — 10항목, 각 `/patent-front/papers/<uuid>?query=…` 링크 |  |
| TC-PAP-DET-002 | 초록 미리보기 규격 |  | 기준 문헌 /papers/019fb750-6b52-7891-a1e5-ea7c180c96ad?query=도서 (§14 공통 사전조건) | ① `초록은 미리보기만 가능합니다.` 출현 횟수 확인(`초록`·`영문초록` 각 1회=2회) ② 표시 문자수 계수 ③ 말줄임 기호 문자 확인 (U+2026 `…` vs ASCII `...`) | 안내 문구 2회, 말줄임은 `…`(U+2026 단일 문자) — 근거: 설계 `src/features/abstractPreview.ts` `PREVIEW_ELLIPSIS`·구간표 | 참고 관측: 초록 150자·영문초록 207자, 말줄임 **`...`(ASCII 3자)** → BUG-033. 원문 전체 길이를 화면에서 확인할 수 없어 구간표 준수는 `확인 불가` |
| TC-PAP-DET-003 | `본문 보기` | `본문 보기\n내부 전용` (button) | 기준 문헌 /papers/019fb750-6b52-7891-a1e5-ea7c180c96ad?query=도서 (§14 공통 사전조건) | ① 클릭 ② 중첩 대화상자 전부 확인 (T9 — `querySelectorAll`) ③ 내용·버튼 채집 | `본문 전문\n내부 전용` 대화상자에 전문 표시, 버튼 `PDF 원본 열기`·`닫기` — 근거: 실측 Pass (내부 전용 기능이므로 전문 노출은 의도된 동작 — 사용자 확인 2026-08-10) |  |
| TC-PAP-DET-004 | `관련 논문` |  | 기준 문헌 /papers/019fb750-6b52-7891-a1e5-ea7c180c96ad?query=도서 (§14 공통 사전조건) | ① 목록 10항목 확인 ② 1번 항목이 현재 논문 자신인지 확인 ③ 2번 항목 클릭 → 해당 논문 상세 이동 확인 (1번은 자기 자신 — BUG-034 확인과 분리) | 관련 문헌 목록·이동 — 자기 자신 포함 시 BUG-034 |  |
| TC-PAP-DET-005 | `출처 이동` |  | 기준 문헌 /papers/019fb750-6b52-7891-a1e5-ea7c180c96ad?query=도서 (§14 공통 사전조건) | ① href 확인 (실제 외부 이동은 실행하지 않음 — `추후 추가`) | (기준 href): `https://scienceon.kisti.re.kr/srch/selectPORSrchArticle.do?cn=JAKO200311921971549` |  |
| TC-PAP-DET-006 | `탭 닫기` (경로 2종 — T10) |  | 기준 문헌 /papers/019fb750-6b52-7891-a1e5-ea7c180c96ad?query=도서 (§14 공통 사전조건) | ① `새 탭에서 열기`로 열린 탭에서 `탭 닫기` → 탭 종료 ② 직접 URL 진입 후 `탭 닫기` → 목록 이동(fallback) | 경로별 동작 — 근거: 실측(결함 아님 확정) |  |
| TC-PAP-DET-007 | 존재하지 않는 uuid 진입 |  | 기준 문헌 /papers/019fb750-6b52-7891-a1e5-ea7c180c96ad?query=도서 (§14 공통 사전조건) | ① `/patent-front/papers/00000000-0000-0000-0000-000000000000` 직접 진입 | `role="alert"` + V5 문구(`논문을 불러오지 못했습니다.…`), `탭 닫기`+푸터만 렌더 — 근거: 실측 Pass (도메인별 문구 구분 정상) |  |
| TC-PAP-DET-008 | `PDF 원본 열기` |  | `본문 보기` 대화상자 개방 상태 / (공통: 기준 문헌 /papers/019fb750-6b52-7891-a1e5-ea7c180c96ad?query=도서 (§14 공통 사전조건)) | ① `PDF 원본 열기` 클릭 → 동작 기록 | `근거 없음` (명세 없음 — 2026-08-10 미실측 항목. 동작 기록만) |  |
