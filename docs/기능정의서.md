# AXP 선행기술검색 프로토타입 — 기능정의서 (검색 기능 · 상세 제외)

문서 목적: 이번에 만든 **목업/프로토타입(웹에서 실제 렌더되는 기능 기준)**의 기능을, **UI 상 이용자의 이용 흐름**과 **표출되는 정보의 표시 기준** 중심으로 화면별 정의한다. 각 화면 요소의 데이터 출처(API)는 **연동 시 참고**로 병기하되, 목업 단계이므로 확정 구현 스펙으로 보지 않는다.

> ⚠️ **범위(2026-07-06)**: **특허 상세페이지는 작성 완료**(UI 렌더 기준). 상세 필드는 **`[BLK] 특허 수집 데이터베이스 명세서`의 수집 컬럼과 1:1 대조**돼 있다(필드→컬럼 매핑은 「특허 · 상세페이지」 **④에 인라인**) — 즉 **필드 소스는 이미 알려져 있다.** 미정인 것은 값이 아니라 **이 수집 컬럼을 상세페이지로 전달할 검색/상세 API 노출 방식**(서비스 DB·API 미설계). 현재 프로토타입의 표시 값만 목업이다. **논문 상세페이지는 여전히 보류.**

원칙(작성 규칙):
- **UI에 실제 렌더되는 것만 기재한다(내용 창작 금지).** 코드로 확인되지 않는 동작·필드는 추정해 쓰지 않는다. 미구현·미확정은 그대로 `⚠ 미구현 / 연동 시 정의`로 표기한다.
- **API 명세에만 있고 UI에 없는 항목의 누락은 허용**한다(문서에서 생략 가능).
- **UI(웹 렌더)에 있는데 이 문서에서 누락되면 안 된다.** → 대상 UI 요소는 전부 기술하고, 대응 API가 없으면 `⚠ API 미정의(목업)`로 명시한다.

> **구성**: 아래 세 부분은 **각각 독립적으로 게시**될 수 있다. ① 배경 및 참고자료(맥락·근거) · ② 메뉴·화면별 기능명세(구현 대상) · ③ 부록(연동 필요 항목·보류).

---

# 배경 및 참고자료 (AXP 선행기술검색 · 검색 기능)

> 핸드오프 컨텍스트. 이 부분은 요구사항·API 구조를 이해하기 위한 **참고 자료**이며, 구현 대상 명세는 「메뉴·화면별 기능명세」에 있다.

## 참고 문서·출처

| 문서/출처 | 위치·경로 | 성격 |
|---|---|---|
| **bigtext 매뉴얼** (PDF, 3종) | 로컬 `검색 API명세서/[JARVIS-PATENT-API] _search_bigtext(-group-by, -string-list-group-by)` | ISE BIGTEXT wrapper 저수준 API. snake_case, `data{}` 봉투 |
| **특허검색 동작방식 개발노트** (PDF) | 로컬 `검색 API명세서/[AXP] 특허검색 동작방식 - 개발노트` | 화면 동작 요구사항 + 표시필드 매핑 |
| **prior-art-search** (데모 저장소) | `github.com/muhayu/prior-art-search` (private, `main`) | 실제 데모 프런트+BFF. `docs/spec/search-ux.md`(≡ 동작방식 개발노트), `docs/dev-notes/search-behavior.md`(구현 매핑), `docs/search-api.md`(bigtext 원형 ≡ bigtext 매뉴얼), `backend/…/api/{patents,papers}.py`, `frontend/src/*` |
| **ck-patent** (실서비스 프런트 저장소) | `github.com/muhayu/ck-patent` (private, `dev`) `application/patent-front/src/entities/prior-art/*` | BFF 와이어 계약(camelCase) — prior-art-search와 동일 계약 |
| **프로토타입** (본 저장소) | `src/views/*`, `src/components/PatentDetail.tsx` 등 | 이번에 만든 목업(웹 렌더 기준) |

> prior-art-search(데모 저장소)의 `docs/spec/search-ux.md`가 특허검색 동작방식 개발노트(PDF)와 **동일 내용**임을 확인했다. 이 데모의 BFF(`/search/prior-art/*`)가 곧 실제 UI 계약이며 ck-patent(실서비스 프런트)와 일치한다.

**상세페이지 보류**: prior-art 검색 API에는 문서 상세 전용 엔드포인트가 없고, **상세에 노출할 필드는 수집 DB 설계에 좌우**된다(현재 미설계). 상세/오버레이는 목록 검색 응답의 `hit`(+`columns`)를 재사용해 렌더하지만, 어떤 필드를 어떻게 채울지는 DB 확정 후 정의한다. 따라서 상세(오버레이 포함)는 「부록」의 보류 항목에 사유·후속 정의 대상만 남기고 본 기능명세에서는 제외한다.

---

## 명세 비교 및 차이 (분석)

### API는 2계층 구조다

```
[UI/프로토타입]
    │  호출
    ▼
(BFF) /resources/search/prior-art/{muhayu-patents | muhayu-papers | patent-facets | paper-facets}   ← prior-art-search BFF (실제 UI 계약)
    │  래핑
    ▼
(ISE) /search/bigtext , /search/bigtext-group-by , /search/bigtext-string-list-group-by            ← bigtext 매뉴얼 (저수준 API)
```

- **bigtext 매뉴얼(저수준)** 은 최하위 ISE 검색엔진 wrapper를 문서화한 것이다. 필터(국가·상태·기간)를 별도 파라미터로 받지 않고 `search_query`/`select_query`/`filter_query`에 접어 넣는다. 응답도 `field_name[]`+`result[][]` 원시 배열이다.
- **prior-art-search BFF** 는 UI 친화 계약이다. 필터를 **구조화 파라미터**(statuses/docTypes/countries/ipcMain/cpcMain/dates/years/venues)로 받고, 응답을 **타입드 객체**(`Patent`/`Paper`) + `columns`(60+ 컬럼 패스스루)로 돌려준다. 패싯도 `FacetAxisResult`로 정규화한다.
- 프로토타입은 BFF 계약과 **거의 일치**한다(관측된 실제 호출 `POST /search/prior-art/muhayu-patents {searchQuery, fetchOffset, fetchCount, orderByQuery, statuses, docTypes, countries}` 와 동일 형태).

### 두 계층 간 주요 차이

| 항목 | bigtext 매뉴얼 (ISE 저수준) | prior-art-search BFF (실제) |
|---|---|---|
| 엔드포인트 | `/search/bigtext(-group-by…)` | `/resources/search/prior-art/{muhayu-patents/papers, patent/paper-facets}` |
| 표기 | snake_case, `data{}` 봉투 | camelCase, 봉투 없음 |
| 검색식 | `search_query` (필터 포함해 직접 작성) | `searchQuery` + 구조화 필터 파라미터 분리 |
| 필터(특허) | select/filter query에 포함 | `statuses[] docTypes[] countries[] ipcMain[] cpcMain[] dateFrom/To applicationYears[]` |
| 필터(논문) | 동일하게 query 포함 | `venues[] years[] yearFrom/To` |
| 응답 | `field_name[]`+`result[][]` 원시 | `hits[].patent/paper`(타입드) + `columns{}` 패스스루 |
| 패싯 | group-by/string-list-group-by(gsize/gkey) | `facets.{축}: {items[{key,count}], totalGroups, fetched, error}` |
| 스니펫 하이라이트 | `keyword_char_offset` 제공 | BFF hit엔 미노출(프런트는 클라이언트 하이라이트) |

### 요구사항(동작방식 개발노트) ↔ 실제 응답(BFF) 불일치 — 확인 필요

| 내용 | 동작방식 개발노트 | prior-art-search BFF | 판정 |
|---|---|---|---|
| 특허 패싯 축 | 국가·**권리상태**·IPC주분류·CPC주분류·출원연도 | `countryCode, ipcMain, cpcMain, applicationYear` | ⚠ **권리상태(register_status) 패싯이 실제 응답에 없음** |
| 논문 국가 | 국가 개념 없음 | `Paper.countryCode: null` | 일치(항상 null) |

### 프로토타입 UI에는 있으나 API(bigtext 매뉴얼·BFF)에 정의가 없는 항목 (⚠ 목업 전용)

검색어 입력·결과 목록에서 **웹에 렌더되지만 대응 API 필드가 없는** 요소는 각 매핑표에서 `⚠ API 미정의(목업)`로 표기한다. 대표적으로:

- **특허 목록**: 원문 PDF URL(원문 버튼), 명칭 키워드 하이라이트(클라이언트 계산).
- **논문 목록**: 권(volume)·호(issue)·시작/끝 페이지·발행 월·논문유형(thesis)·학위수여기관 — `Paper` 타입 미정의.
- **공통**: 라이브러리 저장, 결과 CSV(클라이언트 생성), 검색 히스토리(로컬 상태), 특허↔논문 교차검색 — 모두 프런트 상태/클라이언트 기능(백엔드 API 불필요).
- (상세 전용 ⚠목업 항목 — 도면·패밀리·인용·주소류·구조화 등 — 은 「부록」의 보류 항목으로 이관)

### 프로토타입 ↔ 실제 데모(prior-art-search) 차이 — 확인/보완 필요

| 항목 | 실제 데모 (prior-art-search, search-behavior.md) | 프로토타입 | 판정 |
|---|---|---|---|
| 범위(모드) 매핑 | `key`→`KEY:` = 명칭+**초록**+**대표청구항**, `bib_full`→`TAC:` = 명칭+초록+전체청구항, `full`→`DSC:` = **상세설명(전문)** | `KEY`=명칭+요약+독립항 / `TAC`=명칭+요약+전체청구항 / `DSC`=명칭+요약+전체청구항+상세설명 | ⚠ **용어·구성 상이**. 실제: 초록/대표청구항, DSC=상세설명 단독. 프로토타입은 요약/독립항, DSC=전문 합산(사용자 지시 반영분). 라벨·매핑 정합 확인 필요 |
| 검색식 패스스루 | `search_query`는 1바이트도 변형 금지, 메타필터는 `select_query`(SQL WHERE)로 분리 | 동일(자유어 불변 + 필드 AND 누적) | 일치 |
| 상태관리(SSOT) | **URL 쿼리스트링이 단일 진실원**(q·country·state·from·to·mode·paperMode·sort·page·pageSize·selected·facets), 공유링크/뒤로가기/새로고침 복원 | React 상태(해시 탭). URL 직렬화·공유·복원 **미구현** | ⚠ 프로토타입 미구현(데모 핵심 규칙) → 공통·횡단 기능 참조 |
| 재검색 재요청 | URL 무변경 submit 시 명시적 invalidate | 동일 개념(항상 재실행) | 일치 |
| 검색필드 유지 | 입력값만 비우고 활성필드 구성 유지, 디폴트 복원은 초기화만 | 동일 | 일치 |
| WIPS ON 식(.IDENT./@필드) | 전문가식으로 보고 모드 미부착·원본 통과 | applyScope가 선두 자유어에만 래핑(필드지정식 불변) | 대체로 일치(WIPS 식별자 특례는 미검증) |

---

## BFF 계약 요약 (prior-art-search BFF — 매핑 기준)

**공통**: `POST /resources/search/prior-art/…`, `Content-Type: application/json`, 실패 시 `{message}`.

### 특허 검색 `/muhayu-patents`
- 요청 `PatentSearchRequest`: `searchQuery`, `fetchOffset?`, `fetchCount?`(기본 20), `orderByQuery?`, `statuses?[]`, `docTypes?[]`, `dateFrom?`, `dateTo?`, `countries?[]`, `ipcMain?[]`, `cpcMain?[]`, `applicationYears?[]`
- 응답 `PatentSearchResponse`: `totalCount`, `fetchCount`, `hits[]`(`PatentHit{ patent, dbfile, rowid }`)
- `Patent`: `docid, inventionTitle, inventionTitleEng, abstract, applicant, inventorName, agentName, applicationNumber, applicationDate, publicationDate, registerDate, openDate, searchBaseDate, ipcMain, cpcMain, registerStatus, countryCode, documentKind, columns{60+}`

### 논문 검색 `/muhayu-papers`
- 요청 `PaperSearchRequest`: `searchQuery`, `fetchOffset?`, `fetchCount?`, `orderByQuery?`, `yearFrom?`, `yearTo?`, `venues?[]`, `years?[]`
- 응답 `PaperSearchResponse`: `totalCount`, `fetchCount`, `hits[]`(`PaperHit{ paper, dbfile, rowid }`)
- `Paper`: `paperId, title, titleEng, abstract, authors[], journal, publicationYear, publicationDate, keywords[], internalFileUrl, externalFileUrl, citationCount, countryCode(null), score`

### 패싯 `/patent-facets`, `/paper-facets`
- 요청: 검색요청과 동일 필터(검색어+필터).
- 특허 응답 `facets: { countryCode, ipcMain, cpcMain, applicationYear }` (각 `FacetAxisResult{ items[{key,count}], totalGroups, fetched, error }`)
- 논문 응답 `facets: { journal, publicationYear }`

### 검색식 표기 (동작방식 개발노트·프로토타입 공통)
- 필드절 `CODE:(값)`, 일자 `CODE:([from ~ to])`, 범위코드 `KEY`(명칭+요약+독립항)/`TAC`(명칭+요약+전체청구항)/`DSC`(명칭+요약+전체청구항+상세설명). 자유어는 선두에만 범위 적용, 사용자 직접 필드식은 불변.
- 필드코드 카탈로그는 **검색필드 명세(정본)** 기준으로 정렬한다 — 항목별 상세검색(`PAT-INP-050`)의 필드는 이 정본과 1:1이어야 한다.
- **정합 주의 — 검색필드에서 제외 확정(2026-07-08, 팀 확인)**:
  - **`BT` 배경기술 · `SM` 해결수단 제외** — 상세설명(`DSC`) 본문에 배경기술·해결수단 등이 모두 포함되므로 `DSC`로 충분(개별 검색필드 불필요).
  - **`CC` 국가코드 제외** — 국가 조건은 `select_query`(예: `country_code in ('KR','US')`)로 처리되어 `search_query` 인덱스를 쓰지 않음. 국가는 상단 **국가 메타필터**(`PAT-INP-011`)로만 처리.
  - 따라서 명세서 본문 검색필드는 **기술분야·발명효과·해결과제·구체실시방식·도면의 간단한 설명**뿐. 비특허 인용 필드는 `NPCT`(비특허인용 명칭+저자).

---

# 메뉴·화면별 기능명세 (AXP 선행기술검색 · 검색 기능)

> **목업 단계 서술 기준**: 지금은 목업/프로토타입 단계이므로, **UI 상 이용자의 이용 흐름**과 **표출되는 정보의 표시 기준**을 중심으로 기술한다. 구체적 구현 방안(엔드포인트·필드 배선 등)은 **정보 출처(연동 시 참고)**에 후보로만 남기고, 확정 스펙으로 보지 않는다.
>
> 각 화면 서술 순서: ① 목적 → **② 구성요소별 정의(화면 위계 순 통합표 — 구성요소·표시 정보·기능·사전조건·사후결과를 한 행에)** → (목록 화면) 목록 항목 구성 → ④ 정보 출처(연동 시 참고) → ⑤ 상태/URL → ⑥ 예외 → ⑦ 접근성.
>
> **② 통합표**는 화면에 나타나는 순서(정보 위계)대로 구성요소를 나열하고, 각 구성요소의 표시 내용·상호작용·사전조건·사후결과를 한 행에 모아 이용 흐름을 위→아래로 읽을 수 있게 한다.
>
> 표기: `client`=프런트 계산/상태, `⚠목업`=대응 API 미정의(연동 시 정의).
>
> **기능 ID(채번) · 2레벨(영역→액션)**: `도메인-화면-순번` — 도메인(특허=`PAT` / 논문=`PAP` / 공통=`COM`) · 화면(검색어 입력=`INP` / 결과 목록=`LST` / 상세페이지=`DET`). **영역**은 10단위(`010·020…`), 한 영역에 **사전/사후가 다른 동작이 여럿이면** 1단위로 **액션**을 분리한다(예: `041 검색 실행` / `042 초기화`). 동작이 같은 요소(예: 국가 칩 여러 개)는 나누지 않는다.
> - `data-spec`은 **영역 컨테이너**와 **개별 액션 버튼** 모두에 부착한다 → 영역 여백 클릭=영역 개요, 버튼 클릭=그 액션의 사전/사후를 조회(예: `data-spec="PAT-INP-041"`).
> - 표에서 `└` 접두 행이 상위 영역의 액션이다.

> **기능 설명 정의**: 각 기능을 **사전조건(그 기능을 쓰기 위해 먼저 충족돼야 하는 상태·입력)** 과 **사후결과(그 기능을 쓴 뒤 발생하는 일·상태 변화)** 로 기술한다. 개발·QA가 "언제 이 기능이 동작 가능하고, 실행하면 무엇이 바뀌는가"를 판단하는 기준이다.

## 특허 검색

> **이용 흐름**: 검색어·필터 구성 → 검색 실행 → 결과 목록 확인 → (좁히기: 패싯/정렬) → 행 선택 시 미리보기(오버레이) 또는 새 탭 전체보기 → **상세페이지 정독** · 다중 선택 저장 · 논문으로 교차검색.

### 특허 · 검색어 입력 (`PatentInput`)

**① 목적**: 변리사가 검색식·필터를 구성해 특허 검색을 실행한다.

**② 구성요소별 정의** (화면 위계 순 · 표시 → 기능 → 사전조건 → 사후결과 통합)

| ID | 구성요소 | 표시 정보·구성 | 기능·상호작용 | 사전조건 | 사후결과 |
|---|---|---|---|---|---|
| `PAT-INP-010` | 메타필터 (영역) | 국가·문헌종류·기간·상태 필터 묶음. 활성 칩=체크(✓)+색상 | ↓ 필터별 | 화면 진입 | 개별 필터(011~014) 참조 |
| `└ PAT-INP-011` | 국가 필터 | 전체 / KR / US / JP / CN / EP / PCT / DE / GB + "국가설정"으로 추가국가 | 칩 토글("전체"=라디오, 해제 불가) · 국가설정 팝업에서 추가 | 화면 진입 | 다음 검색 payload에 `countries[]` 포함 |
| `└ PAT-INP-012` | 문헌종류 필터 | 전체 / 공개 / 등록 / 실용공개 / 실용등록 | 칩 토글("전체"=라디오) | 화면 진입 | 다음 검색 payload에 `docTypes[]` 포함 |
| `└ PAT-INP-013` | 기간 필터 | 전체 / 최근5·10·20년 + 일자범위(시작~종료) | 프리셋 라디오 또는 일자 직접 입력 | 화면 진입 | 다음 검색 payload에 `dateFrom`/`dateTo` 반영 |
| `└ PAT-INP-014` | 상태 필터 | 전체 / A(출원·심사중·등록) / I(거절·소멸·취하·포기·무효) | 칩 토글("전체"=전체 초기화, 빈 선택 방지) | 화면 진입 | 다음 검색 payload에 `statuses[]` 포함 |
| `PAT-INP-020` | 검색어 범위 탭 | KEY(명칭+요약+독립항) / TAC(명칭+요약+전체청구항) / DSC(명칭+요약+전체청구항+상세설명) | 탭 전환(라벨 정합은 명세 비교 확인 대상) | 화면 진입 | 선두 자유어에 적용될 범위코드 변경(직접 필드식은 불변) |
| `PAT-INP-030` | 입력방식 토글 | 일반(textarea) / 편집기(구문강조+실시간 진단) | 모드 전환 | 화면 진입 | 표시 방식 전환 · 편집기모드는 진단 표시(실행 차단 안 함) |
| `PAT-INP-040` | 검색식 입력창 + 검색/초기화 (영역) | 입력창 + 우측 검색·초기화 버튼 | ↓ 액션별 | — | — |
| `└ PAT-INP-041` | 검색 실행 | 검색 버튼 | 클릭 / ⏎ | 검색식 입력창 또는 항목별 필드에 값 1개 이상 | `searchQuery` 조립·POST → 결과 목록 전환 · 히스토리 1건 추가 · (연동 시)URL 직렬화 |
| `└ PAT-INP-042` | 초기화 | 초기화 버튼 | 클릭 | — | 입력값·활성필드를 디폴트로 복원 |
| `PAT-INP-050` | 항목별 상세검색 | 필드별 조합(펼침/접힘 · 필드 추가/확장 모달). **입력 컨트롤은 타입별**: 일자=범위(from~to), IPC/CPC=코드+범위(Original/Current), **유무(Y/N)=있음·없음 체크박스 2개**(양도유무·실시권유무·심판/소송유무·비특허인용유무·표준특허유무·서열목록유무 — 둘 다/미선택=전체) · **다중 선택(칩)**=국가류(우선권국가·지정국·출원인/발명자/현재권리자 국적·특허인용/피인용 국가·선언자국적)·문헌종류(kind)·표준화기구, 그 외=텍스트 | 필드 값 입력/체크 시 검색식에 AND 누적 | 상세검색 펼침 · 필드 선택됨 | 검색식에 `CODE:(값)` AND 누적(유무: 있음만=`CODE:(있음)` / 없음만=`CODE:(없음)` / 둘 다·미선택=조건 없음 — Y/N 값 미노출 · 다중선택: 선택값 OR 결합 `CODE:(KR OR US)`, 자유어는 선두만 범위 래핑) · 실행 후 입력값만 비우고 구성 유지 |
| `PAT-INP-060` | 검색 히스토리 (영역) | 최근 검색식 목록(공통·횡단 기능) | ↓ 액션별 | 검색 1회 이상 실행됨 | — |
| `└ PAT-INP-061` | 항목 재검색 | 히스토리 항목 행 | 클릭 | 기록 존재 | 선택 검색식으로 재실행 |
| `└ PAT-INP-062` | ★즐겨찾기 | 별 아이콘 | 토글 | 기록 존재 | 즐겨찾기 on/off 저장 |
| `└ PAT-INP-063` | 삭제 | × 버튼 / 전체 삭제 | 클릭 | 기록 존재 | 해당(또는 전체) 기록 제거 |

**④ 정보 출처 (연동 시 참고 — 목업 단계라 확정 스펙 아님)**

| 요소 | 소스 |
|---|---|
| 검색 실행 payload `searchQuery` | 검색식(입력창) + 항목별필드 AND 누적 + 범위코드 래핑 → `PatentSearchRequest.searchQuery` |
| 국가 칩 | → `countries[]` |
| 문헌종류 칩 | → `docTypes[]` |
| 기간(프리셋/일자) | → `dateFrom`/`dateTo` |
| 상태(A/I 칩) | → `statuses[]` |
| 정렬 기본 | → `orderByQuery`(기본 정렬 규칙은 ⚠연동 시 정의 — 프로토타입엔 입력화면 정렬 컨트롤 없음, 정렬은 결과 목록에서 처리) |
| 필드코드 목록/타입 | prior-art-search `patentSearchField.ts` · 입력 타입 `queryModel.ts`(text/date-range/ipc/**bool**(유무=있음·없음 체크박스)/**multi**(다중선택 칩)) · 필드→코드 정본은 「부록 — 검색 필드코드 카탈로그」 |
| 검색 히스토리·즐겨찾기 | `client` |
| 키워드 추천/파인더 | `⚠목업`(현재 비노출) |

**⑤ 상태/URL**: 검색 실행 시 q·country·state·from·to·mode·필드구성이 URL 쿼리에 직렬화되어야 함(데모 SSOT, 프로토타입 미구현 → 공통·횡단 기능). **⑥ 예외**: 빈 검색식 실행 방지/경고. **⑦ 접근성**: 칩 `aria-pressed`+✓(색상만으로 선택 구분 금지, WCAG 1.4.1).

### 특허 · 결과 목록 (`PatentResults` / `TableResults`)

**① 목적**: 검색 결과를 표/카드로 보여주고 좁히기·정렬·페이지 이동·선택 저장.

**② 구성요소별 정의** (화면 위계 순 · 표시 → 기능 → 사전조건 → 사후결과 통합)

| ID | 구성요소 | 표시 정보·구성 | 기능·상호작용 | 사전조건 | 사후결과 |
|---|---|---|---|---|---|
| `PAT-LST-010` | 상단 요약바 | 적용 검색식 칩 · **총 건수** · → 논문으로 | 교차검색 진입 | 검색 실행 완료 | 총건수 표시 / 교차검색=논문 검색으로 검색어 이관·화면 전환 |
| `PAT-LST-020` | 결과 표(데스크톱)·카드(모바일) (영역) | 컬럼: 체크박스·상태·문헌번호·원문PDF·발명의명칭(+새 탭)·출원인·출원일·IPC. **상태=평문 텍스트**(색 Badge 미사용 — 색만으로 구분 금지, 상태 문자열 그대로 노출) | 목록 렌더 / ↓ 액션별 | 검색 실행 완료(응답 수신) | 목록 렌더(`totalCount`·`hits`) |
| `└ PAT-LST-021` | 행/카드 선택 | 행/카드 본문 | 클릭 | 결과 행 존재 | 선택 인덱스 설정 · 미리보기 패널 오픈(**내용은 보류(상세페이지)**) |
| `└ PAT-LST-022` | 체크박스 선택 | 행별 체크박스 | 체크 | 결과 행 존재 | 저장 대상 선택 상태 반영(→ 다중 선택 저장) |
| `PAT-LST-030` | 패싯 필터바/사이드바 (영역) | 국가 · 권리상태 · **공개/등록(특허구분)** · 출원연도 · IPC(Main/All) · 심판 유무 (프로토타입 `PATENT_FACET_GROUPS_BASE` 기준. **출원인 대표명화 패싯은 이번 텀 미구현으로 제거**) | 체크 후 실행 | 결과 1건 이상 · 패싯 로드됨 | 체크한 축을 필터에 추가해 재검색 → 1페이지로 이동 |
| `└ PAT-LST-031` | IPC 패싯 Main/All 토글 | IPC 패싯 팝오버 상단 `[Main] [All]` (기본 All) | 토글 | IPC 패싯 열림 | Main=최신 대표 IPC(`IPCM`)만 / All=개정이력 포함 전체 IPC(`IPC`) 기준으로 코드목록·건수·매칭 전환 |
| `PAT-LST-040` | 정렬·perPage·페이지네이션 | 출원일/공개일/등록일+방향 · perPage · 페이지 | 변경 시 재요청(새 검색은 1페이지로) | 결과 존재 | `orderByQuery`/`fetchOffset`/`fetchCount` 갱신 후 재요청 |
| `PAT-LST-050` | CSV 다운로드 | 버튼 | 내보내기 | 결과 1건 이상 | 현재 `hits` → CSV 생성·다운로드 |
| `PAT-LST-060` | 다중 선택 저장 | 체크박스 + 선택 저장 버튼 | 라이브러리 일괄 저장 | 체크박스 1개 이상 선택 | 선택 특허를 라이브러리에 저장하고 체크 해제 |
| `PAT-LST-070` | 상태 표시(로딩/0건/오류) | 로딩·0건 안내·오류 메시지 | — | 검색 실행됨 | 응답 대기=로딩 · 0건=안내 · 실패=오류 메시지 |
| `PAT-LST-080` | 중복제거 컨트롤 | 상단 요약바 `중복제거: 사용안함 / 등록 우선 / 공개 우선` | 클릭 | 검색 실행 완료 | 검색이 **문헌번호 기준**이라 동일 출원번호(1건의 출원)가 공개+등록 문헌으로 함께 잡힘 → 출원번호로 묶어 한 건만 남김(등록 우선/공개 우선) · 1페이지로 이동 |

**③″ 목록 항목 구성 (메타데이터 배열 순서·방식·누락 처리)**

- **데스크톱(표)** — 고정 컬럼, 셀당 1필드. 순서: ① 체크박스 → ② 상태(평문) → ③ 문헌번호(mono) → ④ 원문 PDF(아이콘) → ⑤ 발명의 명칭(+새 탭 열기) → ⑥ 출원인 → ⑦ 출원일 → ⑧ IPC
- **모바일(카드)** — ① 체크박스 → ② `상태 · 문헌번호`(한 줄) → ③ 제목(2줄 클램프) → ④ `출원인 · 출원일 · IPC`(한 줄, ` · ` 연결) → ⑤ 원문 PDF·새 탭 버튼
- **문헌번호 구성**: `국가코드 + 번호 + 문헌종류`, 번호는 우선순위(등록 > 공고 > 공개 > 출원)로 1개 확정
- **누락 예외처리**: 표 셀은 값이 없으면 **빈 셀**로 렌더(현재 폴백 텍스트 없음). 모바일 메타라인은 ` · ` **고정 연결**이라 값 누락 시 구분자만 남음 → **⚠ 연동 시 누락 필드 생략/'—' 폴백 규칙 정의 필요**
- CSV 컬럼: 문헌번호·명칭·출원인·출원일·상태·IPC

**④ 정보 출처 (연동 시 참고 — 목업 단계라 확정 스펙 아님)**

| 요소 | 소스 |
|---|---|
| 총 건수 | `PatentSearchResponse.totalCount` |
| 행 목록 | `hits[].patent` |
| 상태(평문) | `patent.registerStatus` |
| 문헌번호 | `patent.countryCode`+공백+번호(우선순위 등록>공고>공개>출원: `registerNumber`>`publicationNumber`>`openNumber`>`applicationNumber`, `columns`에서)+공백+`documentKind` |
| 발명의 명칭 | `patent.inventionTitle`(+`inventionTitleEng`) |
| 출원인 | `patent.applicant` |
| 출원일 | `patent.applicationDate` |
| IPC | `patent.ipcMain` |
| 원문 PDF 아이콘 | `⚠목업`(원문 URL 필드 미정의; `columns` 내 원문키 확인 필요) |
| 명칭 키워드 하이라이트 | `client`(BFF엔 offset 미노출 → 클라이언트 하이라이트) |
| 정렬/페이지/perPage | → `orderByQuery`/`fetchOffset`/`fetchCount` |
| 필터칩·패싯 건수 | 프로토타입: 정적 목업(`facetGroups.ts` — 국가·권리상태·공개/등록·출원연도·IPC(Main/All)·심판. 출원인 대표명화는 이번 텀 미구현으로 제거). 연동: `/patent-facets` `facets.{countryCode,ipcMain,cpcMain,applicationYear}` — ⚠ **축 불일치 확인 필요**(권리상태·심판은 BFF facets에 없음 / CPC는 프로토타입 패싯에 없음) |
| 공개/등록(특허구분) 패싯 | 문헌종류(`documentKind`)·권리상태 기반 파생 — 등록특허=등록/소멸 문헌, 공개특허=그 외 (연동 시 `documentKind`/`registerStatus`로 판정) |
| IPC 패싯 Main/All | Main=`ipcMain`(`IPCM`) 최신 대표 · All=`ipc`(`IPC`) 개정이력 포함 전체 — 토글로 집계·매칭 필드 전환 |
| 중복제거(등록/공개 우선) | `client` — 결과셋을 출원번호(`applicationNumber`)로 묶어 등록/공개 우선 1건만 유지(문헌번호 기준 검색 특성 대응). 연동 시 서버측 중복제거 옵션으로 정의 가능 |
| CSV | `client`(hits→CSV) |
| 체크 저장 | `client`(라이브러리 — 공통·횡단) |
| 행 선택→오버레이 | `client`(선택 인덱스; 패널 내용은 보류) |

**⑤ 상태/URL**: sort·page·pageSize·facets·selected(선택 문서)가 URL에 반영되어야 함(공통·횡단). **⑥ 예외**: 로딩 스켈레톤 / 0건 안내 / 검색 오류 메시지. **⑦ 접근성**: 표 헤더 스코프, 패싯 체크박스 라벨.

### 특허 · 상세페이지 (`PatentDetail`)

**① 목적**: 선택한 특허 1건의 전체 서지·본문·분류·인용·도면을 정독한다. 목록에서 **행 선택 → 우측 오버레이(미리보기)**, 또는 **새 탭 전체보기**로 진입하며, 두 모드는 동일 본문 구성을 공유한다(오버레이=단일 컬럼, 전체보기=본문+우측 도면 rail).

> ⚠️ **데이터 소스 주의**: 상세페이지 필드는 **`[BLK] 특허 수집 데이터베이스 명세서`의 수집 컬럼에서 도출**해 대조 완료(④ 참조). 즉 **각 필드가 어느 수집 컬럼에서 오는지는 정의돼 있다.** 다만 상세 전용 API가 없어 현재는 검색 hit(`patent`+`columns`)를 재사용하며, **이 수집 컬럼들을 상세로 전달할 API 노출 방식은 미정**(서비스 DB·API 미설계). 따라서 `⚠목업`은 "현재 프로토타입 표시 값이 목업"이라는 뜻이다. 조건부 표시 규칙은 아래 **③″**에 인라인(이 문서만으로 판단 가능). **본 명세는 "UI에 실제 렌더되는 구성"을 기준으로 한다.**

**② 구성요소별 정의** (화면 위계 순 · 표시 → 기능 → 사전조건 → 사후결과 통합)

| ID | 구성요소 | 표시 정보·구성 | 기능·상호작용 | 사전조건 | 사후결과 |
|---|---|---|---|---|---|
| `PAT-DET-010` | 헤더 액션 바 (영역) | 전체보기=로고·이전/다음·위치(n/총)·라이브러리 저장·탭 닫기 / 오버레이=이전/다음·위치·새 탭·저장·닫기 | ↓ 액션별 | 상세 진입 | 개별 액션(011~013) 참조 |
| `└ PAT-DET-011` | 이전/다음 네비 | ◀ ▶ + 위치(n/총) | 클릭(← →) | 목록 맥락 존재(인접 문서) | 이전/다음 문서로 전환 |
| `└ PAT-DET-012` | 라이브러리 저장 | 저장 버튼 | 클릭 | — | 현재 문서를 라이브러리에 저장 |
| `└ PAT-DET-013` | 닫기 / 새 탭 전체보기 | 탭 닫기(전체보기)·닫기(오버레이)·새 탭(오버레이) | 클릭(Esc) | — | 오버레이 닫기(목록 복귀) / 새 탭으로 전체보기 열기 |
| `PAT-DET-020` | 제목 영역 | 상태(평문) · 국가 · 문헌번호 · 발명의 명칭 | — | 상세 진입 | 문헌 식별 정보 표시 |
| `└ PAT-DET-021` | 원문 PDF 다운로드 | 버튼 | 클릭 | — | 원문 PDF 다운로드(`⚠목업` 원문 URL 미정의) |
| `PAT-DET-030` | 키워드 하이라이터 바 | 검색어 키워드 칩(색상) + 이동(↑↓) + 접기 | 검색어 유입 시 표시 · 본문 내 키워드 강조 | 검색에서 진입(검색어 존재) | 본문 키워드 하이라이트(`client`) |
| `PAT-DET-040` | 앵커 탭 바 | 서지·인명·요약·상세설명·청구범위·패밀리·인용·피인용·분류·기타 | 탭 클릭 | 상세 진입 | 해당 섹션으로 스크롤 · 활성 탭 표시 |
| `PAT-DET-050` | 서지사항 | 문헌/출원/공개·공고/등록 번호·일자 · 원출원·국제출원 · 우선권주장일·심사청구일 · 존속만료·권리변동 · 최종처분·청구항수 · 출원구분·번역문 · 도면수·실시권등록일 · 지정국·서열목록 + 우선권 주장 목록 + **타임라인** | 정독(스크롤) | 상세 진입 | 서지 표·우선권·타임라인 표시 |
| `PAT-DET-060` | 인명정보 | 출원인/주소 · 특허고객번호(KR)·식별기호(JP) · 대표출원인 · 발명자/주소 · 대리인 · 심사관 | 정독 | 상세 진입 | 인명 표 표시 |
| `PAT-DET-070` | 요약 | 요약 본문 | 정독 | 상세 진입 | 요약 표시 |
| `PAT-DET-080` | 상세설명 | 기술분야·배경기술·해결하려는 과제·과제의 해결수단·발명의 효과·**도면의 설명**·구체적 내용 | 정독 | 상세 진입 | 상세설명(명세서 하위섹션) 표시 |
| `PAT-DET-090` | 청구범위 (영역) | 청구항(독립/종속 구조) | ↓ 액션별 | 상세 진입 | 청구항 표시 |
| `└ PAT-DET-091` | 독립항/전체청구항 토글 | 독립항 · 전체청구항 버튼 | 클릭 | — | 독립항만 / 전체 청구항 목록 전환 |
| `PAT-DET-100` | 패밀리 정보 (영역) | 국가별 건수 pill + 패밀리 문헌 목록(국가·번호·일자·명칭) | ↓ 국가 탭 | 상세 진입 | 패밀리 표시 |
| `└ PAT-DET-101` | 국가 탭 필터 | 전체 / 국가별 탭 | 클릭 | 패밀리 존재 | 선택 국가 패밀리만 필터 표시 |
| `PAT-DET-110` | 인용·피인용 | 인용·피인용 목록(특허/비특허) + 선행기술문헌 | 정독 | 상세 진입 | 인용/피인용·선행기술 표시 |
| `PAT-DET-120` | 분류코드 | IPC · CPC · 국가고유(FI/F-term/테마·UPC·EPC) | 정독 | 상세 진입 | 분류 표시 |
| `PAT-DET-130` | 기타정보 | 대리인 주소 · 권리변동 이력 · 권리이전 이력 · 행정처리(수발신) · 국가 R&D · 표준특허 · 심판 | 정독 | 상세 진입 | 있는 항목만 조건부 표시 |
| `PAT-DET-140` | 도면 패널 | 대표도면 + 썸네일 목록 + 확대 뷰어 + 도면부호 설명 | 도면 선택·확대 | 도면 존재 | 선택 도면 확대 표시(`⚠목업` 도면 API 미정의) |

**③″ 본문 구성·표시 기준**
- **상태값**: 평문 표시(색 Badge 미사용) — 상태별 값·의미는 「특허 · 결과 목록」 상태 정의와 동일.
- **두 모드 공통 본문**: 오버레이(단일 컬럼, 도면은 본문 내)와 전체보기(본문 + 우측 도면 rail)가 동일 섹션 구성을 공유.
- **조건부 표시 규칙**: 아래 항목은 **해당 조건일 때만** 렌더하고, 없으면 그 섹션/행을 생략한다. (조건은 국가·권리상태·데이터 유무 등 **문헌 특성** 기준. 표시되는 항목은 상단 통합표의 해당 ID에 대응)

  | 항목(섹션) | 표시 조건 |
  |---|---|
  | 우선권 주장 (050) | 우선권 주장이 있는 문헌(통상 US·EP 등 해외 우선권 보유) |
  | 원출원번호 (050) | 분할·변경출원 등 원출원이 있는 문헌 |
  | 국제출원번호·지정국 (050) | PCT 국내단계진입 / EP 등 지정국이 있는 문헌 |
  | 번역문 제출일 (050) | 외국어(비-KR) 문헌 |
  | 특허고객번호 / 출원인 식별기호 (060) | KR=특허고객번호 · JP=출원인 식별기호 |
  | 서열목록 (050) | 서열목록 보유 문헌(바이오 등) |
  | 실시권 등록일 (050·130) | 실시권(라이선스) 등록이 있는 문헌 |
  | 권리변동 이력 · 권리이전 이력 (130) | 권리가 발생·변동한 문헌(등록·소멸 등) |
  | 행정처리(수발신) (130) | 출원서=항상 · 의견제출통지서=공개 상태 외 · 등록결정서=등록/소멸 |
  | 국가 R&D (130) | 국가연구개발 과제 지원 문헌(주로 KR) |
  | 표준특허 (130) | 표준화기구에 선언된 표준특허 문헌 |
  | 심판 (130) | 심판이 계속·존재하는 문헌 |
  | 국가 고유 분류 (120) | JP=FI·F-term·테마 · US=UPC · EP=EPC (KR·CN 없음) |
  | US 관련출원 · US 가출원 (130) | US 문헌 |
  | JP 공보판 · 대리인 구분 (130) | JP 문헌 (공보판: 등록=특허공보(B) / 그 외=공개특허공보(A)) |
  | EP 출원인 정리번호 · 출원/공개 언어 (130) | EP 문헌 |
  | 선행기술문헌 (110) | 인용문헌이 1건 이상인 문헌 |
  | 패밀리 (100) | 항상(1건 이상) |

  > 위 조건은 **문헌 특성(국가·권리상태·데이터 보유)** 기준이며, 실제 서비스에서는 수집 DB 값 유무로 결정된다. (데모 목업의 재현용 규칙은 본 문서 판단에 불필요)

**④ 정보 출처 — 표시 필드 → 수집 DB 컬럼 (`[BLK] 특허 수집 데이터베이스 명세서` 기준)**

> 개발자가 **이 문서만 보고** 각 표시 필드를 **어느 수집 테이블·컬럼에서 불러오는지** 알 수 있도록 필드 단위로 명시한다. (테이블: `bibliographic`=서지 자연키 1행, `custom`=국가고유 스칼라, `KPA_*`=KR 보강, JP/US/EP는 국가별 고유 테이블) `⚠파생`=수집 컬럼 아님(계산/파싱), `⚠미정`=수집·전달 컬럼 확인 필요.

**서지사항 (050)**

| 표시 필드 | 수집 컬럼 |
|---|---|
| 문헌번호 | `bibliographic.literature_number` |
| 문헌일 / 공개·공고일 | `bibliographic.open_date` / `publication_date` |
| 출원번호 / 출원일 | `bibliographic.application_number` / `application_date` |
| 공개·공고번호 | `bibliographic.open_number` / `publication_number` |
| 등록번호 / 등록일 | `bibliographic.register_number` / `register_date` |
| 문헌종류 | `bibliographic.document_kind` |
| 권리상태 | `bibliographic.register_status` (+ `custom.legal_status`) |
| 원출원번호 | `bibliographic.original_application_number` |
| 국제출원번호 | `bibliographic.international_application_number` |
| 우선권주장일 | `priority.priority_application_date` |
| 심사청구일 | `bibliographic.original_examination_request_date` (원출원 기준) |
| 존속기간(예상)만료일 | `⚠파생` — 등록일+20년 계산(수집 컬럼 없음) |
| 권리변동(유무) | `custom.has_ownership_change` |
| 최종처분상태 | `bibliographic.final_disposal` |
| 청구항 수 | `bibliographic.claim_count` (KR 보강 `KPA_BIBLIOGRAPHIC.claim_count`) |
| 출원구분 | `bibliographic.application_flag` |
| 번역문 제출일 | `bibliographic.translation_submit_date` |
| 도면 수 | `KPA_BIBLIOGRAPHIC.drawing_count` |
| 실시권 등록일 | `custom.license_registration_date` |
| 지정국 | `DESIGNATED_COUNTRY.designated_country`(KR) · EP `DSGN.national_name` |
| 서열목록 | `custom.sequence_listing_yn` |
| 우선권 주장(목록) | `priority`(priority_application_country_code · priority_application_number · priority_application_date) · JP `PRIR` |
| 타임라인 | `client`(위 일자 컬럼 조합) |

**인명정보 (060)** — 출처 `related_person`(row별 `classification`으로 구분)

| 표시 필드 | 수집 컬럼 |
|---|---|
| 출원인 / 주소 | `related_person`[APPLICANT] `.name` / `.address` |
| 대표출원인 | `REPRESENTATIVE_APPLICANT.representative_applicant_name` |
| 특허고객번호(KR) / 출원인식별기호(JP) | `REPRESENTATIVE_APPLICANT.patent_customer_number` / `custom(JP).applicant_identifier` |
| 발명자 / 주소 | `related_person`[INVENTOR] `.name` / `.address` |
| 대리인 / 주소 | `related_person`[AGENT] `.name` / `.address` |
| 심사관 | `related_person`[EXAMINER] `.name` (또는 `custom.examiners`) |

**본문 (070 요약 · 080 상세설명 · 090 청구범위)**

| 표시 필드 | 수집 컬럼 |
|---|---|
| 요약 | `abstract.abstract` (JP/CP 영문요약 `e_abstract`) |
| 상세설명(본문) | `specification.specification` (KIPRIS는 전문 PDF URL) |
| 상세설명 하위섹션(기술분야·배경·과제·해결수단·효과·구체적내용) | `⚠파생` — `specification` 파싱(구조 분해 규칙 정의 필요) |
| 청구범위(전체) | `claim.claim` |
| 독립/종속 구조 · 대표청구항 | `⚠파생` 구조 분해 (대표청구항 번호 `KPA_BIBLIOGRAPHIC.representation_claim_number`) |
| 도면의 설명(부호 설명) | `⚠파생` — `specification` 파싱 |

**패밀리 (100)**

| 표시 필드 | 수집 컬럼 |
|---|---|
| 국가별 건수 | `custom.family_document_count` / `family_country_count` |
| 패밀리 문헌(국가·번호·일자·명칭) | `family`(family_country_code · family_literature_number · application_date · invention_title) |

**인용·피인용 (110)**

| 표시 필드 | 수집 컬럼 |
|---|---|
| 인용/피인용(특허) | KR `KPA_CITATION`(citation_literature_number · citation_literature_country_code) · JP/US `CTLTR` |
| 비특허 인용 | US `CTLTR_ETC.other_citations` · `PRIOR_TECHNOLOGY_DOCUMENT.non_patent_reference_text` |
| 선행기술문헌 | `PRIOR_TECHNOLOGY_DOCUMENT`(prior_technology_document_number · prior_technology_document_country) |

**분류코드 (120)**

| 표시 필드 | 수집 컬럼 |
|---|---|
| IPC | `ipc.ipc_number` (KR `KPA_IPC.ipc_code`) |
| CPC | `CPC.cpc_code` (JP/EP는 `custom.cpc_code`) |
| JP FI / F-term / 테마 | `FI.fi_code` / `FTERM.fterm_code` / `TEMA.tema_code` |
| US UPC | `UPC.upc_code` |
| EP EPC | `EPC.epc_code` |

**기타정보 (130)**

| 표시 필드 | 수집 컬럼 |
|---|---|
| 대리인 주소 | `related_person`[AGENT] `.address` |
| 권리변동 이력 | `RIGHT_CHANGE`(change_type · name · change_date) |
| 권리이전 이력 | `RIGHT_TRANSFER`(document_name · change_before_content · change_after_content · registration_date · registration_number) |
| 행정처리(수발신) | `ADMINISTRATIVE_PROCESS`(receipt_send_document_name · receipt_send_date · proc_status) |
| 국가 R&D | `RND`(rnd_task_name · rnd_task_number · rnd_department_name · rnd_managing_institute_name · rnd_duration) |
| 표준특허 | `STANDARD`(standardization_organization · standard_numbers · standard_technology_name · standard_declarants · standard_declaration_date) |
| 심판(유무) | `custom.has_trial` |
| US 관련출원 | `REL_APPL`(registration_number · registration_date · classification · status) |
| US 가출원 | `CUSTOM(US).provisional_application_numbers` |
| JP 공보판 / 대리인 구분 | `CUSTOM(JP).edition` / `agent_category` |
| EP 정리번호 / 출원·공개 언어 | `CUSTOM(EP).applicant_file_reference` / `filing_language` |

**도면 (140) · 헤더/액션**

| 표시 필드 | 수집 컬럼 |
|---|---|
| 도면 이미지 | `patent_image`(scrape_site · key_name → S3 URL) / `custom.drawing_url` |
| 대표도면 | `custom.primary_drawing_url` |
| 원문 PDF(021) | `⚠미정` — 원문 PDF URL 컬럼 확인 필요 |
| 이전/다음·저장·닫기·탭 스크롤·키워드 하이라이트 | `client` |

> **남은 확정 사항**: 위 컬럼은 수집 DB에 존재하나, 이를 **상세페이지로 전달하는 검색/상세 API 노출 방식**(hit `columns` 패스스루 vs 상세 조회 API)은 미설계 → 연동 시 결정. `⚠파생`은 수집 컬럼이 아닌 계산·파싱 산출물.

**⑤ 상태/URL**: 전체보기는 새 탭(문헌 식별자 URL). 오버레이는 목록의 `selected`로 표시(공통·횡단). **⑥ 예외**: 조건부 섹션 데이터 없음 시 생략. **⑦ 접근성**: 앵커 탭 포커스, 도면 확대 뷰어 키보드 조작, 상태값 평문(색 비의존).

## 논문 검색

> **이용 흐름**: 검색어·발행연도 구성 → 검색 실행 → 카드 목록 확인 → (좁히기: 저널/연도 패싯, 정렬) → 카드 선택 시 미리보기(보류) · 다중 선택 저장 · 특허로 교차검색.

### 논문 · 검색어 입력 (`PaperInput`)

**① 목적**: 논문 검색식·필터 구성·실행. 흐름은 특허와 대칭.

**② 구성요소별 정의** (화면 위계 순 · 표시 → 기능 → 사전조건 → 사후결과 통합)

| ID | 구성요소 | 표시 정보·구성 | 기능·상호작용 | 사전조건 | 사후결과 |
|---|---|---|---|---|---|
| `PAP-INP-010` | 발행연도 필터 | 전체/최근1·5·10년 + 연도 from~to (국가 필터 없음) | 프리셋/범위 선택 | 화면 진입 | `yearFrom`/`yearTo`(또는 `years[]`) 반영 |
| `PAP-INP-020` | 검색어 범위 탭 | 제목+초록 / 제목+초록+전문 | 탭 전환 | 화면 진입 | 선두 자유어 적용 범위 변경 |
| `PAP-INP-030` | 검색식 입력창 + 검색/초기화 (영역) | 입력창 + 검색·초기화 버튼 | ↓ 액션별 | — | — |
| `└ PAP-INP-031` | 검색 실행 | 검색 버튼 | 클릭 / ⏎ | 검색식 또는 항목별 필드에 값 1개 이상 | `PaperSearchRequest` 조립·POST → 논문 목록 전환 · 히스토리 추가 |
| `└ PAP-INP-032` | 초기화 | 초기화 버튼 | 클릭 | — | 입력값·필드 구성 복원 |
| `PAP-INP-040` | 항목별 상세검색 | 제목·초록·키워드·전문·저자 등(펼침/접힘) | 필드 값 입력 시 검색식 AND 누적 | 상세검색 펼침·필드 선택됨 | 검색식 AND 누적 · 실행 후 입력값만 비움 |
| `PAP-INP-050` | 검색 히스토리 (영역) | 최근 검색식 목록(특허와 동일 · 공통·횡단) | ↓ 액션별 | 검색 1회 이상 실행됨 | — |
| `└ PAP-INP-051` | 항목 재검색 | 히스토리 항목 행 | 클릭 | 기록 존재 | 선택 검색식으로 재실행 |
| `└ PAP-INP-052` | ★즐겨찾기 | 별 아이콘 | 토글 | 기록 존재 | 즐겨찾기 on/off 저장 |
| `└ PAP-INP-053` | 삭제 | × 버튼 / 전체 삭제 | 클릭 | 기록 존재 | 해당(또는 전체) 기록 제거 |

**④ 정보 출처 (연동 시 참고 — 목업 단계라 확정 스펙 아님)**

| 요소 | 소스 |
|---|---|
| 검색 payload | → `PaperSearchRequest.searchQuery` (범위코드 래핑 + 필드 AND 누적) |
| 발행연도(프리셋/from~to) | → `yearFrom`/`yearTo` (또는 `years[]`) |
| 정렬 | → `orderByQuery` |
| 필드코드 | prior-art-search `paperSearchField.ts`(제목·초록·키워드·전문·저자) |

**⑤ 상태/URL**: q·paperMode·yearFrom·yearTo가 URL에(공통·횡단). **⑦ 접근성**: 칩 `aria-pressed`+✓.

### 논문 · 결과 목록 (`PaperResults`)

**① 목적**: 논문 결과 카드 목록·좁히기·정렬·페이지·선택 저장.

**② 구성요소별 정의** (화면 위계 순 · 표시 → 기능 → 사전조건 → 사후결과 통합)

| ID | 구성요소 | 표시 정보·구성 | 기능·상호작용 | 사전조건 | 사후결과 |
|---|---|---|---|---|---|
| `PAP-LST-010` | 상단 요약바 | 검색식 칩 · 총 건수 · → 특허로 | 교차검색 진입 | 검색 실행 완료 | 총건수 표시 / 교차검색=특허 검색으로 검색어 이관·전환 |
| `PAP-LST-020` | 결과 카드 목록 (영역) | 제목(+대등제목) · 메타라인 · 초록("초록" 라벨) · 새 탭 열기 · 체크박스 (카드 세부 구성은 아래 ③″) | 목록 렌더 / ↓ 액션별 | 검색 실행 완료(응답 수신) | 목록 렌더(`totalCount`·`paper`) |
| `└ PAP-LST-021` | 카드 선택 | 카드 본문 | 클릭 | 카드 존재 | 선택 인덱스 설정 · 미리보기 패널 오픈(**내용은 보류(상세페이지)**) |
| `└ PAP-LST-022` | 체크박스 선택 | 카드별 체크박스 | 체크 | 카드 존재 | 저장 대상 선택 상태 반영(→ 다중 선택 저장) |
| `PAP-LST-030` | 패싯 필터바 | 저널 · 발행연도 | 체크 후 실행 | 결과 1건 이상 · 패싯 로드됨 | 저널/연도 필터 추가해 재검색 → 1페이지로 |
| `PAP-LST-040` | 정렬·perPage·페이지네이션 | 매칭순/발행연도(최신·오래된) · perPage · 페이지 | 변경 시 재요청 | 결과 존재 | `orderByQuery`/`fetchOffset` 갱신 후 재요청 |
| `PAP-LST-050` | CSV 다운로드 | 버튼 | 내보내기 | 결과 1건 이상 | 카드(제목·저자·저널·발행연도) → CSV 다운로드 |
| `PAP-LST-060` | 다중 선택 저장 | 체크박스 + 선택 저장 버튼 | 라이브러리 저장 | 체크 1개 이상 | 선택 논문을 라이브러리에 저장하고 체크 해제 |
| `PAP-LST-070` | 상태 표시(로딩/0건/오류) | 로딩·0건 안내·오류 메시지 | — | 검색 실행됨 | 응답 대기=로딩 · 0건=안내 · 실패=오류 메시지 |

**③″ 목록 항목 구성 (메타데이터 배열 순서·방식·누락 처리)**

- **카드 순서**: ① 체크박스 → ② 제목(하이라이트) + 새 탭 열기 → ③ 메타라인 → ④ 초록(**있을 때만** · 앞에 "초록" 라벨 + 2줄 클램프)
- **메타라인 배열(`paperMetaLine`)**: `저자, [저널명 | 학위수여기관] 권(호), 시작-끝페이지, 발행연도(월)` — **콤마(`, `)로 연결하고 없는 항목은 생략**
  - 출처: `paperType==='thesis'` → 학위수여기관, 그 외 → 저널명
  - 권/호: `volume` 있으면 ` {권}`, `issue` 있으면 `({호})` 부가
  - 페이지: 시작·끝 모두 있으면 `시작-끝`, 시작만 있으면 `시작`
  - 발행: `month` 있으면 `연도. 월`, 없으면 `연도`
- **누락 예외처리**: 각 요소 부재 시 **콤마 목록에서 생략**(빈 자리 없음). 초록 부재 시 **초록 줄 자체를 렌더하지 않음**. (오버레이/상세 패널에서는 부재 시 `-` 표기)
- CSV 컬럼: 제목·저자·저널·발행연도

**④ 정보 출처 (연동 시 참고 — 목업 단계라 확정 스펙 아님)**

| 요소 | 소스 |
|---|---|
| 총 건수 | `PaperSearchResponse.totalCount` |
| 카드 목록 | `hits[].paper` |
| 제목/대등제목 | `paper.title`/`paper.titleEng` |
| 저자 | `paper.authors[]` |
| 저널명 | `paper.journal` |
| 발행연도 | `paper.publicationYear` (발행일 `publicationDate`) |
| 권(호)·시작/끝페이지·발행 월·학위수여기관 | `⚠목업`(Paper 타입 미정의) |
| 초록(+"초록" 라벨) | `paper.abstract` |
| 초록 하이라이트 | `client` |
| 필터/패싯 건수 | `/paper-facets` `facets.{journal, publicationYear}`; 요청 `venues[]`/`years[]` |
| CSV/체크 저장 | `client` |
| 카드 선택→오버레이 | `client`(선택 인덱스; 패널 내용은 보류) |

**⑤ 상태/URL**: sort·page·pageSize·facets·selected(공통·횡단). **⑥ 예외**: 로딩/0건/오류. **⑦ 접근성**: 카드 포커스·초록 라벨.

## 명세서 · 대화형 수정 에이전트 (`SpecEditorView` 챗)

> **범위 주의**: 이 화면은 선행기술**검색**과 별개인 **명세서 작성/수정 모듈**의 대화형 수정 기능이다. 원자료 = 개발노트 `[AXP] 명세서 수정 에이전트 (1) 전체 아키텍쳐 / (2) 의도 라우팅 / 이거저거`. **현재 프로토타입은 이용자 화면(UX) 목업**이며, 실제 에이전트(LangChain Router·의도 분류기)는 연동 시 `④`의 후보로 교체한다.

**① 목적**: 명세서 본문(블록)·청구항·도면의 설명을 **대화로 수정**한다. 유저 지시 → **의도 라우팅** → 의도별 응답(수정 제안/플랜/답변/방향 확인/미지원). 수정은 **블록 단위 제안**으로 유저가 **반영/무시**(레이블·UI는 기존 AI 어시스턴트와 통일).

**② 구성요소별 정의** (화면 위계 순 · 표시 → 기능 → 사전조건 → 사후결과 통합)

| ID | 구성요소 | 표시 정보·구성 | 기능·상호작용 | 사전조건 | 사후결과 |
|---|---|---|---|---|---|
| `SPC-EDT-010` | AI 어시스턴트 패널 (영역) | 우측 챗 패널 — 대화 히스토리 + 입력창. 좌측 본문 블록 선택과 연동 | 단락 선택 후 명령 입력 | 명세서 에디터 진입 | 의도 라우팅 → 의도별 응답 |
| `SPC-EDT-030` | AI 응답 메시지(의도 배지) | **의도 배지**(수정제안·플랜·방향확인·답변·미지원) + 답변 텍스트 | 정독 | 명령 전송됨 | 라우팅된 의도로 렌더 · 히스토리 축적(모든 의도 결과 직렬화) |
| `SPC-EDT-031` | 수정 제안 카드 | **action 배지**(치환/재작성/삽입/삭제) · 대상 블록 · 한줄 요약 · **diff(원문→수정, 색상)** | 정독 | `intent=edit` | 블록 단위 수정 제안 표시 |
| `└ SPC-EDT-032` | 반영 / 무시 | 반영·무시 버튼 / 반영됨·무시됨 상태 | 클릭 | 제안 `status=pending` | 반영=해당 블록 적용(accepted) · 무시=declined · 미선택=pending |
| `SPC-EDT-033` | 방향 선택지(clarify) | 방향 선택 칩 | 칩 클릭 | `intent=clarify`(모호한 지시) | 선택 방향으로 수정 제안/답변 |
| `SPC-EDT-040` | 플랜 진행 | `N/M 단계` · 스텝 목록 · 진행/중단 | 진행(다음 스텝)/중단 | `intent=plan`(다단계 지시) · **청구항 수정(독립→종속)** | 스텝별 수정 제안 생성 · **진행 중 입력 잠금** · 중단 시 정지 |
| `SPC-EDT-050` | 명령 입력창 | 텍스트 입력(Enter 전송) · 선택 단락 수 · 플랜 중 잠금 안내 | 입력·전송 | — | 의도 라우팅 실행 · 플랜 진행 중 비활성 |

**③″ 의도 라우팅 규칙 (목업 · 결정론적)**
- **edit**: 단락 선택 + 수정 지시(바꿔/보완/구체화/추가/삭제…) → 블록별 수정 제안. `action`은 지시로 추론(재작성/삭제/삽입/치환).
- **clarify**: 선택은 있으나 지시가 모호 → 방향 선택 칩.
- **answer**: 선택 없이 질의(검토/추천/요약/설명) 또는 **기능 안내** → 텍스트 답변.
- **plan**: "먼저 A하고 그에 맞춰 B" 등 다단계 → 스텝 분해 후 순차 실행. **청구항 수정은 독립항→종속항 2스텝 고정**.
- **terminate(미지원)**: 외부검색(인터넷/특허/논문)·도면 이미지 생성·기타 → 안내 후 종료.
- 수정 대상은 **본문/청구항/도면의 설명**(M2). 발명정보(InventionContext) 수정·외부검색·도면 이미지는 미지원.

**②′ 어시스턴트 모드 — 작성 플로우 AI 어시스턴트(`GuidePanel`)**

> 명세서 **작성 플로우(분석)** 화면의 AI 어시스턴트도 **동일 에이전트 UX**로 통일한다. 단, **단일 섹션(focus) 대상**이라 다단계 플랜·청구항 파이프라인은 **명세서 에디터**로 안내한다.

| ID | 구성요소 | 표시 정보·구성 | 기능·상호작용 | 사전조건 | 사후결과 |
|---|---|---|---|---|---|
| `SPC-AST-010` | AI 어시스턴트 챗 (영역) | 가이드 패널 우측 챗 — 선택 섹션(focus) 대상 | 섹션 선택 후 명령 입력 | 작성 플로우 진입 | 의도 라우팅 → 의도별 응답 |
| `SPC-AST-030` | AI 응답(의도 배지) | 의도 배지(수정제안·방향확인·답변·미지원) + 답변 | 정독 | 명령 전송됨 | 라우팅 의도로 렌더 |
| `SPC-AST-031` | 수정 제안 카드 | action 배지 · 대상 섹션 · 요약 · diff(원문→수정) | 정독 | 섹션 선택 + edit 의도 | 단일 섹션 수정 제안 |
| `└ SPC-AST-032` | 반영 / 무시 | 반영·무시·다시생성 / 반영됨·무시됨 | 클릭 | 제안 pending | 반영=섹션 적용 · 무시=declined |
| `SPC-AST-033` | 방향 선택지(clarify) | 방향 선택 칩 | 칩 클릭 | clarify 의도(모호) | 선택 방향으로 수정 제안 |

**④ 정보 출처 (연동 시 참고 — 목업 단계라 확정 스펙 아님)**

| 요소 | 소스 |
|---|---|
| 명세서 본문/블록 | `InventionSpecification`(`SpecificationBlock` 단위, 블록 id) — `[JARVIS-PATENT-API] InventionSpecification/SpecificationBlock` |
| 의도 라우팅·지시 보강 | ⚠ 연동 시 **LangChain Router + 의도 분류기**(개발노트 (2)) — 현재 `client` 결정론적 목업(`specAgentMock.ts`) |
| 수정 제안 | `EditProposalMeta`(ref·action·target_desc·summary·source/target·status) — 현재 목업 생성 |
| 플랜 진행 | `PlanProgress`(step·status·instruction) — 현재 `client` |
| 채팅 메시지 | `ChatMessage`(role·content·intent·clarify_options·edit_proposals·picked_option_index) |
| 질의응답 답변 | ⚠ 연동 시 LLM(내부 정보=발명정보/명세서 기반) — 현재 목업 |

**⑤ 상태**: 대화 세션(메시지 히스토리)·제안 status·플랜 진행. **⑥ 예외**: 미지원 요청=terminate 안내 · 플랜 진행 중 입력 잠금. **⑦ 접근성**: 의도 배지 텍스트 라벨(색 비의존), 제안 카드 diff 색+취소선 병행.

## 공통 · 횡단 기능

검색 도메인 전체에 걸치는 기능. 대부분 프런트 상태(`client`)이나, **URL SSOT는 데모의 핵심 규칙이며 연동 시 반드시 정의**해야 한다.

| ID | 기능 | 사전조건 | 사후결과 | 소스 / 비고 |
|---|---|---|---|---|
| `COM-010` | **검색 히스토리** | 검색 1회 이상 실행됨(기록 존재) | 항목 선택 시 해당 검색식으로 재실행 · ★즐겨찾기 토글 저장(최대 100건) | `client`(로컬 store) |
| `COM-020` | **URL 상태(SSOT)** | 검색 실행 or 쿼리 포함 URL 진입 | 상태를 URL(`q·country·state·from·to·mode·paperMode·sort·page·pageSize·selected·facets`)에 직렬화 → 공유·뒤로가기·새로고침 복원 | ⚠ **프로토타입 미구현** — 연동 시 라우팅/직렬화 규칙 정의 필요 |
| `COM-030` | **재검색 재요청** | 동일 검색 조건으로 재실행 | URL 무변경이어도 명시적 invalidate·재요청 | `client` |
| `COM-040` | **라이브러리 저장** | 저장 대상(목록 체크 1개↑) 선택 | 선택 항목을 로컬 라이브러리에 저장하고 체크 해제 | `client`(오버레이·상세 저장 버튼은 보류) |
| `COM-050` | **특허 ↔ 논문 교차검색** | 현재 검색어 존재 | 반대 도메인 검색으로 검색어 이관·화면 전환 | `client`(별도 API 없음) |
| `COM-060` | **CSV 내보내기** | 결과 1건 이상 | 현재 hits → CSV 생성·다운로드 | `client` |
| `COM-070` | **로딩/0건/오류 상태** | 검색 실행됨 | 응답 대기=로딩 · 0건=안내 · 실패=오류 메시지 | `client` + 응답 성공/실패 |
| `COM-080` | **서비스 푸터(단일 규칙 · copykiller 통일)** | 페이지 진입 | **단일 컴포넌트 `SiteFooter`** — `copykiller.com` 푸터와 통일된 리치 구성(㈜무하유 회사정보 / 고객센터·기관문의 / 콘텐츠(SNS) 3단 + 하단 정책링크(개인정보처리방침·이용약관·연락처·FAQ) + `Copyright © MUHAYU Inc. Since 2011`). CK.Patent를 무하유 패밀리 서비스로 식별. **배치 규칙**: ⑴ **각 페이지 스크롤 최하단에 1회**(검색·결과·상세 전체보기·정책·FAQ·홈), ⑵ 오버레이 미리보기 드로어는 제외(미리보기·부모 페이지에 존재), ⑶ 회사·연락처 상세는 '연락처' 페이지에도 게재, ⑷ 정책/연락처/FAQ 링크는 서비스 내 페이지로 연결(새 탭 독립화면에선 앱으로 이동). (구 `DetailFooter` 폐지·일원화) | `client` |
| `COM-081` | **정책·FAQ 콘텐츠 페이지** | 푸터 링크 진입 | 이용약관·개인정보처리방침·연락처(자리표시자) · FAQ(SEO/GEO/AEO 대비 골격). **확정 문구는 muhayu 제공본으로 대체 예정**, FAQ 콘텐츠는 순차 작성(현재 영역만 확보) | `client` · 콘텐츠 출처=muhayu(⚠ 미확정) |

---

# 비기능 요구사항 (NFR) · 서비스화 요건

> 영업/사업 관점에서 요구된 **서비스화(비기능) 요건**. 기능명세(위)와 별개로, 개발·인프라·운영이 챙겨야 할 항목을 요구사항으로 명시한다. **미확정·미착수는 `⚠`**로 표기하며, 착수 시점을 함께 적는다. 본 프로토타입은 목업 단계이므로 대부분 **연동/오픈 시 정의** 대상이다.

| # | 요구사항 | 내용 | 상태 / 착수 |
|---|---|---|---|
| NFR-01 | **이용량 정책** | 검색 건수·횟수 제한 없이 전 사용자 이용 허용 여부(무제한 vs 쿼터·과금) 결정 | ⚠ 정책 미확정 — 오픈 전 결정 |
| NFR-02 | **멀티테넌시·캠퍼스별 게이팅** | 고객사(캠퍼스)별로 특허 서비스 노출/차단(feature gating) 가능해야 함. 테넌트 단위 on/off·권한 모델 | ⚠ 미설계 — 오픈 전 설계 |
| NFR-03 | **인증·계정(SSO)** | 회원가입·로그인을 **카피킬러 캠퍼스 자동로그인(SSO) 사용자 정보**로 진행. 계정·회원정보 소스=Copykiller Campus, 매핑·중복·탈퇴 정합 확인 | ⚠ 연동 필요 (현재 프로토타입은 데모 로그인) |
| NFR-04 | **서비스 기본 요소** | CK.Patent를 독립 서비스로 인지시키는 푸터 + 기본 콘텐츠(이용약관·개인정보처리방침·연락처). → **COM-080/081로 골격 반영**(문구는 muhayu 확정본 대체 예정) | ✅ 골격 반영 / 문구 ⚠ |
| NFR-05 | **체류 지표 연동** | 사용자가 CK.Patent 체류 시 **Copykiller 체류율 지표에 반영**되도록 세션·이벤트 연동 | ⚠ 연동 정의 필요 |
| NFR-06 | **애널리틱스·SEO 메타** | GA4 연동(데이터 확인) · UTM 파라미터 규칙 · 페이지별 Title/Meta Description 최적화 | ⚠ 오픈 전 적용 |
| NFR-07 | **관리자 통계** | 캠퍼스 중간관리자용 통계 기능(이용 현황 등). 오픈 후 **순차 제공** | ⚠ 후순위(오픈 후) |
| NFR-08 | **SEO/GEO/AEO 콘텐츠** | 검색엔진·생성형 AI 노출을 위한 콘텐츠(FAQ 등)를 초기부터 확보. → **FAQ 영역 확보(COM-081)**. ⚠ **주의: 색인/크롤링되려면 로그인 게이트 밖 공개 영역(또는 SSR/사전렌더)이 필요** — 현재 SPA는 로그인 뒤라 색인 불가 | 🔲 영역 확보 / 공개영역·렌더 방식 ⚠ 미정 |

# 부록 (AXP 선행기술검색 · 검색 기능)

## 종합 — 검색 기능 연동 시 정의가 필요한 항목 (상세 제외)

검색어 입력·목록·공통에서 실제 API 연동 시 확정해야 할 항목:

1. **특허 원문 PDF URL** — 목록 원문 버튼(`⚠목업`). `columns` 내 원문키 존재 여부 확인.
2. **특허 권리상태 패싯** — 요구사항엔 있으나 `/patent-facets` 응답에 없음(추가 or 상태는 검색필터 `statuses`로만).
3. **논문 서지 보강**(권/호/페이지/월/논문유형/학위수여기관) — 목록 메타라인에 렌더되나 `Paper` 타입 미정의 → 타입 확장 필요.
4. **URL 상태(SSOT) 규칙** — 데모 핵심 규칙(공통·횡단)이나 프로토타입 미구현. 라우팅·직렬화·복원 정의 필요.
5. **검색어 범위(모드) 라벨·매핑 정합** — 데모(초록/대표청구항, DSC=상세설명)와 프로토타입(요약/독립항, DSC=전문합산) 용어 차이 확정.
6. **명칭·초록 하이라이트** — 클라이언트 계산(BFF에 offset 미노출) — 유지/서버제공 여부 결정.

---

## 보류 — 논문 상세페이지 (서비스 DB 설계 후)

> **특허 상세페이지는 작성 완료** — 「메뉴·화면별 기능명세 > 특허 검색 > 특허 · 상세페이지」(`PAT-DET-*`)에 UI 렌더 기준으로 정의했다(오버레이·전체보기 공용). 단 **필드 소스는 수집 DB 확정 시 정의**(현재 다수 `⚠목업`).
>
> **보류 사유(논문 상세)**: 수집·서비스 DB 설계 전이라 논문 상세의 필드·구조를 확정할 수 없으므로 DB 설계 후 착수한다.

**보류 대상 (2)**: 논문 오버레이 미리보기 · 논문 전체 상세페이지.
- 검색 hit(`paper`+`columns`) 재사용, 별도 상세 API 없음. 목록 측 "선택→패널 오픈"은 논문 결과 목록에 정의돼 있고, **패널·페이지 내용(필드/섹션) 명세만 보류**한다.

**DB 설계 시 정의해야 할 상세 필드·섹션 (후속 작업 대상)**
- 특허(작성 완료): 수집 컬럼과 대조 완료(수집 명세서 기준). 남은 것은 이 컬럼들을 상세로 전달할 **검색/상세 API 노출 방식** 확정뿐(도면·패밀리·인용·서지 보강·주소·국가고유 등).
- 논문(보류): 영문 저널/영문 초록 · 분야 · 학위수여기관 · 인용 포맷(APA/MLA/Chicago/Harvard/BibTeX, 클라이언트 생성) · 관련논문

**보강 원자료 (선택 — 본 문서만으로 판단 가능하며, 아래는 더 깊은 원자료일 뿐 필수 아님)**
- `상세페이지-수집필드-대조.md` — 필드↔수집 컬럼 **행 단위** 전수 대조(본 문서 ④에 요지 인라인)
- `상세페이지-상태값-정의.md` — 상태값 표시 근거(본 문서엔 "평문·색 비의존"으로 인라인)
- `목업-조건표.md` — 데모 시드의 재현용 조건(본 문서 조건은 문헌 특성 기준으로 인라인)

> 위 문서들은 **원자료·검증 기록**이며, 기능 판단에 필요한 내용은 본 기능정의서 본문(② 통합표·③″ 조건·④ 컬럼 매핑)에 이미 인라인돼 있다.

---

## 부록 — 검색 필드코드 카탈로그 (정본: 검색필드 시트)

항목별 상세검색(`PAT-INP-050`)의 **정본 필드 목록**을 옮긴 것이다(코드=검색식 필드코드/인덱스명). UI는 이 정본과 1:1로 유지한다.

- **UI/비고** 열: `체크박스(있음/없음)`=유무 Y/N 필드, `다중선택 칩`=국가/문헌종류/표준화기구 등 enum·multi, `⛔제외`=검색필드 미사용.
- **검색필드 제외(팀 확정)**: `CC`(국가코드) — 국가는 `select_query`(`country_code in (...)`)로 처리, `search_query` 인덱스 미사용 → 상단 국가 메타필터로만. `BT`(배경기술)·`SM`(해결수단) — 상세설명 `DSC`에 포함되어 미도입(정본에도 없음).
- `—` 코드 행(특허/실용·공개/등록·상태정보·심판종류 등)은 검색식 필드코드가 아니라 **메타필터/파생**으로 처리.
- `_ko`/`_en` 번역본은 동일 인덱스라 생략.

**1. 텍스트 — 명칭 / 요약 / 키워드**

| 코드 | 한글 명칭 | 타입 | 적용국가 | 후보값/형식 | UI/비고 |
|---|---|---|---|---|---|
| TI | 발명의 명칭(원문) | string | All | — |  |
| TI | 발명의 명칭(국문) | string | All | — |  |
| TI | 발명의 명칭(영문) | string | All | — |  |
| AB | 요약(원문) | text | All | — |  |
| AB | 요약(국문) | text | All | — |  |
| AB | 요약(영문) | text | All | — |  |
| KWD | 키워드 | multi | KR | 구분자 분리(; 등) |  |

**2. 서지 — 문헌 구분**

| 코드 | 한글 명칭 | 타입 | 적용국가 | 후보값/형식 | UI/비고 |
|---|---|---|---|---|---|
| KC | 문헌종류(kind code) | enum | IN 제외 전국가 | KR/공통: A,A1(공개), B1,B2(등록), U(공개실용), Y1(등록실용) / EP: A1,A2,B1 | 다중선택 칩 |
| — | 특허/실용 구분 | enum | — | 특허, 실용신안 |  |
| — | 공개/등록 구분 | enum | — | 공개, 등록 |  |
| CC | 국가코드 | enum | All | ISO 3166-1 alpha-2 (부록 A) | ⛔제외(국가 메타필터/select_query) |

**3. 청구항 (Claims)**

| 코드 | 한글 명칭 | 타입 | 적용국가 | 후보값/형식 | UI/비고 |
|---|---|---|---|---|---|
| CL | 대표청구항 | text | 전문제공 국가 | — |  |
| CLI | 독립청구항 | multi/text | KR,US,JP,CN,EP,IN | — |  |
| CLA | 전체청구항 | text | 전문제공 국가 | — |  |
| CLA | 전체청구항(국문) | text | 전문제공 국가 | — |  |
| CLA | 전체청구항(영문) | text | 전문제공 국가 | — |  |

**4. 명세서 본문 (Description)**

| 코드 | 한글 명칭 | 타입 | 적용국가 | 후보값/형식 | UI/비고 |
|---|---|---|---|---|---|
| DSC | 발명의 설명 | text | 전문제공 국가 | — |  |
| TF | 기술분야 | text | KR,US,JP,CN,EP | — |  |
| IE | 발명효과 | text | KR,JP,EP | — |  |
| SP | 해결과제 | text | KR,JP | — |  |
| DE | 구체실시방식 | text | CN | — |  |
| DD | 도면의 간단한 설명 | text | KR,US,JP,CN | — |  |

**7. 번호 (출원 / 공개 / 등록 / 공고)**

| 코드 | 한글 명칭 | 타입 | 적용국가 | 후보값/형식 | UI/비고 |
|---|---|---|---|---|---|
| AN | 출원번호 | string | All | 국가별 형식(KR: 10-YYYY-NNNNNNN 등) |  |
| PN | 공개번호/특허번호 | string | All | 국가별 형식 |  |
| RN | 등록번호 | string | KR,US,JP,CN,IN,TW | 국가별 형식 |  |
| FN | 공고번호 | string | KR,JP | 국가별 형식 |  |

**8. 날짜 / 연도**

| 코드 | 한글 명칭 | 타입 | 적용국가 | 후보값/형식 | UI/비고 |
|---|---|---|---|---|---|
| AD | 출원일 | date | All | YYYY-MM-DD |  |
| — | 출원연도 | int | — | YYYY |  |
| PD | 공개일/특허일 | date | All | YYYY-MM-DD |  |
| — | 공개연도 | int | — | YYYY |  |
| RD | 등록일 | date | PCT 제외 전문제공 국가 | YYYY-MM-DD |  |
| — | 등록연도 | int | — | YYYY |  |
| FD | 공고일 | date | KR,JP | YYYY-MM-DD |  |
| — | 검색 기준일/문헌일 | date | — | YYYY-MM-DD |  |

**9. 우선권 (Priority)**

| 코드 | 한글 명칭 | 타입 | 적용국가 | 후보값/형식 | UI/비고 |
|---|---|---|---|---|---|
| PRN | 우선권 번호 | multi/string | All | 국가별 형식 |  |
| PRC | 우선권 국가 | multi/enum | All | ISO alpha-2 (부록 A) | 다중선택 칩 |
| PRD | 우선권 주장일 | multi/date | All | YYYY-MM-DD |  |

**10. 국제출원 (PCT)**

| 코드 | 한글 명칭 | 타입 | 적용국가 | 후보값/형식 | UI/비고 |
|---|---|---|---|---|---|
| IPN | 국제공개번호 | string | PCT 등 전문제공 국가 | WO/YYYY/NNNNNN |  |
| IPD | 국제공개일 | date | PCT 등 전문제공 국가 | YYYY-MM-DD |  |
| IAN | 국제출원번호 | string | PCT 등 전문제공 국가 | PCT/CCYYYY/NNNNNN |  |
| IAD | 국제출원일 | date | PCT 등 전문제공 국가 | YYYY-MM-DD |  |
| DC | 지정국 | multi/enum | KR,JP,PCT | ISO alpha-2 (부록 A) | 다중선택 칩 |

**11. 출원인 / 발명자 / 대리인 / 심사관**

| 코드 | 한글 명칭 | 타입 | 적용국가 | 후보값/형식 | UI/비고 |
|---|---|---|---|---|---|
| WAP | 대표출원인 코드 | string | All |  |  |
| AP | 출원인(국문) | multi/string | All | — |  |
| AP | 출원인(영문) | multi/string | All | — |  |
| APC | 출원인 국적 | multi/enum | All | ISO alpha-2 (부록 A) | 다중선택 칩 |
| APD | 출원인 주소(국문) | string | KR | — |  |
| APD | 출원인 주소(영문) | string | KR | — |  |
| INV | 발명자(국문) | multi/string | All | — |  |
| INV | 발명자(영문) | multi/string | All | — |  |
| INVC | 발명자 국적 | multi/enum | CN,IN,AU 제외 전문제공 국가 | ISO alpha-2 (부록 A) | 다중선택 칩 |
| AG | 대리인(국문) | multi/string | 일부 제외 전문제공 국가 | — |  |
| AG | 대리인(영문) | multi/string | 일부 제외 전문제공 국가 | — |  |
| AGD | 대리인 주소 | string | — | — |  |
| EXN | 심사관 | multi/string | KR,US,JP,CN | — |  |
| AC | 출원인 식별기호 | string | JP | — |  |
| PCN | 특허고객번호 | string | KR | — |  |

**12. 분류코드 (Classification)**

| 코드 | 한글 명칭 | 타입 | 적용국가 | 후보값/형식 | UI/비고 |
|---|---|---|---|---|---|
| IPCM | IPC (Main) | string | All | Snn Cnn/nn (예: H04L 9/32) |  |
| IPC | IPC (All) | multi | All | IPC 형식 |  |
| CPCM | CPC (Main) | string | 전문제공 국가 | CPC 형식 |  |
| CPC | CPC (All) | multi | 전문제공 국가 | CPC 형식 |  |
| UCM | US Class (Main) | string | US | USPC 형식 |  |
| UC | US Class (All) | multi | US | USPC 형식 |  |
| FI | FI | multi | JP | JP FI 형식 |  |
| FTC | F-term | multi | JP | JP F-term 형식 |  |

**13. 법적상태 / 권리 / 권리이전**

| 코드 | 한글 명칭 | 타입 | 적용국가 | 후보값/형식 | UI/비고 |
|---|---|---|---|---|---|
| — | 상태정보 | enum | KR,US,JP,CN,EP,CA,AU | 이벤트 단위 granular 코드 — KR 실값 예: 특허출원,출원공고,등록결정,등록료납부,연차료납부,연차료미납,특허거절결정,존속기간만료,포기서제출 등. 전체 통제어휘 = KR LEGALSTATUS_CODE / CN PRSCODE 사전 |  |
| CAP | 현재권리자 | multi/string | KR,US,JP,CN,CA,AU | — |  |
| CAC | 현재권리자 국적 | multi/enum | — | ISO alpha-2 (부록 A) | 다중선택 칩 |
| ASNO | 양도인 | multi/string | KR,US,CN | — |  |
| ASNE | 양수인 | multi/string | KR,US,CN | — |  |
| CAD | 최근 양도일 | date | KR,US,CN | YYYY-MM-DD |  |
| ASY | 양도 유무 | bool(Y/N) | — | Y, N | 체크박스(있음/없음) |
| EL | 실시권자(전용) | multi/string | KR | — |  |
| LRD | 실시권/라이선스 등록일 | date | KR,CN | YYYY-MM-DD |  |
| LY | 실시권 유무 | bool(Y/N) | — | Y, N | 체크박스 |
| PLGE | 질권자 | multi/string | CN | — |  |
| JIC | 심판/소송 유무 | bool(Y/N) | KR,US,JP,EP | Y, N | 체크박스 |
| — | 심판/소송 종류 | enum | — | KR 실값: 거절결정불복, 취소, 무효, 권리범위확인(적극적), 권리범위확인(소극적), 정정, 보정각하불복, 취소결정불복, 정정무효 |  |

**14. 인용 (Citation)**

| 코드 | 한글 명칭 | 타입 | 적용국가 | 후보값/형식 | UI/비고 |
|---|---|---|---|---|---|
| BCC | 특허인용 국가 | multi/enum | — | ISO alpha-2 (부록 A) | 다중선택 칩 |
| BCN | 특허인용 번호 | multi/string | — | 번호 형식 |  |
| FCC | 특허피인용 국가 | multi/enum | — | ISO alpha-2 (부록 A) | 다중선택 칩 |
| FCN | 특허피인용 번호 | multi/string | — | 번호 형식 |  |
| NPCY | 비특허인용 유무 | bool(Y/N) | — | Y, N | 체크박스 |
| NPCT | 비특허인용 명칭+저자 | multi/text | — | — |  |

**15. R&D 과제 (국가연구개발)**

| 코드 | 한글 명칭 | 타입 | 적용국가 | 후보값/형식 | UI/비고 |
|---|---|---|---|---|---|
| NRTBT | 과제/연구사업 명칭 | string | KR | — |  |
| NRTDO | 부처/주관기관 명칭 | multi/string | KR | — |  |
| NRTN | 과제 고유번호 | string | KR | — |  |
| NRDS | 연구시작일 | date | — | YYYY-MM-DD |  |
| NRDE | 연구종료일 | date | — | YYYY-MM-DD |  |

**16. 표준특허 (Standard Essential Patent)**

| 코드 | 한글 명칭 | 타입 | 적용국가 | 후보값/형식 | UI/비고 |
|---|---|---|---|---|---|
| SEYN | 표준특허 유무 | bool(Y/N) | 전문제공 국가 | Y, N | 체크박스 |
| SEI | 표준정보 | string | 전문제공 국가 | - |  |
| SET | 표준기술명 | string | — | — |  |
| SESO | 표준화기구 | enum | 전문제공 국가 | ETSI, ISO, IEC, ITU, IEEE, 3GPP, JEDEC 등 | 다중선택 칩 |
| SEN | 표준번호 | multi/string | 전문제공 국가 | — |  |
| SED | 선언(등재)자 | multi/string | 전문제공 국가 | — |  |
| SEDC | 선언(등재)자 국적 | multi/enum | 전문제공 국가 | ISO alpha-2 (부록 A) | 다중선택 칩 |
| SEDD | 선언일 | date | 전문제공 국가 | YYYY-MM-DD |  |

**17. 서열 (Sequence Listing)**

| 코드 | 한글 명칭 | 타입 | 적용국가 | 후보값/형식 | UI/비고 |
|---|---|---|---|---|---|
| SEQY | 서열목록 유무 | bool(Y/N) | — | Y, N | 체크박스 |
| SEQC | 서열내용 | text | — | — |  |
