# 레이블 워싱 · UI 틀어짐 · 삭제 확인 — 수정 위치 확인 (2026-08-28)

> 기준: 커밋 `944a2d0`, localhost:5173 전 단계 순회(새 작업 → 에디터) + 코드 문구 인벤토리(`SpecView.tsx`·`SpecEditorView.tsx`·`Sidebar.tsx`·`PreviewModal.tsx`)
> 상태: **위치 확인만. 수정 미착수.** 각 항목에 통일안(제안)을 함께 적었으니 확정해 주시면 일괄 반영합니다.

---

## A. 레이블 워싱 — 화면 간 불일치 · 모호한 표현

| # | 현재(위치) | 문제 | 통일안 |
|---|---|---|---|
| A1 | **채택 / 선택 / 사용** — 발명 설명 "채택 4/4"(체크 title 채택) · 구성요소 체크 title **"선택 해제/선택"**([SpecView.tsx:2168](../src/views/SpecView.tsx#L2168)) · 종속항 "(N개 채택)" · 이미지 선별 "선택 2개"(체크 라벨 "선택") · 명세서 도면 "도면 채택 / 참고만" · 요약 패널 "관련 이미지 2/2개 선택" | 같은 동작(항목을 결과물에 포함)이 화면마다 다른 단어 | **항목·구성요소·종속항·도면 = "채택"**, 이미지(맥락 사용) = "선택" 유지. 구성요소 title → "채택/채택 해제" |
| A2 | **다시 ~ / 재~** — "다시 선택"(완료 행) · **"← 세트 재선택"**([:3054](../src/views/SpecView.tsx#L3054)) · "↻ 후보 다시 생성" · "↻ 다시 분석"(구성요소) · **"재생성"**(종속항 그룹, 텍스트 링크 [:3364](../src/views/SpecView.tsx#L3364)) · "↻ 중간명세서 다시 생성" · 에디터 **"↺ 다시 생성"** · 안내문 "재생성됩니다"([:3303](../src/views/SpecView.tsx#L3303)) vs "다시 생성됩니다"([:3043](../src/views/SpecView.tsx#L3043)) | 한자어/우리말 혼용, 아이콘 ↻/↺ 혼용 | 동사는 **"다시 ~"** 로 통일(재선택→다시 선택, 재생성→다시 생성), 아이콘은 **↻** 하나 |
| A3 | **제목·요약 / 발명의 명칭 / 개요 / 요약** — 스텝바 "제목·요약" · 본문 제목 "발명의 명칭" · 카드 필드 "개요" · 요약 패널 "발명의 명칭" · 에디터 섹션 **"요약"** · 미리보기 【요약】 | 같은 텍스트가 개요↔요약, 단계명은 제목 | 스텝 **"명칭·요약"**, 카드 필드 **"요약"**(개요 → 요약), 직접 입력 안내도 "요약" |
| A4 | **이미지 / 도면 / 도 N** — 3단계 "이미지 선별" 카드 배지 **"도면1·도면2"**(`detail.symbol`, [:2651](../src/views/SpecView.tsx#L2651)) · 6단계 "도 1" + "도면1" 병기([:2768](../src/views/SpecView.tsx#L2768)) · 에디터 도면 카드 "도면1" · 도면 설명 "도 1은" | 채택 전에는 '이미지', 채택 후 '도 N'이어야 하는데 원본 기호 "도면N"이 계속 노출 | 3단계 배지 **"이미지 1/2"**, 6단계·에디터는 **"도 N"만**(원본 기호는 툴팁) |
| A5 | **항목 / 카드 / 블록 / 단락** — 발명 설명 "항목"(채택 N, 항목 내용...) · AI 패널 안내 "각 **카드**의 AI 수정" · 중간명세서 **"블록"**("(N개 블록)", "블록 추가...") · 에디터 **"단락"**("+ 단락 추가", "단락 삭제") · 제안 카드 "'기술분야' 2번째 블록" | 같은 문단 단위가 3개 이름 | 위저드 항목 단위 = **"항목"**, 명세서 본문 문단 = **"단락"**(블록 → 단락 전면 교체) |
| A6 | **에디터 / 편집기 / 수정모드** — "명세서 에디터"(AI_NEXT) · 사이드바 "에디터 편집 중" · 도면 카드 **"수정모드 ↗"**(에디터 [1360](../src/views/SpecEditorView.tsx#L1360)) · 도면 카드 "범위 조정 · CAD 변환" · 코드 주석 "편집기" | 도면 도구 이름이 없음 | 명세서 = **"에디터"**, 도면 도구 = **"도면 편집기"** → "수정모드 ↗" → "도면 편집기 ↗", 위저드 버튼 "범위 조정 · CAD 변환" → "도면 편집기에서 조정·변환 ↗" |
| A7 | **확정 / 완료** — 요약 "구성요소 4개 확정", 카드 하단 **"구성요소 확정 완료"**, 사이드바 "분석 완료", 하단 "청구항 확정 →" | '확정 완료'는 중복 | 단계 마무리 = **"확정"**, 전체 8단계 끝 = **"분석 완료"** |
| A8 | **적용/취소 vs 반영/무시** — 위저드 제안 카드 "[취소] [적용]" · 에디터 제안 카드 **"✓ 반영 / ✕ 무시"** · 용어 교체 "반영/무시" · 토스트 "적용되었습니다/반영되었습니다" | 같은 수락/거절 동작에 2쌍 | **"적용 / 무시"**(거절은 '취소'와 구분되게 무시), 토스트 "적용했습니다" |
| A9 | **진행 표기 분모** — 사이드바 배지 **"7/8 청구항"**(업로드 포함 8) · AI 패널 **"확정 6 / 7"**(확정 가능 7) · 스텝바 ①~⑧ | 같은 진행 상태가 7과 8로 | 스텝바 기준 **8**로 통일: 배지 "7/8 청구항", 패널 "6/8 단계 확정"(업로드는 확정 1로 계산) |
| A10 | **구성 요소 / 구성요소** — placeholder "**구성 요소** 전반에 대한 AI 지시사항"([:2114](../src/views/SpecView.tsx#L2114)), "**구성 요소**의 결합 관계"([:1589](../src/views/SpecView.tsx#L1589)) vs 그 외 "구성요소" | 띄어쓰기 불일치 | **"구성요소"** |
| A11 | **명세서 생성 / 명세서 초안 편집으로 이동 / 작성 단계로** — 중간명세서 하단 "명세서 생성 →", 완료 화면 "명세서 초안 편집으로 이동 →"([:~760](../src/views/SpecView.tsx#L760)), 에디터 "← 작성 단계로" | 같은 목적지(에디터)가 두 이름 | "명세서 생성 →"(최초) / **"에디터로 이동 →"**(재진입), 에디터 뒤로가기 "← 작성 단계로" 유지 |
| A12 | **삭제 표기** — 이미지 카드 **"삭제"** 텍스트 · 그 외 전부 **"✕"** 아이콘(항목·슬롯·종속항·블록) | 같은 동작 2표기 | 카드 내부는 ✕ 아이콘(title "삭제") 통일, 이미지도 ✕ |
| A13 | **종속항 그룹 헤더** — "종속항 (4개 채택) 재생성" 한 줄 | '재생성'이 라벨처럼 붙어 보임 | "종속항 · 채택 4" + 우측 "↻ 다시 생성" 버튼 |
| A14 | **AI 패널 제목/안내** — 위저드 "AI 어시스턴트 / 지금까지 확정된 내용" · 에디터 "AI 어시스턴트 / 중앙에서 단락을 선택하고…" · 위저드 입력 "단계 진행·작성 방법을 질문하세요..." · 에디터 입력 "명령을 입력하세요... (Enter 전송)" | 같은 패널이 위저드에선 Q&A, 에디터에선 명령 — 이름은 동일 | 위저드 **"작성 도우미"**, 에디터 **"AI 수정"** 으로 역할이 드러나게 (또는 부제 추가) |
| A15 | **"AI 추출 구성요소"** 헤더 vs 단계명 "구성요소" vs 요약 "구성요소 4개 확정" | 'AI 추출'은 사용자 추가분 포함 후 부정확 | **"구성요소 목록"** |

## B. UI 틀어짐 — 버튼 · 라디오 · 체크박스

| # | 위치 | 실측 | 수정안 |
|---|---|---|---|
| B1 | **체크박스 크기 불일치** — 발명 설명 **20px**(w-5) · 구성요소 **16px**(w-4, [:2160](../src/views/SpecView.tsx#L2160)) · 종속항 **16px**(w-4, [:3383](../src/views/SpecView.tsx#L3383)) · 이미지 **18px** · 에디터 단락 **16px**(w-4, [1421](../src/views/SpecEditorView.tsx#L1421)) · 에디터 청구항 체크 없음 | 같은 의미의 컨트롤이 4가지 크기 | **20px(w-5) + 아이콘 10** 통일, 공통 `AdoptCheckbox` 컴포넌트 |
| B2 | **라디오 크기/정렬** — 제목 카드 16px(w-4, `mt-0.5`) · 이미지 "대표" 16px · 세트 카드 라디오 | 16px는 24px 타깃 미달 | 18px + 클릭 영역 24px, 텍스트와 `items-center` |
| B3 | **이미지 카드 상단 배지 높이** — "↗" 20px vs "선택/대표" 23px, 세로 중심 3px 차(실측 rowHeightMismatch) | 배지 3개가 미세하게 어긋남 | 세 배지 `h-6` 통일, 같은 padding |
| B4 | **구성요소 카드 헤더** — 체크 16px · 핸들 ⠿(text-xs) · 부호 칩 · AI 수정 24px · 화살표 24px | 크기 혼재로 행이 울퉁불퉁 | 체크 20px, 핸들 아이콘 11px 버튼 24px |
| B5 | **에디터 툴바** — "← 작성 단계로" **39px**(h-full) vs 나머지 **26px** | 첫 버튼만 높이 다름 | 전부 `h-7`(28px) |
| B6 | **완료 요약 행** — "펼치기"(텍스트 버튼) vs "다시 선택"(outlined 28px) | 같은 행에 스타일 2종 | 둘 다 outlined 소형 or 둘 다 텍스트 |
| B7 | **종속항 카드** — textarea `rows≥2`로 1줄 문장에도 ~110px | 하단 여백 과다(발명 설명은 B3에서 해결) | auto-height(rows=1 + scrollHeight) |
| B8 | **구성요소 툴바 3종 스타일** — "↻ 다시 분석"(outline gray) · "AI 추가"(brand outline) · "→ 부호 자동 부여"(light filled) | 동급 동작에 3스타일 | 전부 brand outline 소형, 주요 동작(부호 부여)만 filled |
| B9 | **종속항 그룹 "재생성"** 텍스트 링크 vs 다른 곳의 버튼 | 클릭 가능 여부 불명 | outline 소형 버튼 "↻ 다시 생성" |
| B10 | **이미지 카드 "삭제"** 텍스트 우하단 vs 다른 카드 ✕ 아이콘 우상단 | 위치·형태 다름 | ✕ 아이콘 28px, 헤더 우측 |
| B11 | **발명 설명 표 카드** — AI 수정 버튼이 없어 "종래기술로 →"가 왼콽으로 이동, 열 정렬 흔들림 | 카드마다 버튼 x 위치 다름 | 표 카드에도 같은 폭의 자리(placeholder) 확보 또는 버튼을 `ml-auto` 고정 |
| B12 | **사이드바 배지** — 작업명 행 우측 배지로 이름이 12자에서 잘림("카피킬러 참고...") | 배지가 이름 공간을 먹음 | 배지를 2행(메타 행) 우측 끝으로, 이름은 1행 전체 |
| B13 | **AI 수정 버튼 x 정렬** — 제목 카드는 `items-start`로 상단, 발명 설명·구성요소는 `items-center` | 카드 종류별 버튼 세로 위치 다름 | 헤더 행 `items-center` 통일(제목이 2줄이면 첫 줄 기준 `self-start`) |
| B14 | **하단 바 좌우 여백** — "← 이전"과 "다음 →"이 콘텐츠 열(max-w-5xl)에 정렬되지만 좌우 padding(px-6)이 본문(px-4)과 다름 | 2px 어긋남 | padding 통일 |

## C. 삭제 확인 얼럿 누락 (사용자 지적 — 전반)

| 위치 | 현재 | 제안 |
|---|---|---|
| 발명 설명 — 직접 추가 항목 ✕ ([SpecView.tsx:1518](../src/views/SpecView.tsx#L1518)) | 즉시 삭제 | 내용이 있으면 확인 |
| 청구항 구성 슬롯 ✕ ([:3115](../src/views/SpecView.tsx#L3115)) | 즉시 삭제(최소 2개 제한만) | 방향·강조점 입력이 있으면 확인, 없으면 즉시 |
| 종속항 ✕ ([:3395](../src/views/SpecView.tsx#L3395)) | 즉시 삭제 | 확인("종속항 N을 삭제할까요? 채택 해제로 대신할 수 있습니다") |
| 중간명세서 블록 ✕ ([:3636](../src/views/SpecView.tsx#L3636)) | 즉시 삭제 | 확인 |
| 에디터 단락 삭제 ([SpecEditorView.tsx:1474](../src/views/SpecEditorView.tsx#L1474)) | 즉시 삭제(실행 취소 가능) | 확인 대신 **토스트 + "실행 취소"** 버튼(되돌리기 가능하므로) |
| 에디터 청구항 삭제 ([:306](../src/views/SpecEditorView.tsx#L306)) | 인용 종속항 있을 때만 확인 | 단독 삭제도 확인(또는 토스트+실행 취소) |
| 이미지 삭제 ([SpecView.tsx:2673](../src/views/SpecView.tsx#L2673)) | **확인 있음** ✔ | 유지 |
| 구성요소 | 삭제 없음(채택 해제만) | 유지 |

**정책 제안**: (1) 실행 취소가 있는 곳(에디터)은 토스트 + 실행 취소, (2) 없는 곳은 사용자가 입력·편집한 내용이 있을 때 확인 다이얼로그, (3) AI 생성 항목은 '삭제' 대신 '채택 해제'를 기본 동작으로.

---

## D. 반영 순서 제안
1. **C 삭제 확인**(전반, 로직) → 2. **B1·B2·B4 컨트롤 크기 통일**(공통 컴포넌트) → 3. **A1·A2·A5·A8·A10·A12 용어 일괄 치환** → 4. A3·A4·A6·A9·A14 (단계명·기호·패널명은 기능정의서 용어와 함께 확정) → 5. 나머지 B.
