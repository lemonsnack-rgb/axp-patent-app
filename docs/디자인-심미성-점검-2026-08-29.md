# 디자인 심미성 점검 — 명세서 작성 워크플로우 (2026-08-29)

> 범위: 새 명세서 폼 · 위저드(8단계) · AI 어시스턴트 패널 · 에디터. 실측 환경: localhost:5173, 뷰포트 982×1541(노트북 분할 화면) 및 2000×1000(이전 세션). **도출만 — 수정은 하지 않음.** 우선순위: P1 = 첫인상·가독성에 직접 영향, P2 = 일관성·밀도, P3 = 마감.

## 0. 현황 수치 (코드 기준)

| 항목 | 값 | 판단 |
|---|---|---|
| 본문 폰트 사용 분포 (SpecView+Editor) | `text-xs2`(12px) **251회** · `sm2`(13px) 38 · `base2`(14px) 19 · `md2` 2 · `lg2` 1 · `text-sm/xs/base/xl` 잔존 19 | 12px가 사실상 본문 크기 — 변리사·대학원생이 장문(청구항·단락)을 읽는 화면에 과소 |
| 모서리 반경 | `rounded-lg` 79 · `rounded-md` 78 · `rounded-full` 45 · `rounded-xl` 31 · `2xl` 3 | 같은 위계(카드/버튼)에 md·lg·xl 혼재 |
| 색 토큰 | brand(#3B8EF5) 계열 + `blue-*`, `amber-*`, `green-*`, `zinc/gray/neutral` 3종 회색 | 회색 계열 3종 혼용, blue와 brand 병존 |
| AI 아바타 | `linear-gradient(135deg,#7c3aed,#1d4ed8)` (보라→파랑) | brand 팔레트 밖의 유일한 그라디언트 — 브랜드 규칙 위반 |

## 1. 타이포그래피

| # | 우선순위 | 위치 | 문제 | 수정안 |
|---|---|---|---|---|
| T1 | **P1** | 위저드 항목 본문·청구항 본문·중간명세서 단락·에디터 단락 | 본문이 12~13px(`xs2/sm2`). 청구항 한 항이 4~6줄인데 12px로 읽힘. 독립항·종속항 문장 가독성이 가장 중요한 화면에서 가장 작은 글자 | **읽는 텍스트 = 14px(`base2`), 조작 레이블·메타 = 12~13px**로 역할 기준 2단 스케일 고정. 에디터 단락·청구항·중간명세서 단락·발명 설명 본문에 `base2` 적용, 줄 간격 1.65 |
| T2 | P2 | 섹션 제목(발명의 설명·청구항·에디터 h2) | 제목 14px 굵게 + 본문 12px → 위계 차 2px로 제목이 "굵은 본문"처럼 보임 | 화면 제목 18px/700, 섹션 제목 16px/600, 카드 제목 14px/600 3단 |
| T3 | P2 | 안내 문장(STEP_HINT, 파란 안내 박스, 에디터 힌트) | 회색 12px 장문이 카드 사이에 3종(AiMsg 좌측 파란 바 · 파란 배경 안내 · 회색 캡션)으로 등장 — 시각적 소음 | 단계 안내는 **한 형식**(좌측 바 AiMsg)만 쓰고, 파란 배경 박스는 "생성 전 빈 상태" 전용으로 한정 |
| T4 | P3 | 배지 텍스트 `text-[7px]`, `text-[8px]`(AI 미니 아바타) | 8px 이하 글자는 렌더 흐림 | 글자 대신 아이콘(✈) 단독 사용 |
| T5 | P3 | 숫자(부호 100/200, 청구항 번호, N/8) | 비례 숫자 → 열 정렬 흔들림 | `tabular-nums` |

## 2. 색상

| # | 우선순위 | 위치 | 문제 | 수정안 |
|---|---|---|---|---|
| C1 | **P1** | AI 아바타(패널 헤더·메시지·모바일 FAB) | 보라→파랑 그라디언트가 brand 블루와 충돌. 화면에서 유일한 보라 | brand-400 단색 + 흰 ✈ 아이콘(이미 `AiIcon` 존재) |
| C2 | **P1** | 발명 설명 카드 | 채택 카드 전부 **파란 테두리 2px** → 기본 상태가 "선택됨"처럼 강조돼 진짜 선택/편집 상태와 구분 안 됨(스크린샷: 5장 모두 파란 테두리) | 기본 = `border-zinc-200`, 채택 해제 = 흐림, 편집 중/선택 = brand 테두리. 테두리는 상태 표현용으로만 |
| C3 | P2 | 회색 3종(`zinc`, `gray`, `neutral`) | 같은 화면에 색온도 다른 회색 혼용 → 미세한 얼룩 | **`neutral` 1종**으로 통일(텍스트 700/500/400, 선 200, 배경 50) |
| C4 | P2 | `blue-*` vs `brand-*` | 청구항 `독립항` 배지·탭 활성·링크가 `blue-600`, 버튼은 `brand-400` — 두 파랑 | 의미색 규칙: **행동/선택 = brand**, 정보 배지 = neutral 또는 카테고리색. `blue-*` 제거 |
| C5 | P2 | 종속항 `amber` 배지 + `종래기술로 →` amber 버튼 + 보완 필요 amber | amber가 "종속항", "이동 액션", "경고" 3역할 | amber = 경고 전용. 종속항 배지는 neutral, 이동 버튼은 ghost(회색 텍스트 + 화살표) |
| C6 | P3 | 확정 단계 요약 행 초록 배경 + 초록 테두리 + 초록 아이콘 | 5행 연속이면 초록 덩어리 | 배경 흰색, 좌측 초록 ✓ 아이콘만 + 얇은 선 |

## 3. 레이아웃 · 밀도

| # | 우선순위 | 위치 | 문제 | 수정안 |
|---|---|---|---|---|
| L1 | **P1** | 스텝바(982px 뷰포트) | 8칩이 가로 스크롤 + 스크롤바 노출, `1 업로드`가 잘려 "이미지 선별"부터 보임. 현재 단계가 어디인지 첫눈에 안 잡힘 | 1280px 미만에서 **완료 단계는 번호 원만**(라벨 숨김), 현재·다음 단계만 라벨. 스크롤바 숨김(`scrollbar-none`) + 현재 칩 자동 센터링 |
| L2 | **P1** | 에디터 툴바(982px) | `미리보기`가 잘리고 DOCX·PDF가 화면 밖 → 내보내기 진입 불가 | 우측 그룹을 `⋯ 더보기` 또는 `내보내기 ▾` 하나로 접기(≤1280px). 좌측 도구는 아이콘만 |
| L3 | **P1** | 에디터 우측 AI 패널(360px 고정) | 982px에서 본문 폭 ~330px, 청구항이 두 단어씩 줄바꿈. 패널이 본문보다 넓어 보임 | 패널 폭 `clamp(280px, 28vw, 360px)` + 접기 토글(위저드 패널처럼 리사이즈 핸들 제공). 1024px 미만은 기본 접힘 |
| L4 | P2 | 에디터 패널 「선택 단락」 영역 | 단락 1개 선택 시 미리보기가 `flex-1`로 전체 높이를 차지 → 대화 영역이 화면 맨 아래 제목 바 하나로 눌림(스크린샷). 정보 영역이 대화를 밀어냄 | 선택 단락 미리보기 `max-h: 40vh` 고정, 나머지는 대화. 대화가 비어 있으면 "명령 예시 3개" 칩으로 빈 상태 채움 |
| L5 | P2 | 위저드 본문 폭 | 1080px 이상에서 카드 폭 `max-w-3xl`(768px) 내에 좌우 여백 균등. 2000px에선 좌측 정렬 고정으로 우측이 빈다(이전 세션 스크린샷) | 본문 컬럼 중앙 정렬 + 최대 840px, 좌우 여백은 패널 폭과 비례 |
| L6 | P2 | 발명 설명 카드 헤더 | 체크 · 핸들 · 라벨 · ▲▼ · AI 수정 · 종래기술로 → 6개 컨트롤이 한 줄 → 라벨(목적/구성)이 컨트롤 사이에 끼어 제목 역할 못 함 | 헤더 = 체크 + 라벨(굵게) 좌측, 컨트롤은 **호버 시 우측**에만(종속항 행과 같은 규칙). 이동/AI는 `⋯` 메뉴로 묶는 것도 가능 |
| L7 | P2 | 하단 액션 바 | `← 이전`이 좌측 끝, 주 동작이 우측 끝 — 982px에서 두 버튼 사이 600px 공백. 바 자체가 흰색이라 본문과 경계 약함 | 두 버튼을 우측 정렬로 모으고(이전 = ghost), 바 배경 `neutral-50` + 상단 선 + 은은한 그림자 |
| L8 | P3 | 카드 간격 | 카드 간 12px, 카드 내부 패딩 14px, 섹션 간 24px — 카드 내부·외부 여백 차이가 작아 카드 경계가 약함 | 카드 내부 16px / 카드 간 8px / 섹션 간 32px로 "안 > 밖" 대비 |
| L9 | P3 | 새 명세서 폼 | 폼 카드 아래 큰 공백 후 짙은 남색 푸터 — 첫 화면에서 푸터 비중이 절반 | 폼 아래 "최근 작업 3개" 또는 "진행 방식 3단계" 요약을 두어 여백을 콘텐츠로 채움(푸터는 유지) |

## 4. 컴포넌트 일관성

| # | 우선순위 | 위치 | 문제 | 수정안 |
|---|---|---|---|---|
| K1 | **P1** | 버튼 형태 | 같은 화면에 5종 — filled(brand) / outlined brand / outlined amber / ghost 텍스트 / 점선(단락 추가·이미지 추가). 반경도 `md·lg·xl` 혼재 | **4종으로 고정**: Primary(filled brand, xl) · Secondary(outlined neutral, lg) · Ghost(텍스트, md) · Danger(텍스트 red, 호버 시만). 점선 = "추가" 전용 1종. 반경은 버튼 lg, 카드 xl, 배지 full로 위계별 고정 |
| K2 | P2 | 배지 | 카테고리(장치항/방법항), 라벨(목적/효과), 상태(채택 N/M, 확정됨, CAD 변환 완료), 종속 관계(제1항 종속) 모두 같은 pill 형태 | 3종 분리: **상태 = 아이콘+텍스트 pill**, **분류 = 사각 라벨(rounded-sm, 회색)**, **관계 = 텍스트만(회색 소문)** |
| K3 | P2 | 체크박스·라디오·토글 | 체크 20px(brand), 라디오 18px(blue), 세그먼트 토글(도 N/참고만) 별도 스타일 — 3종 3색 | `@muhayu/axp-ui` 컨트롤로 통일(색 brand), 세그먼트는 라디오 그룹 스타일 재사용 |
| K4 | P2 | 삭제 ✕ 위치 | 발명 설명(헤더 좌측 체크 자리) · 종속항(우측 끝) · 에디터 단락(우측 상단 호버) — 위치 3곳 | **항상 우측 끝, 호버 시 노출, red 호버** |
| K5 | P3 | 아이콘 | ✓·✕·▲▼·↻·↗·✎·✈ 가 SVG/유니코드 혼용 → 굵기·정렬 불일치 | Icon 컴포넌트 단일 세트(16px 1.5 stroke)로 교체 |

## 5. AI 어시스턴트 패널 (위저드·에디터 공통)

| # | 우선순위 | 문제 | 수정안 |
|---|---|---|---|
| A1 | **P1** | 두 영역 제목 바(확정 내용 / 대화)는 생겼지만 빈 대화 영역이 흰 공백 400~900px — 패널 대부분이 비어 보임 | 빈 상태 콘텍스트 카드: "이 단계에서 물어볼 수 있는 것" 예시 질문 3개(클릭 시 입력). 에디터는 "명령 예시"(더 간결하게 / 특허 문체로 / 구성요소 결합 관계 보강) |
| A2 | P2 | 확정 내용 카드가 패널 안에서 또 카드(테두리+헤더) — 이중 프레임 | 카드 테두리 제거, 정의 목록(dt/dd)만 12/13px로 |
| A3 | P2 | 헤더 48px + 영역 제목 바 32px + 카드 헤더 28px = 상단 108px가 제목만 | 영역 제목 바를 24px로, 카드 헤더는 A2로 제거 |
| A4 | P3 | 입력창 전송 버튼이 둥근 정사각(brand)인데 비활성 상태가 대부분이라 회색 사각으로 남음 | 비활성 시 아이콘만 회색(배경 없음), 입력 시 brand 채움 |

## 6. 에디터

| # | 우선순위 | 문제 | 수정안 |
|---|---|---|---|
| E1 | **P1** | 청구항 카드: 독립항 파란 배경+파란 테두리, 종속항 노란 테두리 들여쓰기 — 10항이면 파란/노란 줄무늬 | 독립항 = 좌측 3px brand 바 + 흰 배경, 종속항 = 들여쓰기 + 좌측 얇은 회색 바. 색은 배지에만 |
| E2 | P2 | 단락 카드 체크박스가 좌상단 밖으로 돌출(스크린샷 상단) → 카드 좌측 정렬선 깨짐 | 체크박스를 카드 안 좌측 패딩 영역에 넣고 본문은 `pl-9` |
| E3 | P2 | 섹션 제목 h2 아래 1px 선 + 카드 테두리 → 선 2겹 | 섹션 제목은 여백(32px)과 크기로만 구분, 선 제거 |
| E4 | P2 | `+ 단락 추가` 우측 정렬 점선 버튼이 다음 섹션 제목과 붙어 어느 섹션 소속인지 모호 | 카드 목록 바로 아래 **전폭 점선 행**(`+ 단락 추가`) — 소속 명확 |
| E5 | P3 | 툴바 힌트 "단락을 클릭하면 수식을 삽입할 수 있습니다"가 툴바 중앙에 상시 노출 | 수식 버튼 툴팁으로 이동, 툴바는 도구만 |
| E6 | P3 | 구성요소 하이라이트 `mark` 배경(brand-50) + 점선 밑줄 + 굵기 — 한 문장에 4개면 얼룩 | 배경 제거, **점선 밑줄 + brand-700 텍스트**만(클릭 가능 표시 유지) |

## 7. 반영 순서 제안

1. **P1 묶음(첫인상·가독성)**: T1 본문 14px → C1 아바타 brand 단색 → C2 카드 기본 테두리 중립 → K1 버튼 4종 고정 → L1·L2·L3 반응형(스텝바·툴바·패널 폭) → A1 빈 상태 예시 → E1 청구항 카드 색 절제
2. **P2 묶음(일관성·밀도)**: 회색 1종(C3) · blue 제거(C4) · amber 역할 축소(C5) · 배지 3종(K2) · 컨트롤 통일(K3) · 삭제 위치(K4) · 카드 헤더 컨트롤 호버화(L6) · 하단 바(L7) · 패널 비율(L4·A2·A3) · 에디터 체크박스·선·단락 추가(E2~E4)
3. **P3 묶음(마감)**: tabular-nums · 아이콘 세트 · 간격 스케일 · 힌트 툴팁화 · 하이라이트 절제 · 폼 빈 공간

> 검증 방법: 982px(분할 화면)·1280px·1920px 3폭에서 ① 발명 설명 ② 청구항(종속항 화면) ③ 에디터 청구범위 ④ AI 패널 빈 상태 스크린샷 비교. 목표: 파랑은 "행동/선택"에만, 회색 1종, 본문 14px, 버튼 4종.

## 8. 반영 현황

**P1 (2026-08-29 반영)** — T1 위저드 본문 14px(발명 설명·청구항·종속항·중간명세서) · C1 AI 아바타 brand 단색 + ✈(위저드·에디터 헤더·메시지) · C2 발명 설명 카드 기본 테두리 neutral 1px · K1(부분) 이동 버튼 ghost, 청구항 추가 버튼 brand/neutral, amber 제거 · L1 xl 미만 스텝바 비활성 단계 라벨 숨김 + 스크롤바 숨김 · L2 xl 미만 툴바 라벨 숨김(아이콘만) → 미리보기·DOCX·PDF 노출 · L3 AI 패널 폭 `clamp(280px,28vw,360px)` · A1 빈 대화 영역 예시 질문(위저드 단계별 3개)·명령 예시(에디터, 선택 유무별 3개) · E1 청구항 카드 좌측 바 + 흰 배경, 배지 brand/neutral.
**미반영(P1 잔여)**: K1 버튼 4종 전면 정리(반경 통일 포함), L3 패널 접기 토글, L1 현재 칩 자동 센터링.

**P2·P3 (2026-08-29 반영)** — C3 회색 `neutral` 1종(gray·zinc 0건) · C4 `blue-*` 전부 `brand-*`(0건) · C5 amber 경고(보완 필요·clarify·⚠ 번호 확인) 전용(6건 잔존) · L4 선택 단락 미리보기 `max-h 40vh`, 대화 영역 flex-1 · L7 하단 바 버튼 우측 정렬 + neutral-50 배경·상단 그림자 · A2 확정 내용 이중 프레임·중복 헤더 제거 · A3 영역 제목 바 py-1 · C6 확정 요약 행 흰 배경 · T4 7px 글자 배지 → 아이콘 · T5 스텝 번호 tabular-nums · E3 섹션 제목 lg2/선 제거 · E4 단락 추가 전폭 점선 행 · E5 툴바 힌트 제거(수식 툴팁으로) · E6 하이라이트 배경 제거(brand-700 텍스트 + 점선 밑줄) · L1 스텝바 스크롤바 숨김 위치 교정.
**미반영**: T2 제목 3단 스케일(위저드) · T3 안내 1형식 · K2 배지 3종 · K3 컨트롤 axp-ui 통일 · K4 삭제 위치 통일 · K5 아이콘 세트 · L5 본문 중앙 정렬 · L6 카드 헤더 컨트롤 호버화 · L8 간격 스케일 · L9 폼 빈 공간 · A4 전송 버튼 · E2 체크박스 위치(현 상태 카드 내 정렬 확인됨 — 보류).
**다음**: 미반영 항목은 사용자 확인 후 진행(구조 변경·컴포넌트 교체 포함).
