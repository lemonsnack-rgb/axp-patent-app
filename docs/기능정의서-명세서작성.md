# 기능정의서 — 명세서 작성 (CK.Patent)

| 문서 정보 | |
|---|---|
| 버전 / 일자 | v1 / 2026-08-31 |
| 작성 | 서비스기획 (읽는 사람: 개발) |
| 범위 | 명세서 작성 모듈 — 새 명세서 폼 · 7단계 작성(위저드) · 에디터(초안 생성 포함) · 도면 편집기 · AI 어시스턴트 |
| 기준 | **화면에 렌더되는 기능만** 기재(프로토타입 코드 대조 완료). 정의되지 않은 항목의 임의 추가 금지 |
| 용어 | `docs/용어집-명세서작성.md`(v2)를 따른다. 본문에서 처음 나오는 도메인 용어는 용어집 번호(A-07 등)로 참조 |
| 정본 대조 | 각 표의 `SPC` 열은 명세 카탈로그 ID(`docs/기능정의서.md`·`specCatalog.ts`·화면 `data-spec`과 1:1). **133개 ID 전수 포함** — 상세 사전/사후조건은 정본 참조 |
| API | `[AXP] jarvis-patent-api 매뉴얼 목차 (M2)` 기준. 현재 프로토타입은 전 구간 결정론적 목업이며, 표의 API는 **연동 시 이 자리에 붙는다**는 의미 |
| 범위 외 | **저장소·데이터 구조는 본 문서 범위가 아니다** — 목업의 저장 방식(localStorage 등)은 참고하지 말고 신규 설계한다. 본 문서는 UI(화면 기능·표시 기준)와 API 결합만 정의한다 |

---

## 1. 전체 이용 흐름

이용자(변리사·연구자)는 **직무발명서(A-01) PDF 하나**로 시작한다. AI가 뽑아 준 초안을 단계마다 **확인 → 수정 → 확정(B-03)** 하며 내려가고, 앞 단계의 확정 내용이 다음 단계 생성의 재료가 된다. **상태는 앞으로만 진행하며, 확정한 단계는 조회 전용**이다(2026-09-10 결정). 7단계(청구항)가 끝나면 곧바로 에디터로 넘어가고, 거기서 「초안 생성」을 한 번 눌러 실시예를 포함한 **명세서(A-02)** 를 만든 뒤 편집해 DOCX·PDF로 내보낸다. 모든 진행 상태는 자동 저장되어(B-15) 언제든 이어서 할 수 있다.

| 단계 | 이용자가 하는 일 | 시스템(AI)이 하는 일 | 이때 붙는 API |
|---|---|---|---|
| 작업 생성 | 작업 이름·기술분야(선택) 입력 후 「명세서 작성 시작」 | 작업(B-01)을 만들고 ①로 진입 | — (클라이언트 상태) |
| ① 업로드 | 직무발명서 PDF 1개 업로드 | 발명 설명·도면 이미지를 추출 | `POST /v2/extract/idf/description` · `POST /v2/extract/idf/drawing` — 페이지 이미지들을 보내고 설명 항목·도면(BBox 포함)을 받는다 |
| ② 발명 설명 | 항목을 제안/종래로 검토, 쓸 것만 채택·편집 | 원천 정보를 정제해 항목으로 제시, 요청 시 AI 수정(항목/전체) | `/v2/generate/context/description/refined`() · `…/description/item-modification`(항목 AI 수정) · `…/description/modification`(전체 AI 수정) |
| ③ 이미지 선별 | 맥락에 쓸 이미지 선택, 대표 이미지 1개 지정 | — (추출 결과 표시) | — (①의 추출 결과 사용, 선택 플래그는 클라이언트) |
| ④ 명칭·요약 | 명칭 후보 중 선택 또는 직접 입력, 요약 확정 | 명칭·요약·추천 이유 후보 생성 | 명칭·요약 생성 API(매뉴얼 미제공 — 연동 시 확인) |
| ⑤ 구성요소 | 명칭·정의 보완, 순서·계층 조정, 부호 부여 후 확정 | 발명 설명에서 구성요소 후보 추출 | `/v2/generate/context/invention-element`(추출·다시 분석) · `…/item-modification`·`…/modification`(AI 수정) |
| ⑥ 명세서 도면 | 도면 채택(도 N)·대표도면 지정, 필요 시 도면 편집기에서 크롭·CAD 변환 | — | `POST /generate/drawing/style-transfer`(CAD 변환 — §3.12) |
| ⑦ 청구항 | 권리범위 설정 → 독립항 세트 1개 선택 → 종속항 채택·편집 → 확정 | 세트 후보(최대 5개)·종속항 생성 | `/v2/generate/specification/independent-claim/set` · `…/independent-claim/modification` · `…/dependent-claim` · `…/dependent-claim/item-modification`·`…/modification` |
| 에디터 · 초안 생성 | 「초안 생성」 1회 클릭 후 대기 | 중간명세서(A-03) 4개 그룹을 순차 생성하고 완료되는 대로 본문에 채움 → 실시예(A-04)까지 | `/v2/generate/specification/drawing-description` → `…/background` → `…/disclosure` → `…/embodiment-description` (상태 조회 + 단계별 결과 조회) |
| 에디터 | 11개 섹션 편집, AI에 수정 명령, 내보내기 | 명령 의도를 분류해 수정 제안 | `POST /chat/specification/assistant`(+`_stream`) |

## 2. 화면 지도

```
[새 명세서 작성]  폼(작업 이름·기술분야) + 진행 방식 3단계 안내
      ↓ 명세서 작성 시작
[발명 정보 단계]                              [에디터]
 ┌─ 스텝바(7단계) ──────────────┐                ┌─ 툴바(편집 도구) ───────────────┐
 │  본문: 단계 화면 1개   │ AI 어시스턴트 │   ⇄ 이동    │  ←발명 정보 | 섹션 탭 11종      │
 │  (①~⑧ 세로 나열)  │ (작성 안내)  │                │  본문: 섹션·단락 카드 │ AI 어시스턴트│
 ├─ 하단 바: ←이전 · 주 동작 1개 ─┤                │                      │ (본문 수정)  │
 └──────────────────────────┘                └─ 명령 입력창 ────────────────────┘
      ⑥/에디터 도면 카드 → 새 탭 [도면 편집기]: 도면 목록 | 영역 확인(크롭) → CAD 변환 → 완료
```

## 3. 화면·영역별 상세

각 절의 표 형식: **기능 요소 | 표시 기준 | 이용자 동작 → 결과 | 연결 API | SPC**. AI 수정류의 공통 동작은 §3.13에 한 번만 정의한다.

### 3.1 새 명세서 작성 폼

**여기서 이용자가 하는 일** — 작업 하나를 만들고 시작한다. 두 입력 모두 선택 사항이라 빈 채로도 시작할 수 있다.

| 기능 요소 | 표시 기준 | 이용자 동작 → 결과 | 연결 API | SPC |
|---|---|---|---|---|
| 입력 폼 | `작업 이름 (선택)`(80자)·`기술분야 (선택)`(60자). 이름 아래 보조문 "발명의 명칭을 입력하면 명세서 작성 중 자동 반영됩니다." | 입력(Enter=시작) | — | WIZ-010 |
| 명세서 작성 시작 | primary 버튼, 항상 활성 | 클릭 → 작업 생성(미입력 시 이름 `새 명세서`) → ① 업로드 진입 | — | WIZ-011 |
| 취소 | outlined 버튼 | 클릭 → 최근 명세서 작업으로 복귀(없으면 홈) | — | WIZ-012 |
| 진행 방식 안내 | 폼 아래 3단계 카드(①직무발명서 업로드 ②발명 정보 7단계 확정 ③에디터에서 완성) | 정독 | — | (WIZ-010에 포함) |

### 3.2 발명 정보 단계 공통 프레임

**동작 원리** — **한 화면에 한 단계만** 띄우고 `← 이전` / `다음 →`으로 오간다(2026-09-11 결정, 구 스크롤 누적 방식 폐지). 보고 있는 단계와 진행 단계를 분리해, 지나온 단계를 열어 봐도 진행이 되돌아가지 않는다. 확정한 단계도 **직접 수정(텍스트·채택·순서·AI 수정)은 모두 가능**하다(2026-09-11 사용자 결정) — 잠기는 것은 후보/세트 **선택 변경**과 **다시 생성**뿐이며, 이 둘은 뒤 단계 리셋 팝업 대상(회의 0-2)으로 기획 확정 대기. **하단 바의 주 동작 1개**가 항상 "다음에 할 일"이며, 확정 시 0.9초 로딩 후 다음 단계 화면으로 넘어간다.

| 기능 요소 | 표시 기준 | 이용자 동작 → 결과 | 연결 API | SPC |
|---|---|---|---|---|
| 스텝바 | 상단 48px. 칩=번호+단계명: **보고 있는 단계**=brand 채움, 지나온 단계=초록 ✓, 미도달=회색(비활성). <1280px에서는 보고 있는 단계만 라벨. 스크롤바 숨김 | 도달한 단계 칩 클릭 → 해당 단계 **화면으로 전환**(진행 상태 불변) | — | WIZ-020·021 |
| ↺ 처음부터 다시 시작 | 스텝바 좌측 | 클릭 → 확인 다이얼로그 → 전 단계 초기화, ①로 | — | WIZ-022 |
| 단계 안내 | 각 단계 상단: **단계명(굵게)** + 할 일 1~2문장(좌측 파란 바) | 정독 | — | WIZ-030 |
| 확정 단계 머리띠 | 화면 맨 위 초록 띠: ✓ + `확정됨` + 1줄 요약(예: `구성요소 4개 확정`, `명세서 도면 2개 (도 1~2)`) + 우측 `직접 수정 가능 · 선택 변경·다시 생성 잠김`. 본문은 딤·잠금 없음 | 텍스트·채택·순서·AI 수정은 그 자리에서 바로 반영(진행 상태 불변). 잠김: 명칭 후보 선택·후보 다시 생성, 독립항 권리범위/슬롯·세트 선택·세트 생성·종속항 분량·종속항 다시 생성, 구성요소 다시 분석 | — | WIZ-040 |
| 하단 바 | 좌 `← 이전` + 중앙 `{N} / 7 · {단계명}` + 우: 지나온 단계를 보는 중이면 보조 `다음 →`(조회 이동), 진행 단계를 보는 중이면 주 동작 1개(`다음 →`·`독립항 세트 생성 →` 등). 비활성 시 좌측에 사유 힌트. 0개 진행(이미지·도면 없음)은 확인 다이얼로그 | 주 동작 클릭 → **확정** → 파생 처리(⑤도면 채택 프리셋 등) → `분석 중…`(0.9초) → 다음 단계 화면 | — | WIZ-050·051·052·053·060 |
| 확정 완료 화면 | ⑦ 청구항까지 모두 확정 시: "발명 정보를 모두 확정했습니다" + `에디터로 이동 →`(편집 이력 있으면 이어서 편집 안내) | 클릭 → 에디터 | — | WIZ-070 |
| 명세서 생성 오버레이 | 전체 화면: 스피너 + "명세서를 작성하고 있습니다" + 취소. **시간·단계 표시 없음**(오래 걸림을 강조하지 않는다는 결정) | 대기 → 완료 시 에디터 자동 이동 / 취소 → 토스트, ⑧ 유지 | `…/embodiment-description`(1분+) | WIZ-080 |
| 삭제·덮어쓰기 확인 | 모든 삭제=✕+확인(제목 `{대상} 삭제`) · 모든 재생성=`↻ 다시 ~`+확인 | 확인 후에만 실행 | — | WIZ-100 |
| 자동 저장 | **UI 요구**: 별도 저장 버튼·표시 없이 모든 편집·확정이 자동 보존되고, 작업 재진입 시 그 지점부터 복원된다 | — | 저장소·데이터 구조는 신규 설계(범위 외) | WIZ-110 |
| AI 어시스턴트(작성 안내) | 우측 패널(폭 조절). 「확정 내용」 정보 영역(`N/7 단계` + 단계별 1줄 요약, 비면 안내문) → 「대화 · 작성 안내」 영역 → 질문 입력창 | 단계 진행·작성 방법 질문(Enter) → 답변(의도 배지: 답변·방향 확인·미지원). **내용 수정 제안은 만들지 않고** 각 항목의 AI 수정으로 유도, 다단계 요청은 에디터로 안내. 단계 전환 시 대화 초기화 | (연동 시) `POST /chat/specification/assistant` | AST-010·011·012·030·033·050 |
| 사이드바 진행 배지 | 2상태: `발명 정보`(brand, 툴팁 `발명 정보 N/7 {단계명}`) / `명세서 생성완료`(초록) | 정독 | — | WIZ-120 |

### 3.3 ① 업로드

**여기서 이용자가 하는 일** — 직무발명서 PDF 1개를 올린다. 이것으로 이용자 입력은 끝이고 나머지는 추출 결과의 확인이다.

| 기능 요소 | 표시 기준 | 이용자 동작 → 결과 | 연결 API | SPC |
|---|---|---|---|---|
| 업로드 드롭존 | 점선 박스 "직무발명서 PDF를 여기에 끌어 놓으세요 / .pdf 지원" + 안내문 | 박스 클릭 또는 `파일 선택` → PDF 1개 선택(`.pdf`만). 작업 이름이 기본값이면 파일명으로 자동 개명. ⚠ 끌어 놓기 핸들러는 미구현(문구만) | — | UPL-010·011 |
| 분석 로딩 | 3단계 문구(PDF 텍스트 추출 중 → 발명 구성요소 파악 중 → 명세서 항목 분석 중) + 진행바 | 대기 → ② 발명 설명 열림 | `POST /v2/extract/idf/description` · `POST /v2/extract/idf/drawing` — PDF를 페이지 이미지(`ImageFile[]`)로 보내면 설명 항목(`InventionDescriptionItem[]`)과 도면 목록(BBox 포함)을 받는다 | UPL-020 |

### 3.4 ② 발명 설명

**여기서 이용자가 하는 일** — AI가 정제한 발명 설명(A-05)을 **제안기술/종래기술 2열**로 검토해, 명세서에 쓸 항목만 채택하고 내용을 다듬는다. 여기서 채택된 항목이 구성요소·청구항 생성의 근거다.

| 기능 요소 | 표시 기준 | 이용자 동작 → 결과 | 연결 API | SPC |
|---|---|---|---|---|
| 2열 항목 목록 | lg 이상 2열(헤더 `제안기술 채택 N/M`·`종래기술 …`), 미만은 탭. 카드=채택 체크+라벨 배지(배경기술·구성(해결수단)/종래 구성·목적·효과·기타)+본문. 미채택=흐림 | 카드 안 편집·정렬 | 초기 목록 = ①의 추출 결과 | DSC-010 |
| 채택 | 체크박스(AI 생성 항목만 — 직접 추가 항목은 삭제로 관리) | 토글 → 미채택은 이후 생성에서 제외 | — | DSC-011 |
| 순서·그룹 이동 | 드래그 핸들(반대편 열로도 이동) · ▲▼ · `← 제안기술로`/`종래기술로 →`(호버 노출) | 같은 열 순서 변경 또는 반대 열 이동 | — | DSC-012 |
| 본문 편집 | textarea(자동 높이), 채택 항목만 활성 | 입력 → 발명정보에 즉시 반영 | — | DSC-013 |
| 표 항목 | `표 캡션` 배지 + 원문 표 렌더 — 읽기 전용(편집·AI 수정 불가), 원문 그대로 명세서 반영 | 정독 | `InventionDescriptionItem.type=table` | DSC-017 |
| 항목 추가 | 열 하단: 라벨 select + 내용 입력 + `추가` | 추가 → 삭제 가능한 항목으로 등록 | — | DSC-016 |
| 항목 삭제 | ✕(직접 추가 항목만, 우측 호버) | 확인 후 제거 | — | DSC-015 |
| AI 수정 / 전체 AI 수정 | §3.13 공통 | 항목 1개 / 채택 항목 전체 수정 제안 | `…/description/item-modification` / `…/description/modification` | DSC-014·020 |

### 3.5 ③ 이미지 선별

**여기서 이용자가 하는 일** — 추출 이미지 중 **AI 맥락에 쓸 것을 "선택"**(B-06)하고 **대표 이미지 1개**를 지정한다. 미선택 이미지는 이후 생성에서 완전히 빠진다. 명세서 게재 여부(채택)는 ⑥에서 정한다.

| 기능 요소 | 표시 기준 | 이용자 동작 → 결과 | 연결 API | SPC |
|---|---|---|---|---|
| 이미지 목록 | 카운트 `추출된 이미지 N개 · 선택 M개` + 카드 그리드. 썸네일은 BBox로 크롭해 표시, `이미지 N`(툴팁 `원본 기호:`)·분류·이름 | — | ①의 추출 결과(`Drawing[]`) | IMG-010 |
| 선택 | `선택` 체크 — 미선택은 흐림, 해제 시 대표도 함께 해제 | 토글 | `included`(클라이언트 플래그) | IMG-011 |
| 대표 | `대표` 라디오(1개만, 재클릭 해제). 확정 시 미지정이면 첫 선택 이미지 자동 지정 | 클릭 | `isRepresentative` — 이후 생성 API에 대표도로 전달 | IMG-012 |
| 분류 | select: 제안기술·종래기술·배경·효과·기타 | 변경 → `Drawing.detail.label` | label enum | IMG-013 |
| 삭제 / 새 탭 보기 | ✕(확인 다이얼로그) / ↗(호버, 크롭 PNG 새 탭) | — | — | IMG-014·015 |
| 이미지 추가 (로컬 업로드) | 하단 버튼(`image/*`) | 파일 선택 → 목록 끝 추가(선택됨·이름=파일명) | — | IMG-020 |

### 3.6 ④ 명칭·요약

**여기서 이용자가 하는 일** — AI 명칭 후보 중 하나를 고르거나 직접 입력한다. 후보에는 요약과 추천 이유가 붙어 있고, 확정한 명칭·요약(A-20)은 발명정보가 되어 이후 모든 생성과 요약서에 쓰인다.

| 기능 요소 | 표시 기준 | 이용자 동작 → 결과 | 연결 API | SPC |
|---|---|---|---|---|
| 명칭 후보 카드 | 라디오+명칭, 아래 `요약` 행·`추천 이유`(이탤릭). 기호(A/B/C) 없이 명칭 자체가 선택지. 첫 후보 기본 선택. **선택된 후보의 명칭·요약은 인라인 textarea로 직접 수정**(확정 후에도 가능, TTL-014·015) | 카드 클릭 → 선택 명칭·요약이 발명정보로 전파 | 명칭·요약·추천 이유 = 생성 API 응답(매뉴얼 미제공 — 연동 시 확인) | TTL-010·011·013 |
| AI 수정(명칭·요약) | §3.13 공통 — 대상 2개(명칭·요약) 동시 제안 | 해당 후보의 명칭·요약 교체 | 〃 | TTL-012 |
| 직접 입력 | 라디오 `직접 입력` + 명칭 textarea + `요약 (선택 — 요약서에 반영)` | 입력 → 후보 선택 해제, 입력값 전파 | — | TTL-020 |
| ↻ 후보 다시 생성 | 목록 하단 | 클릭 → 후보 재생성(선택 전 목록이라 확인 없음) | 〃 | TTL-030 |

### 3.7 ⑤ 구성요소

**여기서 이용자가 하는 일** — 구성요소(A-07) 목록을 검토해 쓸 것만 채택하고, 명칭·영문명·상위어·정의를 채운 뒤 순서·계층을 정하고 **부호(A-08)를 부여**한다. **명칭은 문서 전체의 원천** — 이후 어디서 바꾸든 전부 함께 바뀐다.

| 기능 요소 | 표시 기준 | 이용자 동작 → 결과 | 연결 API | SPC |
|---|---|---|---|---|
| 구성요소 목록 | 헤더 `구성요소 목록`. 카드=채택 체크+드래그 핸들+부호 배지(`100`/`—`)+5개 필드+우측 컨트롤. 계층은 16px 들여쓰기. 확정 후 읽기 전용+`구성요소 확정` 배너 | — | 초기 목록 = `/v2/generate/context/invention-element` | CMP-010 |
| 채택 | 체크 — 미채택=흐림·부호 없음·청구항 기준 제외. **이름 없는 미채택은 즉시 제거**(개별 삭제 버튼 없음) | 토글 | — | CMP-011 |
| 순서·계층 | 드래그·▲▼·`하위로(→)`/`상위로(←)`(최대 3계층) | 변경 → 부호 자동 재계산 | — | CMP-012 |
| 필드 편집 · 명칭=전역 치환 | 명칭/명칭 영문명/상위어/상위어 영문명/정의. **명칭은 포커스 아웃 시** 이전 값과 비교 | 명칭 변경 → 발명 설명·명칭·요약·청구항·에디터 본문 전부 치환(부호 보존·긴 이름 우선) + 토스트 `'A' → 'B' — 본문 N곳도 함께 바꿨습니다` | 치환은 클라이언트, 원천=`InventionElement.value_ko` | CMP-013 |
| 도구 모음 | 헤더 우측 `↻ 다시 분석`(확인 후 전체 대체)·`부호 자동 부여`(100·200/110·120/111…) + 하단 `구성요소 추가`(빈 카드) | — | 다시 분석=`invention-element` 재호출 · 부호는 클라이언트 부여 후 `symbol`로 전달 | CMP-020·021·022·023·024 |
| AI 수정 / 전체 AI 수정 | §3.13 공통(정의 대상) | — | `…/invention-element/item-modification` / `…/modification` | CMP-014·030 |

### 3.8 ⑥ 명세서 도면

**여기서 이용자가 하는 일** — ③에서 선택한 이미지가 전부 **도면으로 기본 채택**되어 온다(대표 1개 포함 프리셋). 명세서에 싣지 않을 것은 「참고만」으로 돌리고, 필요하면 도면 편집기(§3.12)에서 크롭·CAD 변환한다.

| 기능 요소 | 표시 기준 | 이용자 동작 → 결과 | 연결 API | SPC |
|---|---|---|---|---|
| 도면 목록 | 카운트 `관련 이미지 N개 · 명세서 도면 M개` + 카드. 채택 도면은 `도 N`(채택 순 자동 번호) | — | — | DRW-010 |
| 도면 채택 / 참고만 | 세그먼트 토글. 채택=도 번호 부여·CAD 변환 대상 / 참고만=AI 참고용(번호 없음·대표 해제) | 클릭 → 번호 재부여 | `useForSpec`(클라이언트) | DRW-011 |
| 대표도면 | `대표` 라디오(채택 도면만) → 요약서 `【대표도】 도 N`·도면 설명 `(대표도면)` | 클릭 | `isRepresentative` | DRW-012 |
| 분류 | select(제안기술·종래기술·배경·효과·기타) — 에디터 도면 카드 배지와 동일 표기 | 변경 | label enum | DRW-013 |
| 도면 편집기 진입 | `도면 편집기에서 조정·변환 ↗`(채택 도면만) | 클릭 → 새 탭(§3.12). 결과는 실시간(또는 재진입 시) 반영 | — | DRW-014 |
| CAD 변환 완료 배지 | 변환 결과 수신 시 초록 배지 + 썸네일이 변환본으로 교체 | 정독 | `cadConverted` | DRW-015 |

### 3.9 ⑦ 청구항

**여기서 이용자가 하는 일** — 3구역을 차례로 지난다. ⑴ **권리범위 설정**(A-15): 추상화 수준(넓은/균형/한정)과 청구항 구성(장치항·방법항… 2~3슬롯)을 정하고 → ⑵ **독립항 세트**(A-14): 하단 「독립항 세트 생성 →」으로 후보를 만들어 **1개 선택** → ⑶ **종속항**: 「종속항 구성 →」으로 독립항별 종속항을 받아 채택·편집 후 「청구항 확정 →」.

| 기능 요소 | 표시 기준 | 이용자 동작 → 결과 | 연결 API | SPC |
|---|---|---|---|---|
| 권리범위 설정 | 용어 도움말(추상화 수준·청구항 구성 설명) + 추상화 수준 3버튼 + `청구항 구성 (n/3)` 슬롯(select+방향·강조점 입력+✕, `+ 청구항 추가`, 최소 2개) + `추가 지시사항 (선택)` | 수준·구성 변경 → 생성 결과 초기화(재생성 필요) · 슬롯 삭제는 입력값 있으면 확인 | 요청 `Preference{abstraction_level, claims[{category,description}]}` + `instruction` | CLM-010·011·012·013 |
| 독립항 세트 목록 | 생성 후: 세트 카드 `세트 N · {권리범위}` + 부제(카테고리 조합·구성요소 수) + 항별 카테고리 배지·본문(구성요소 하이라이트). 조건 불일치 시 "선택한 조건에 맞는 세트가 없습니다." | 카드 클릭 → `선택됨`(1개만) → 하단 「종속항 구성 →」 활성(미선택 힌트) | `/v2/generate/specification/independent-claim/set` — 발명정보+선호를 보내면 세트 최대 5개(넓은2·균형2·한정1)를 받는다 | CLM-020·021 |
| 독립항 편집·세트 단위 AI 수정(독립항 세트 수정 제안 바 — independent-claim/modification) | 선택 세트의 항만 textarea 편집 가능 | §3.13 공통 | `…/independent-claim/modification` | CLM-022 |
| 종속항 그룹 | 요약 `{권리범위} — 독립항 N개, 종속항 M개` + 독립항별: 항 본문 → `종속항 · 채택 K`+`↻ 다시 생성` → 종속항 행(체크·통산 번호·**인용부**(접두/번호/접미 3칸)+본문·**연관 구성요소 칩**(부호+명칭)) | `← 세트 다시 선택`으로 ⑵ 복귀 가능 | `/v2/generate/specification/dependent-claim` — 독립항들+`claim_count_level`+`instruction`을 보내면 항별 종속항(`depends_on`·`element_idxs`)을 받는다 | CLM-030·031 |
| 종속항 분량·생성 지시 | `적게(10~14항)/보통(13~17)/많이(16~20)` + `생성 지시사항 (선택)` — 변경 시 재생성(편집 소실 확인) | 클릭·입력 | `claim_count_level`·`instruction` | CLM-032 |
| 종속항 인용부 | 한 행 3칸 — 접두(기본 `제`) · **인용 항 번호(필수·숫자)** · 접미(기본 `항에 있어서,`). 접두·접미는 영문 작성용으로 수정 가능(`In claim 1,` — 숫자 앞뒤 공백 자동). 번호를 비우면 자기 독립항의 통산 번호를 따라간다 | 오류 시 빨간 테두리+사유(`인용 항 번호를 입력하세요` / `숫자만 입력하세요` / `제N항은 인용할 수 없습니다 (선행 항만 인용 가능)`) → 하단 「청구항 확정 →」 비활성 + 힌트 `인용 항 번호를 확인하세요 (N건)` | `DepClaimItem.depends_on`(숫자)에 대응 — 자유 텍스트에 섞지 않는다 | CLM-039 |
| 종속항 채택·편집·AI 수정·삭제·추가·전체 AI 수정 | 채택 체크 / 본문 textarea(인용부와 분리) / §3.13 / ✕ 확인("빼기만 하려면 체크 해제") / `제 [번호] 항에 있어서,`+본문 입력+`추가` / 상단 바 | — | `…/dependent-claim/item-modification`·`…/modification` | CLM-033·034·035·036·037·038 |
| (제외) 선행 근거 확인 | `ENABLE_ANTECEDENT_CHECK=false` — API에 검증 기능 없음 | 미노출 | — | CLM-040 |

### 3.10 에디터 · 초안 생성

**여기서 이용자가 하는 일** — 청구항 확정과 동시에 에디터로 넘어온다. 상단 띠의 **「초안 생성」을 한 번** 누르면 중간명세서(A-03)부터 실시예(A-04)까지 한 번에 작성되어 본문에 채워진다. 진행 상태는 **작성 순서대로** 보여 주고(표기는 「생성」이 아니라 「작성」), 완료되는 그룹부터 화면에 출력한다. **재작성은 없다** — 토큰 비용 때문에 1회만 허용하고, 이후 수정은 에디터에서 한다(2026-09-10 결정).

| 기능 요소 | 표시 기준 | 이용자 동작 → 결과 | 연결 API | SPC |
|---|---|---|---|---|
| 초안 생성 띠 (미작성) | 섹션 탭 아래 띠: "명세서 초안이 아직 작성되지 않았습니다" + "확정한 구성요소·도면·청구항을 바탕으로 도면의 간단한 설명부터 실시예까지 작성합니다. **작성은 한 번만 가능**하며, 이후 수정은 에디터에서 진행합니다." + `초안 생성` | 클릭 → 작성 시작. **같은 버튼을 본문 맨 아래에도 둔다**(안내를 다 읽고 내려온 자리에서 바로 실행, 2026-09-11) | 아래 4종 순차 호출 | EDT-075·076 |
| 작성 중 | 스피너 + `명세서 초안을 작성하고 있습니다 — {그룹}` + `N / 4` + **작성 순서 칩 4개**(완료=초록 ✓ · 현재=brand `작성 중…` · 대기=회색) + 진행 바. 본문은 흐림 처리되고 편집이 잠긴다. **표기는 「생성」이 아니라 「작성」**(사용자 결정) | 대기 | 상태 조회 API로 그룹 완료를 확인 | EDT-075 |
| 작성 순서(4그룹) | ⑴ 도면의 간단한 설명 ⑵ 기술분야 · 배경기술 ⑶ 발명의 내용(해결과제·해결수단·효과) ⑷ 실시예 — **완료되는 그룹부터 순서대로** 채워지고, 방금 채운 섹션으로 화면이 자동 스크롤되어 결과가 눈에 보인다 | — | `…/drawing-description` → `…/background` → `…/disclosure` → `…/embodiment-description`. 뒤 단계 호출에 앞 단계 결과를 함께 넘기므로 실제로도 이 순서로 완료된다 | EDT-075 |
| 작성 완료 | 띠가 사라진다(재진입해도 복귀하지 않음) + 토스트 `명세서 초안을 작성했습니다` | 이후 수정은 에디터 편집·AI 어시스턴트로 | 결과를 본문·저장소에 반영 | EDT-075 |
| 작성 대상 제외 | **명칭 · 부호의 설명 · 청구범위 · 요약** — 위저드에서 확정한 값을 그대로 쓰므로 생성하지 않는다 | — | 클라이언트 조립(유지 결정) | EDT-075 |

### 3.11 에디터

**여기서 이용자가 하는 일** — 완성 초안을 11개 섹션에서 다듬는다. **단락 클릭=직접 편집, 체크=AI 명령 대상 지정** — 두 조작이 명확히 분리돼 있다. 구성요소 하이라이트를 클릭하면 문서 전체 이름 변경.

| 기능 요소 | 표시 기준 | 이용자 동작 → 결과 | 연결 API | SPC |
|---|---|---|---|---|
| 툴바 | 편집 도구만: 실행 취소·다시 실행(Ctrl+Z/Y, 20단계) ｜ 표 삽입 ｜ ∑ 수식 ｜ 찾기(Ctrl+F) …… 미리보기·DOCX·PDF. <1280px는 아이콘만 | 표=3열 마크다운 모달(행 1~10) · 수식=TeX 모달(인라인/블록, 미리보기, 템플릿 15종) · 찾기=바꾸기 바+문서 통계 `N단락 · N자` | 표=`SpecificationBlock.type=table` · 수식=`type=equation` | EDT-060·061·062·063·066 |
| 내보내기 | 미리보기 모달(`【섹션명】`+`【도면】`) · DOCX 즉시 다운로드 · PDF 인쇄 창 — 모두 도면 포함 | 클릭 | 클라이언트 생성 | EDT-064·065 |
| 내비게이션 행 | 좌측 고정 `← 발명 정보`(편집 내용 유지) + 섹션 탭 11종(스크롤 추적 활성 표시) | 탭 클릭 → 해당 섹션으로 스크롤 | — | EDT-070·071·072 |
| 본문 단락 카드 | 섹션 제목 아래 카드. 상태 테두리: 편집 중/체크됨/빈 단락(점선). 읽기 모드는 표·수식·하이라이트 렌더 | 클릭=편집(자동 저장 0.5초) · 체크=AI 대상(패널 연동) · ▲▼ 이동 · ✕ 삭제(확인+실행 취소 가능) · `+ 단락 추가`(전폭 점선) | — | EDT-080·081·082·083·084·085 |
| 구성요소 하이라이트 → 이름 전체 변경 | brand색+**점선 밑줄=클릭 가능**. 클릭 → 모달 「구성요소 이름 전체 변경」("…모든 언급이 한 번에 바뀝니다. 부호는 유지됩니다.") | 새 이름 입력 → 전체 변경 → 본문+작성 단계 전체 치환+토스트 | 클라이언트(원천=`value_ko`) | EDT-086 |
| 청구범위 편집기 | 항 카드: `청구항 N`+배지(독립항/제N항 종속/⚠ 번호 확인)+좌측 바. 이동·삭제 시 인용 자동 재작성, 종속항 딸린 삭제는 「함께 삭제/이 항만 삭제」 선택 | 항 클릭 편집 · **항 체크=세트 단위 AI 수정(독립항 세트 수정 제안 바 — independent-claim/modification) 대상**(단락과 동일 — 체크 후 명령하면 그 항에 직접 제안, 미체크로 청구항 언급 시 독립항→종속항 2단계 플랜) · ▲▼ · ✕ · `+ 독립항/종속항 추가` | `ClaimSet/ClaimItem` 직렬화 | EDT-090·091·092·093·094 |
| 특수 섹션 | 도면설명=채택 도면 카드(도 N·분류 배지·`CAD 변환 완료`·`도면 편집기 ↗`) · 부호의 설명=`100: 명칭` 자동 조립 · 요약=`【대표도】` 포함 — 이후 일반 단락처럼 편집 | — | 부호의 설명·요약은 클라이언트 조립(유지 결정) | EDT-100·101 |
| AI 어시스턴트(본문 수정) | 우측 패널(접기 토글 ↔ 세로 레일). 「선택 단락」 정보 영역(1개=미리보기/여러 개=목록/0개=전체 문서 대상) + 「대화 · 본문 수정」 | 명령 입력(Enter) → 의도 분류(`요청 분석 중...` 트리) → 의도별 응답 | `POST /chat/specification/assistant`(+stream) — 명령+선택 단락 참조를 보내면 `SpecificationEditorChatMessage`(intent·제안·플랜)를 받는다 | EDT-010·011·030·050·110 |
| 수정 제안 카드 | 액션 배지(재작성/치환/삽입/삭제)+대상+Before/After diff+`✓ 적용`/`✕ 취소`+보강 지시 입력+`↻ 다시 생성`+`↗ 확대` | 적용 → 단락 반영(실행 취소 가능) | `edit_proposals` | EDT-031·032 |
| 방향 확인 / 플랜 / 용어 교체 / 미지원 | 칩 선택 / `플랜 · n/N 단계`+진행·중단(청구항은 독립항→종속항 2단계 고정, 진행 중 입력 잠금) / `A → B` 행별 반영·무시 / 안내 후 종료 | — | intent=clarify/plan/replace_expression/terminate | EDT-033·040·034·041 |

### 3.12 도면 편집기 (새 탭 · 범위: 크롭 → CAD 변환)

**여기서 이용자가 하는 일** — 도면 1개씩 2단계: ⑴ **영역 확인** — 추출 BBox가 초기값으로 잡힌 크롭 박스를 조정하고 명칭·분류·캡션·변환 지시문을 다듬는다 ⑵ **CAD 변환** — 후보 3개(버전 A/B/C) 중 1개를 골라 **「✓ 이 버전으로 CAD 변환 완료」**. 완료 화면(초록 ✓)이 뜨고 메인 탭 도면 카드에 배지·변환본이 반영된다. 도면 편집(부호 배치)은 이번 버전 제외.

| 기능 요소 | 표시 기준 | 이용자 동작 → 결과 | 연결 API | SPC |
|---|---|---|---|---|
| 도면 목록(좌) | 도면별 진행 배지: 영역 확인·변환 중·후보 선택·`변환 완료`(초록) | 클릭 → 도면 전환 | — | (DRW-014 하위 — 새 탭 내부) |
| 영역 확인 | 원본 위 파란 크롭 박스(8핸들·이동·크기 %표시). **추출/조정 BBox가 초기값**, 좌표는 이미지 실제 크기 기준. 우측: 명칭·분류·캡션·`변환 지시문 (선택)` · `영역 초기화` | 「영역 확인 완료 — CAD 변환 시작」 | 요청에 `Drawing`+`bbox`(원본 px)+`user_instruction` | 〃 |
| CAD 변환·후보 선택 | 변환 중 스켈레톤 → 원본↔선택본 비교 + 하단 후보 3개(버전 A/B/C, 확대 보기) + `다시 변환`(지시문 수정) | 후보 클릭 → 「✓ 이 버전으로 CAD 변환 완료」 | `POST /generate/drawing/style-transfer`(`num_images=3`) → 후보 이미지 3개. `provider`·`quality`는 기본값 사용(UI 미노출 결정) | 〃 |
| 변환 완료 화면 | 초록 ✓ + "선택한 변환본이 명세서 도면에 반영되었습니다" + 변환본 미리보기 + `↻ 다시 변환 (영역부터)`/`완료 — 닫기` | — | 결과(BBox·명칭·분류·캡션·변환본)는 해당 작업의 도면에 반영 — 메인 화면이 열려 있으면 즉시, 아니면 재진입 시(전달 방식은 신규 설계) | DRW-015 연계 |

### 3.13 AI 수정 공통 규칙 (전 화면 동일)

모든 AI 수정은 **① 지시 입력 → ② 제안 카드 → ③ 적용/취소** — 즉시 반영은 없다(플래그 `AI_EDIT_CONFIRM`로 일괄 전환 가능). (공통 영역: WIZ-090)

| 기능 요소 | 표시 기준 | 이용자 동작 → 결과 | SPC |
|---|---|---|---|
| AI 수정(인라인) | 항목 우측 ✈ 버튼(호버 노출, 채택 항목만) → 지시 입력 카드(`수정 요청`/`취소`, Enter 전송·Esc 닫기) | 전송 → 제안 카드 | WIZ-091 |
| 전체 AI 수정 바 | 패널 상단 1줄: 지시 입력+`전체 AI 수정`. 제안 0건이면 "변경이 필요 없다고 판단했습니다" | 전송 → 채택 항목 전체 제안 | WIZ-092 |
| 제안 확인 카드 | 헤더(N건·지시사항) + 항목별 태그(수정/추가/삭제) + **Before/After 단어 diff**(삭제=빨강 취소선·추가=초록) + `✦ 설명` + `취소`/`적용` | 적용 → 반영+토스트 / 취소 → "수정을 적용하지 않았습니다" | WIZ-093 |

## 4. API 연동 맵 (역방향: API → 쓰는 곳)

| API (M2) | 쓰는 화면·기능 | 요청에 담는 것 → 응답을 표시하는 곳 |
|---|---|---|
| `POST /v2/extract/idf/description` | ① 업로드 | 페이지 이미지들 → ② 발명 설명 항목 목록 |
| `POST /v2/extract/idf/drawing` | ① 업로드 | 페이지 이미지들 → ③ 이미지 목록(BBox는 썸네일 크롭·도면 편집기 크롭 초기값) |
| `/v2/generate/context/description/refined` | ② (이번 버전 미사용 — 「다시 정제」 제거, 8/31 번복) | — |
| `/v2/generate/context/description/item-modification`·`/modification` | ② AI 수정 / 전체 AI 수정 | 항목(들)+지시 → 제안 카드 Before/After |
| 명칭·요약 생성 API (매뉴얼 미제공) | ④ 후보·다시 생성·AI 수정 | 발명정보 → 후보 카드(명칭·요약·추천 이유) |
| `/v2/generate/context/invention-element` | ⑤ 초기 추출·다시 분석 | 채택 발명 설명 → 구성요소 목록 |
| `…/invention-element/point` | (이번 버전 미사용 — 이미지 내 구성요소 좌표 예측 API로, 구 AI 추가와의 매핑은 오류였음) | — |
| `…/invention-element/item-modification`·`/modification` | ⑤ AI 수정 / 전체 AI 수정 | 요소(들)+지시 → 제안 카드 |
| `/v2/generate/specification/independent-claim/set` | ⑦ 독립항 세트 생성 | 발명정보+`Preference`+`instruction`(+대표도 최대 3) → 세트 카드 최대 5개 |
| `…/independent-claim/modification` | ⑦ 독립항 세트 단위 AI 수정(독립항 세트 수정 제안 바 — independent-claim/modification) | 항+지시 → 제안 카드 |
| `/v2/generate/specification/dependent-claim` | ⑦ 종속항 구성·분량 변경·다시 생성 | 독립항들+`claim_count_level`+`instruction` → 종속항 행(연관 구성요소 칩=`element_idxs`) |
| `…/dependent-claim/item-modification`·`/modification` | ⑦ 종속항 AI 수정 / 전체 | 항(들)+지시 → 제안 카드 |
| `/v2/generate/specification/background`·`/disclosure` | ⑧ 초기 생성·다시 생성 | 확정 발명정보 → 섹션 단락들 |
| `…/drawing-description` | ⑦ 확정 시 자동·⑧ 도면 설명 다시 생성 | 발명정보+명세서(도면 symbol 필수)+`instruction` → 「도면의 간단한 설명」(idxs 묶음 문장) |
| `…/embodiment-description` | ⑧ 명세서 생성(오버레이) | 확정 전체 → 에디터 「구체적 내용」 섹션 |
| `POST /generate/drawing/style-transfer` | 도면 편집기 CAD 변환 | `Drawing`+BBox+`user_instruction`+`num_images=3` → 후보 3개(버전 A/B/C) |
| `POST /chat/specification/assistant`(+`_stream`) | 에디터 AI 어시스턴트 | 명령+선택 단락 → intent별 응답(제안·플랜·교체·답변) |

**대표 시퀀스 — 위저드 한 단계(⑤→⑦)**: 확정(⑤) → `independent-claim/set` 요청 → 세트 선택 → `dependent-claim` 요청 → 채택·편집 → 확정(⑦) → `drawing-description` 요청(도면 설명 자동 생성).
**대표 시퀀스 — CAD 변환**: 도면 카드 ↗ → 새 탭(작업·도면·BBox 컨텍스트 전달) → 크롭 조정 → `style-transfer`(bbox·지시문·3장) → 후보 선택·완료 → 메인 화면 도면 카드에 변환 완료 배지+변환본 썸네일 반영(부재 시 재진입 때).

## 5. 저장·복원 (UI 요구만 — 구조는 신규 설계)

목업의 저장 방식은 설계 참고 대상이 아니다. UI가 요구하는 것만 정의한다:

- **자동 저장**: 저장 버튼·저장됨 표시가 없다. 모든 편집·확정·선택이 즉시 보존된다.
- **재진입 복원**: 작업을 다시 열면 떠났던 지점(단계·확정 내용·에디터 본문·화면 위치)부터 이어진다. 사이드바 배지(분석 중/명세서 생성완료)도 저장 상태에서 산출된다.
- **복원 범위 결정 필요(신규 설계 시)**: ⑦ 청구항의 중간 선택(세트 선택·종속항 채택·권리범위 설정), 구성요소 계층, AI 대화 이력 — 목업은 확정 텍스트만 유지하나, 서비스에서 어디까지 복원할지는 데이터 설계에서 정한다.

## 6. 이번 버전 제외 (플래그·결정)

| 항목 | 플래그/근거 | 사유 |
|---|---|---|
| 도면 참조 삽입(`(도 N 참조)`) | `ENABLE_DRAWING_REF=false` | API 대응 기능 없음 |
| 선행 근거 검증 경고 | `ENABLE_ANTECEDENT_CHECK=false` | API에 검증 기능 없음 |
| 도면 편집(부호 배치) | `ENABLE_DRAWING_EDIT=false` | API 대응 기능 없음 — 범위=CAD 변환까지 |
| 다중 인용 종속항 | — | `depends_on` 리스트는 API 지원, UI는 단일 인용(연동 시 파싱·재작성 정의) |
| 선행 문헌 청구항 입력(`prior_art_claims`) | — | 매뉴얼상 "M2 미사용 허용" |
| CAD 변환 `provider`/`quality` | — | 기본값(auto) 사용, UI 미노출 결정 |

> 결정 이력 전체: `docs/API-대조-클라이언트-임의항목-2026-08-28.md`
